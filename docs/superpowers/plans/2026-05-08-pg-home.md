# PG Home Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PG 사용자가 `/home`에서 KPI 4개 + 2컬럼 리스트(응답 대기 · 최근 제출)를 보는 대시보드를 표시한다.

**Architecture:** `home/page.tsx`가 `workspaceType`에 따라 `<BuyerHome />` 또는 `<PgHome />`으로 분기. `PgHome`은 invitation repo + bid repo를 병렬로 호출. 데이터 계산 로직은 `lib/server/pg-home.ts`의 `computePgHomeData` 순수 함수로 추출해 Vitest로 단위 테스트.

**Tech Stack:** Next.js 16.2 App Router RSC, Drizzle ORM (InvitationRepo + BidRepo), Vitest, Tailwind v4 + CSS Variables (Korean Editorial Modernism)

**Spec:** `docs/superpowers/specs/2026-05-08-pg-home-design.md`

---

## File Map

| 파일 | 변경 |
|---|---|
| `lib/server/pg-home.ts` | 신규 — `computePgHomeData` 순수 함수 |
| `lib/server/__tests__/pg-home.test.ts` | 신규 — Vitest 단위 테스트 |
| `components/home/BuyerHome.tsx` | 신규 — `page.tsx`에서 buyer 콘텐츠 추출 |
| `components/home/PgHome.tsx` | 신규 — PG 대시보드 RSC |
| `app/(app)/home/page.tsx` | 수정 — 분기 로직으로 교체 |
| `app/(public)/login/page.tsx` | 수정 — PG redirect 단순화 |

---

## Task 1: `computePgHomeData` 순수 함수 + Vitest 테스트

**Files:**
- Create: `lib/server/pg-home.ts`
- Create: `lib/server/__tests__/pg-home.test.ts`

- [ ] **Step 1-1: 테스트 파일 작성 (먼저 실패하는 테스트)**

`lib/server/__tests__/pg-home.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { computePgHomeData } from '../pg-home';
import type { RfpInvitation } from '@/lib/types/invitation';
import type { RFP } from '@/lib/types/rfp';
import type { Bid } from '@/lib/types/bid';

function makeRfp(id: string, overrides: Partial<RFP> = {}): RFP {
  return {
    id,
    buyerWsId: 'ws-buyer',
    title: `RFP ${id}`,
    memo: '',
    rfpFiles: [],
    allowedPgEmails: [],
    deadline: new Date(Date.now() + 7 * 24 * 3600 * 1000).toISOString(),
    status: 'sent',
    createdBy: 'user-1',
    createdAt: new Date().toISOString(),
    ...overrides,
  };
}

function makeInv(id: string, rfpId: string, status: RfpInvitation['status']): RfpInvitation {
  return {
    id,
    rfpId,
    pgEmail: 'pg@test.com',
    uniqueToken: '',
    sentAt: new Date().toISOString(),
    expiresAt: new Date(Date.now() + 7 * 24 * 3600 * 1000).toISOString(),
    status,
  };
}

function makeBid(id: string, rfpId: string, submittedAt: string): Bid {
  return {
    id,
    rfpId,
    pgWsId: 'ws-pg',
    invitationId: `inv-${id}`,
    settleCycle: 'D+1',
    deposit: 0,
    setupFee: 0,
    monthlyMin: 0,
    bankTransferFeePct: 0.003,
    easyPayFeePct: 0.015,
    proposalPdf: { id: '', name: '', size: 0, mimeType: '', url: '' },
    status: 'submitted',
    submittedBy: 'user-1',
    submittedAt,
  };
}

describe('computePgHomeData', () => {
  it('KPI: total = 전체 pair 수', () => {
    const pairs = [
      { invitation: makeInv('i1', 'r1', 'sent'), rfp: makeRfp('r1') },
      { invitation: makeInv('i2', 'r2', 'accepted'), rfp: makeRfp('r2') },
    ];
    const { kpi } = computePgHomeData(pairs, []);
    expect(kpi.total).toBe(2);
  });

  it('KPI: pending = sent + opened 상태', () => {
    const pairs = [
      { invitation: makeInv('i1', 'r1', 'sent'), rfp: makeRfp('r1') },
      { invitation: makeInv('i2', 'r2', 'opened'), rfp: makeRfp('r2') },
      { invitation: makeInv('i3', 'r3', 'accepted'), rfp: makeRfp('r3') },
    ];
    const { kpi } = computePgHomeData(pairs, []);
    expect(kpi.pending).toBe(2);
  });

  it('KPI: submitted = accepted 상태', () => {
    const pairs = [
      { invitation: makeInv('i1', 'r1', 'accepted'), rfp: makeRfp('r1') },
      { invitation: makeInv('i2', 'r2', 'sent'), rfp: makeRfp('r2') },
    ];
    const { kpi } = computePgHomeData(pairs, []);
    expect(kpi.submitted).toBe(1);
  });

  it('KPI: won = rfp.awardedBidId가 자사 bid인 건 수', () => {
    const pairs = [
      { invitation: makeInv('i1', 'r1', 'accepted'), rfp: makeRfp('r1', { awardedBidId: 'b1', status: 'awarded' }) },
      { invitation: makeInv('i2', 'r2', 'accepted'), rfp: makeRfp('r2', { awardedBidId: 'b-other', status: 'awarded' }) },
    ];
    const bids = [
      makeBid('b1', 'r1', '2025-04-01T00:00:00Z'),
      makeBid('b2', 'r2', '2025-04-02T00:00:00Z'),
    ];
    const { kpi } = computePgHomeData(pairs, bids);
    expect(kpi.won).toBe(1);
  });

  it('pendingPairs: deadline 오름차순 정렬', () => {
    const near = new Date(Date.now() + 1 * 24 * 3600 * 1000).toISOString();
    const far  = new Date(Date.now() + 7 * 24 * 3600 * 1000).toISOString();
    const pairs = [
      { invitation: makeInv('i1', 'r1', 'sent'), rfp: makeRfp('r1', { deadline: far }) },
      { invitation: makeInv('i2', 'r2', 'sent'), rfp: makeRfp('r2', { deadline: near }) },
    ];
    const { pendingPairs } = computePgHomeData(pairs, []);
    expect(pendingPairs[0].rfp.id).toBe('r2');
    expect(pendingPairs[1].rfp.id).toBe('r1');
  });

  it('pendingPairs: accepted/expired 제외', () => {
    const pairs = [
      { invitation: makeInv('i1', 'r1', 'accepted'), rfp: makeRfp('r1') },
      { invitation: makeInv('i2', 'r2', 'sent'), rfp: makeRfp('r2') },
      { invitation: makeInv('i3', 'r3', 'expired'), rfp: makeRfp('r3') },
    ];
    const { pendingPairs } = computePgHomeData(pairs, []);
    expect(pendingPairs).toHaveLength(1);
    expect(pendingPairs[0].rfp.id).toBe('r2');
  });

  it('recentBids: submittedAt 내림차순 top 3', () => {
    const bids = [
      makeBid('b1', 'r1', '2025-04-01T00:00:00Z'),
      makeBid('b2', 'r2', '2025-04-03T00:00:00Z'),
      makeBid('b3', 'r3', '2025-04-02T00:00:00Z'),
      makeBid('b4', 'r4', '2025-04-04T00:00:00Z'),
    ];
    const { recentBids } = computePgHomeData([], bids);
    expect(recentBids).toHaveLength(3);
    expect(recentBids[0].bid.id).toBe('b4');
    expect(recentBids[1].bid.id).toBe('b2');
    expect(recentBids[2].bid.id).toBe('b3');
  });

  it('recentBids stage: won/lost/pending 정확히 판별', () => {
    const pairs = [
      { invitation: makeInv('i1', 'r1', 'accepted'), rfp: makeRfp('r1', { awardedBidId: 'b1', status: 'awarded' }) },
      { invitation: makeInv('i2', 'r2', 'accepted'), rfp: makeRfp('r2', { awardedBidId: 'b-other', status: 'awarded' }) },
      { invitation: makeInv('i3', 'r3', 'accepted'), rfp: makeRfp('r3', { status: 'sent' }) },
    ];
    const bids = [
      makeBid('b1', 'r1', '2025-04-03T00:00:00Z'),
      makeBid('b2', 'r2', '2025-04-02T00:00:00Z'),
      makeBid('b3', 'r3', '2025-04-01T00:00:00Z'),
    ];
    const { recentBids } = computePgHomeData(pairs, bids);
    expect(recentBids[0].stage).toBe('won');
    expect(recentBids[1].stage).toBe('lost');
    expect(recentBids[2].stage).toBe('pending');
  });
});
```

- [ ] **Step 1-2: 테스트 실행 — 실패 확인**

```bash
pnpm test -- lib/server/__tests__/pg-home.test.ts
```

Expected: FAIL — "Cannot find module '../pg-home'"

- [ ] **Step 1-3: `computePgHomeData` 구현**

`lib/server/pg-home.ts`:

```typescript
import type { RfpInvitation } from '@/lib/types/invitation';
import type { RFP } from '@/lib/types/rfp';
import type { Bid } from '@/lib/types/bid';

export type PgHomeData = {
  kpi: { total: number; pending: number; submitted: number; won: number };
  pendingPairs: { invitation: RfpInvitation; rfp: RFP }[];
  recentBids: { bid: Bid; rfpTitle: string; stage: 'won' | 'lost' | 'pending' }[];
};

export function computePgHomeData(
  pairs: { invitation: RfpInvitation; rfp: RFP }[],
  bids: Bid[],
): PgHomeData {
  const rfpMap = new Map(pairs.map((p) => [p.rfp.id, p.rfp]));

  const total = pairs.length;
  const pending = pairs.filter(
    (p) => p.invitation.status === 'sent' || p.invitation.status === 'opened',
  ).length;
  const submitted = pairs.filter((p) => p.invitation.status === 'accepted').length;
  const won = bids.filter((b) => rfpMap.get(b.rfpId)?.awardedBidId === b.id).length;

  const pendingPairs = pairs
    .filter((p) => p.invitation.status === 'sent' || p.invitation.status === 'opened')
    .sort(
      (a, b) => new Date(a.rfp.deadline).getTime() - new Date(b.rfp.deadline).getTime(),
    );

  const recentBids = bids
    .filter((b) => b.status === 'submitted')
    .sort((a, b) => new Date(b.submittedAt).getTime() - new Date(a.submittedAt).getTime())
    .slice(0, 3)
    .map((bid) => {
      const rfp = rfpMap.get(bid.rfpId);
      const stage: 'won' | 'lost' | 'pending' =
        rfp?.awardedBidId === bid.id
          ? 'won'
          : rfp?.status === 'awarded'
            ? 'lost'
            : 'pending';
      return { bid, rfpTitle: rfp?.title ?? '', stage };
    });

  return { kpi: { total, pending, submitted, won }, pendingPairs, recentBids };
}
```

- [ ] **Step 1-4: 테스트 실행 — 전체 통과 확인**

```bash
pnpm test -- lib/server/__tests__/pg-home.test.ts
```

Expected: 8 tests PASS

- [ ] **Step 1-5: 커밋**

```bash
git add lib/server/pg-home.ts lib/server/__tests__/pg-home.test.ts
git commit -m "feat(pg-home): add computePgHomeData pure function + tests"
```

---

## Task 2: `BuyerHome` 컴포넌트 추출

**Files:**
- Create: `components/home/BuyerHome.tsx`

기존 `app/(app)/home/page.tsx`의 buyer 콘텐츠(KPI strip + active RFPs 섹션)를 RSC 컴포넌트로 분리한다. RSC이므로 별도 단위 테스트 없음 — Task 4의 통합 검증으로 대체.

- [ ] **Step 2-1: `BuyerHome.tsx` 작성**

`components/home/BuyerHome.tsx`:

```tsx
import Link from 'next/link';
import { Eyebrow } from '@/components/primitives/Eyebrow';
import { Tag } from '@/components/primitives/Tag';
import { Button } from '@/components/primitives/Button';
import { EmptyState } from '@/components/primitives/EmptyState';
import { PageEnter } from '@/components/primitives/PageEnter';
import { FileTextIcon } from '@/components/icons';
import { KpiCell } from '@/components/primitives/KpiCell';
import { getRfpRepo } from '@/lib/server/repositories/factory';
import { formatDate } from '@/lib/format';

export async function BuyerHome({ workspaceId }: { workspaceId: string }) {
  const rfps = await (await getRfpRepo()).findByBuyerWs(workspaceId);
  const sentRfps = rfps.filter((r) => r.status === 'sent');
  const awardedRfps = rfps.filter((r) => r.status === 'awarded');

  return (
    <PageEnter className="px-8 py-10">
      {/* KPI Strip */}
      <div className="flex items-start gap-16 mb-12 pb-12 border-b border-[var(--color-hair)]">
        <KpiCell label="전체 제안" serial="A" value={String(rfps.length)} />
        <KpiCell label="진행 중" serial="B" value={String(sentRfps.length)} />
        <KpiCell label="수주 완료" serial="C" value={String(awardedRfps.length)} />
      </div>

      {/* Active RFPs */}
      <section>
        <div className="flex items-center gap-3 mb-4">
          <Eyebrow>진행 중인 제안</Eyebrow>
          <span className="font-mono tabular-nums text-[11px] text-[var(--color-ink-soft)]">
            {sentRfps.length}건
          </span>
        </div>
        {sentRfps.length === 0 ? (
          <EmptyState
            icon={<FileTextIcon size={32} />}
            title="진행 중인 제안이 없습니다."
            description="새로운 제안을 작성해 PG사에 발송하세요."
            action={
              <Link href="/rfp/new">
                <Button size="sm">+ 신규 제안</Button>
              </Link>
            }
          />
        ) : (
          <div className="divide-y divide-[var(--color-hair)] border-t border-[var(--color-hair)]">
            {sentRfps.map((rfp) => (
              <Link
                key={rfp.id}
                href={`/rfp/${rfp.id}`}
                className="relative py-4 flex items-center justify-between group hover:bg-[var(--color-paper-warm)] -mx-4 px-4 cursor-pointer transition-colors before:absolute before:left-0 before:top-0 before:bottom-0 before:w-2 before:bg-[var(--color-ink)] before:opacity-0 hover:before:opacity-100 before:transition-opacity"
              >
                <div>
                  <p className="text-[13px] font-medium text-[var(--color-ink)]">
                    {rfp.title}
                  </p>
                  <span className="font-mono text-[11px] text-[var(--color-ink-soft)] tabular-nums">
                    {rfp.id} · {formatDate(rfp.sentAt ?? rfp.createdAt)}
                  </span>
                </div>
                <Tag variant="amber">발송됨</Tag>
              </Link>
            ))}
          </div>
        )}
      </section>
    </PageEnter>
  );
}
```

- [ ] **Step 2-2: 커밋**

```bash
git add components/home/BuyerHome.tsx
git commit -m "feat(home): extract BuyerHome RSC component"
```

---

## Task 3: `PgHome` 컴포넌트 구현

**Files:**
- Create: `components/home/PgHome.tsx`

- [ ] **Step 3-1: `PgHome.tsx` 작성**

`components/home/PgHome.tsx`:

```tsx
import Link from 'next/link';
import { PageEnter } from '@/components/primitives/PageEnter';
import { KpiCell } from '@/components/primitives/KpiCell';
import { Eyebrow } from '@/components/primitives/Eyebrow';
import { EmptyState } from '@/components/primitives/EmptyState';
import { InboxIcon } from '@/components/icons';
import { getInvitationRepo, getBidRepo } from '@/lib/server/repositories/factory';
import { GRADE_LABELS } from '@/lib/types/biz-profile';
import { formatDeadline, formatDate } from '@/lib/format';
import { computePgHomeData } from '@/lib/server/pg-home';

const stageLabel: Record<'won' | 'lost' | 'pending', string> = {
  won: '[ 수주 ]',
  lost: '[ 미선정 ]',
  pending: '[ 검토중 ]',
};

const stageColor: Record<'won' | 'lost' | 'pending', string> = {
  won: 'text-[var(--color-moss)]',
  lost: 'text-[var(--color-ink-soft)]',
  pending: 'text-[var(--color-ink-muted)]',
};

export async function PgHome({
  userId,
  workspaceId,
}: {
  userId: string;
  workspaceId: string;
}) {
  const [pairs, bids] = await Promise.all([
    (await getInvitationRepo()).findByPgUser(userId),
    (await getBidRepo()).findByPgWs(workspaceId),
  ]);

  const { kpi, pendingPairs, recentBids } = computePgHomeData(pairs, bids);

  return (
    <PageEnter className="px-8 py-10">
      {/* KPI Strip */}
      <div className="flex items-start gap-16 mb-12 pb-12 border-b border-[var(--color-hair)]">
        <KpiCell label="전체 수신" serial="A" value={String(kpi.total)} />
        <KpiCell label="응답 대기" serial="B" value={String(kpi.pending)} />
        <KpiCell label="제출 완료" serial="C" value={String(kpi.submitted)} />
        <KpiCell label="수주" serial="D" value={String(kpi.won)} />
      </div>

      {/* 2-column list */}
      <div className="grid grid-cols-2 gap-10">
        {/* Left: 응답 대기 */}
        <section>
          <div className="flex items-center gap-2 mb-3">
            <Eyebrow>응답 대기</Eyebrow>
            <span className="font-mono tabular-nums text-[11px] text-[var(--color-ink-soft)]">
              {kpi.pending}건
            </span>
          </div>
          {pendingPairs.length === 0 ? (
            <EmptyState
              icon={<InboxIcon size={24} />}
              title="응답 대기 중인 제안이 없습니다."
              description="구매사가 초대한 RFP가 /inbox에 표시됩니다."
            />
          ) : (
            <div className="border-t border-[var(--color-hair)]">
              {pendingPairs.map((p) => {
                const dday = formatDeadline(p.rfp.deadline);
                const urgent =
                  dday.startsWith('D-') && parseInt(dday.slice(2)) <= 3;
                const grade = p.rfp.bizProfile?.grade
                  ? GRADE_LABELS[p.rfp.bizProfile.grade]
                  : '—';
                return (
                  <Link
                    key={p.invitation.id}
                    href={`/inbox/${p.rfp.id}`}
                    className="relative flex items-center justify-between py-3 border-b border-[var(--color-hair)] hover:bg-[var(--color-paper-warm)] -mx-4 px-4 transition-colors before:absolute before:left-0 before:top-0 before:bottom-0 before:w-2 before:bg-[var(--color-ink)] before:opacity-0 hover:before:opacity-100 before:transition-opacity"
                  >
                    <div>
                      <p className="text-[13px] font-medium text-[var(--color-ink)]">
                        {p.rfp.title}
                      </p>
                      <span className="font-mono text-[11px] text-[var(--color-ink-soft)] tabular-nums">
                        {p.rfp.id} · {grade}
                      </span>
                    </div>
                    <span
                      className={`font-mono text-[11px] tabular-nums font-medium ${
                        urgent
                          ? 'text-[var(--color-terracotta)]'
                          : 'text-[var(--color-ink-muted)]'
                      }`}
                    >
                      {dday}
                    </span>
                  </Link>
                );
              })}
            </div>
          )}
        </section>

        {/* Right: 최근 제출 */}
        <section>
          <div className="flex items-center gap-2 mb-3">
            <Eyebrow>최근 제출</Eyebrow>
            <span className="font-mono tabular-nums text-[11px] text-[var(--color-ink-soft)]">
              최근 3건
            </span>
          </div>
          {recentBids.length === 0 ? (
            <EmptyState
              icon={<InboxIcon size={24} />}
              title="제출한 제안이 없습니다."
            />
          ) : (
            <div className="border-t border-[var(--color-hair)]">
              {recentBids.map(({ bid, rfpTitle, stage }) => (
                <Link
                  key={bid.id}
                  href={`/inbox/${bid.rfpId}`}
                  className="relative flex items-center justify-between py-3 border-b border-[var(--color-hair)] hover:bg-[var(--color-paper-warm)] -mx-4 px-4 transition-colors before:absolute before:left-0 before:top-0 before:bottom-0 before:w-2 before:bg-[var(--color-ink)] before:opacity-0 hover:before:opacity-100 before:transition-opacity"
                >
                  <div>
                    <p className="text-[13px] font-medium text-[var(--color-ink)]">
                      {rfpTitle}
                    </p>
                    <span className="font-mono text-[11px] text-[var(--color-ink-soft)] tabular-nums">
                      {bid.rfpId} · {formatDate(bid.submittedAt)}
                    </span>
                  </div>
                  <span
                    className={`font-mono text-[11px] tracking-[0.06em] ${stageColor[stage]}`}
                  >
                    {stageLabel[stage]}
                  </span>
                </Link>
              ))}
            </div>
          )}
        </section>
      </div>
    </PageEnter>
  );
}
```

- [ ] **Step 3-2: 타입 체크**

```bash
pnpm tsc --noEmit 2>&1 | grep -E "PgHome|pg-home|BuyerHome"
```

Expected: 에러 없음 (출력 없음)

- [ ] **Step 3-3: 커밋**

```bash
git add components/home/PgHome.tsx
git commit -m "feat(home): add PgHome RSC dashboard component"
```

---

## Task 4: `home/page.tsx` 분기 로직으로 교체

**Files:**
- Modify: `app/(app)/home/page.tsx`

- [ ] **Step 4-1: `page.tsx` 교체**

`app/(app)/home/page.tsx` 전체를 아래로 교체:

```tsx
import { redirect } from 'next/navigation';
import { auth } from '@/auth';
import { BuyerHome } from '@/components/home/BuyerHome';
import { PgHome } from '@/components/home/PgHome';

export const dynamic = 'force-dynamic';

export default async function HomePage() {
  const session = await auth();
  if (!session?.user?.id) redirect('/login?next=/home');

  if (session.user.workspaceType === 'pg' && session.user.workspaceId) {
    return (
      <PgHome userId={session.user.id} workspaceId={session.user.workspaceId} />
    );
  }

  if (session.user.workspaceType === 'buyer' && session.user.workspaceId) {
    return <BuyerHome workspaceId={session.user.workspaceId} />;
  }

  redirect('/login');
}
```

- [ ] **Step 4-2: 타입 체크 + lint**

```bash
pnpm tsc --noEmit && pnpm lint 2>&1 | grep -E "home/page|BuyerHome|PgHome"
```

Expected: 에러 없음

- [ ] **Step 4-3: 커밋**

```bash
git add app/\(app\)/home/page.tsx
git commit -m "feat(home): dispatch BuyerHome/PgHome by workspaceType"
```

---

## Task 5: 로그인 리다이렉트 단순화

**Files:**
- Modify: `app/(public)/login/page.tsx`

이전 작업(2026-05-08)에서 `router.push(r.workspaceType === 'pg' ? '/inbox' : next)`로 수정했다. PG home이 생겼으므로 모든 타입이 `next`(`/home`)로 가면 된다.

- [ ] **Step 5-1: 리다이렉트 수정**

`app/(public)/login/page.tsx`에서 아래 한 줄 수정:

```tsx
// 변경 전
router.push(r.workspaceType === 'pg' ? '/inbox' : next);

// 변경 후
router.push(next);
```

- [ ] **Step 5-2: 타입 체크**

```bash
pnpm tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 5-3: 커밋**

```bash
git add app/\(public\)/login/page.tsx
git commit -m "fix(login): simplify post-login redirect — all workspace types go to /home"
```

---

## Task 6: 전체 검증

- [ ] **Step 6-1: 전체 테스트 통과 확인**

```bash
pnpm test
```

Expected: 신규 pg-home 테스트 8개 포함 전체 통과. 실패 있으면 원인 수정 후 재실행.

- [ ] **Step 6-2: 타입 체크 최종**

```bash
pnpm tsc --noEmit
```

Expected: 에러 없음

- [ ] **Step 6-3: 수동 검증 체크리스트**

로컬 서버 `pnpm dev` 실행 후:

1. PG 계정 로그인 → `/home` 직접 진입, KPI 4개(전체·대기·제출·수주) + 2컬럼 렌더링 확인
2. Buyer 계정 로그인 → 기존 buyer home(KPI 3개 + 진행 중 제안 목록) 정상 렌더링 확인
3. PG `/inbox` 직접 URL 접근 → 기존 전체 목록 정상 렌더링 확인
4. PG 응답 대기 0건 → 왼쪽 컬럼 EmptyState 표시 확인
5. PG 제출 완료 0건 → 오른쪽 컬럼 EmptyState 표시 확인
