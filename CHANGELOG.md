# Changelog

All notable changes to this project will be documented in this file.

## [0.4.28.2] - 2026-07-29

### Changed

- **사이드바 워크스페이스 전환 목록 순서를 바꿨어요**: 소속된 워크스페이스가 여러 개일 때, 이제 PG사를 구매사보다 먼저 보여주고 각 그룹 안에서는 이름 가나다순으로 정렬해요. (마스터 계정의 전체 워크스페이스 목록도 동일하게 정렬돼요.)

## [0.4.28.1] - 2026-07-28

### Fixed

- **인증 메일을 다시 보내지 못했는데 보냈다고 하던 문제를 고쳤어요**: '인증 메일 다시 보내기'를 눌렀을 때 발송이 실패해도 화면은 '메일을 다시 보냈어요'라고 알렸어요. 게다가 입력칸에 넣어둔 번호까지 지워져서, 오지 않을 메일을 기다리며 번호도 잃는 상황이 됐어요. 이제 실패하면 실패했다고 알려주고, 지웠던 번호를 되돌려줘요. 되돌린 번호로 저절로 다시 인증이 나가지도 않아요.

## [0.4.28.0] - 2026-07-28

### Changed

- **인증번호 6자리를 다 넣으면 버튼을 누르지 않아도 넘어가요**: 가입할 때 만나는 인증칸 두 곳(휴대전화 인증번호, 가입 직후 이메일 인증 코드) 모두 숫자를 다 채우고도 확인 버튼을 한 번 더 눌러야 했어요. 어차피 6자리로 정해져 있으니 마지막 자리를 넣는 순간 바로 확인해요. 버튼은 그대로 있어서, 틀린 번호를 다시 시도하고 싶을 때 쓸 수 있어요. 휴대전화 인증번호 칸은 문자로 받은 번호를 휴대폰이 대신 채워주기도 해요.

- **틀린 번호를 지웠다 그대로 다시 넣어도 저절로 다시 보내지지 않아요**: 인증 시도 횟수에는 제한이 있어서, 같은 번호가 저절로 반복해서 나가면 사용자가 모르는 사이에 횟수를 다 써버릴 수 있어요. 같은 번호를 다시 보내는 건 버튼이나 Enter를 눌렀을 때만 해요.

- **내부 테스트 보강 (사용자 영향 없음)**: 위 변경에는 모두 회귀 방지 테스트를 붙였어요. 기존 테스트 중 실제로는 아무것도 검사하지 못하던 두 건(Enter 키가 가입 폼을 제출하지 않는지 확인하는 테스트)도 실제로 검사하도록 고쳤어요.

### Fixed

- **인증번호를 다시 받았는데 새 번호가 계속 틀리다고 나오던 문제를 고쳤어요**: 5분이 지나기 전에 '재전송'을 누르면 문자로는 새 번호가 오는데, 서버는 여전히 먼저 보낸 번호를 정답으로 들고 있었어요. 그래서 방금 받은 번호를 정확히 넣어도 계속 틀렸다고 나오고, 시도 횟수만 줄었어요. 이제 가장 마지막에 보낸 번호로 확인해요.

- **인증 시도 횟수를 넘겼을 때 이유를 알려줘요**: 횟수를 다 쓰면 입력칸이 통째로 사라지면서 안내 문구까지 같이 사라져서, 화면만 바뀌고 왜 그런지는 알 수 없었어요. 이제 '시도 횟수를 초과했습니다. 번호를 다시 인증해주세요.' 안내가 남아요.

- **이메일 인증 중 네트워크가 끊기면 화면이 멈춰 있던 문제를 고쳤어요**: 인증 요청이 실패하면 화면이 '확인 중' 상태로 굳어서, 번호를 다시 넣어도 아무 반응이 없고 오류 문구도 뜨지 않았어요. 이제 오류를 알려주고 바로 다시 시도할 수 있어요.

- **인증 메일을 다시 받으면 입력칸을 비워줘요**: 이전에 넣은 번호가 그대로 남아 있어서, 이미 못 쓰게 된 번호를 다시 보내게 되기 쉬웠어요.

- **화면을 읽어주는 기기에서 인증 결과를 알 수 있어요**: 버튼을 누르지 않고 저절로 확인이 되다 보니, 확인 중이라는 사실도 실패했다는 사실도 소리로는 전해지지 않았어요. 이제 둘 다 안내해요.

## [0.4.27.0] - 2026-07-26

### Fixed

- **문의하기로 보낸 메일이 전부 반송되던 문제를 고쳤어요**: 화면 아래 '문의하기'가 지금은 쓰지 않는 옛 주소로 연결돼 있어서, 문의를 보내도 저희에게 닿지 않고 그대로 되돌아갔어요. 이제 실제로 받는 주소로 연결돼요.

- **견적을 쓸 때 월 정산한도를 반드시 채우게 했어요**: 정산한도를 비워 두면 `0원`으로 저장돼서, 구매사 비교 화면에 '한도 0원'으로 그대로 보였어요. '한도가 없다'는 뜻이 아니라 '정산을 못 받는다'로 읽히는 값이라, 견적을 보내기 전에 입력하도록 막았어요. 새 견적·새 템플릿을 열면 칸이 비어 있고 예시 금액이 흐리게 보여요.

- **1단계 안내가 이미 채운 칸까지 다시 입력하라고 하던 문제를 고쳤어요**: 정산 주기와 정산한도 중 하나만 비었을 때도 안내 문구가 둘의 이름을 다 불러서, 방금 채운 칸을 또 채우라는 말처럼 읽혔어요. 이제 비어 있는 칸만 짚어 줘요.

- **새 견적 템플릿을 열자마자 빨간 오류가 떠 있던 문제를 고쳤어요**: 아무것도 입력하지 않았는데 정산한도 칸이 이미 빨갛게 시작해서, 뭘 잘못한 것처럼 보였어요. 이제 값을 만진 뒤에 알려 줘요. 다만 한도가 0으로 저장된 기존 템플릿을 열면, 저장이 잠긴 이유를 처음부터 보여줘요.

- **좁은 칸에서 '입력 완료' 표시가 두 줄로 쪼개지던 문제를 고쳤어요**: 정산한도처럼 폭이 좁은 칸에서 표시가 '입력 완 / 료'로 끊겨 보였어요. 이제 한 줄로 붙어서 나와요.

- **환영 인사의 서비스 이름을 '서포트비'로 맞췄어요**: 가입 후 처음 보는 환영 창만 이름을 '서포트 B'로 적고 있어서, 사이드바·랜딩·푸터와 이름이 달라 보였어요.

### Changed

- **내부 문서·테스트 보강 (사용자 영향 없음)**: 위 수정에는 모두 회귀 방지 테스트를 함께 붙였어요. 릴리스 전 점검에서 확인한 나머지 항목 중 화면 디자인 결정이나 실제 주소가 필요한 것(한글 문단 줄바꿈 규칙, 푸터의 빈 링크 5개, 견적 제출 서버 검증)은 추측으로 채우지 않고 과제로 남겨 두었어요.

## [0.4.26.0] - 2026-07-26

### Fixed

- **다크 모드로 바꾸면 휴대폰 상태바 색도 같이 바뀌어요**: 앱 안에서 테마를 바꿔도 브라우저 위쪽 영역(모바일 상태바·주소창 배경)은 휴대폰 시스템 설정만 따라갔어요. 그래서 시스템은 라이트인데 앱만 다크로 쓰면, 화면은 어두운데 상태바만 하얗게 남아 위쪽이 잘려 보였어요. 이제 앱에서 고른 테마를 그대로 따라가고, 새로고침해도 유지돼요.
## [0.4.25.0] - 2026-07-26

### Changed

- **파일 첨부 영역의 두 줄 안내가 이제 한눈에 구분돼요**: 견적 요청 첨부와 견적서 PDF 업로드 칸에서 '무엇을 하면 되는지' 알려주는 안내 문구와 '어떤 파일을 올릴 수 있는지' 알려주는 힌트가 완전히 같은 크기·같은 색으로 붙어 있었어요. 두 줄이 한 덩어리로 보여서 눈이 어디부터 읽어야 할지 잡히지 않았어요. 이제 안내 문구가 조금 더 크고 진하게, 힌트는 작고 흐리게 나와서 위아래 순서가 자연스럽게 읽혀요.

- **랜딩 페이지 문제 카드의 번호가 제목을 가리지 않아요**: 카드 왼쪽의 장식용 번호(01·02·03)가 바로 옆 제목보다 커서, 정작 읽어야 할 제목보다 번호에 먼저 눈이 갔어요. 이제 번호를 제목보다 작게 줄여서 제목이 주인공으로 읽히고, 번호는 순서를 알려주는 표시로 물러나요. 화면 너비와 밝은/어두운 테마 어디서든 같게 보여요.

- **내부 문서·테스트 보강 (사용자 영향 없음)**: 두 안내 문구가 다시 같은 표기로 붙는 것을 막는 회귀 방지 테스트를 추가했어요. 디자인 문서에는 사이드바 브랜드 마크 애니메이션이 랜딩 데모 화면에서도 쓰인다는 사실과, 그 화면에서도 '동작 줄이기' 설정을 그대로 존중한다는 점을 함께 적어 두었어요.

## [0.4.24.0] - 2026-07-26

### Fixed

- **워크스페이스 초대 화면의 탭 제목이 '견적 요청 초대'로 뜨던 것을 고쳤어요**: 초대 화면 두 종류(견적 초대·워크스페이스 멤버 초대)가 제목 하나를 같이 쓰는데, 그 제목이 견적 쪽 문구로 고정돼 있었어요. 그래서 회사 멤버로 초대받은 분도 브라우저 탭과 링크 미리보기에서 '견적 요청 초대'를 보게 됐어요. 이제 '초대'로 떠서 어느 쪽으로 초대받았든 어색하지 않아요.

### Removed

- **아무 데서도 연결되지 않던 초대 화면을 정리했어요 (사용자 영향 없음)**: 주소창에 직접 입력해야만 닿는 `/invite` 화면이 남아 있었어요. 초대자·회사명·멤버 수가 전부 예시 문구로 박혀 있었고 `거절하기` 버튼은 눌러도 아무 일이 없었는데, 실제 초대 메일은 모두 다른 주소로 연결되기 때문에 정상적으로 초대를 받은 분이 이 화면을 볼 일은 없었어요. 실제 초대 수락 화면(견적 초대·워크스페이스 초대)은 그대로예요.

### Changed

- **내부 테스트 보강 (사용자 영향 없음)**: 초대 화면이 검색엔진에 색인되지 않도록 막아 두는 설정에 회귀 방지 테스트를 추가했어요. 초대 주소에는 일회용 토큰이 들어 있어서, 이 설정이 실수로 빠지면 아직 살아 있는 초대 링크가 검색 결과에 노출될 수 있어요.

## [0.4.23.0] - 2026-07-25

### Fixed

- **계정 탈퇴 창에서 빈 비밀번호로 Enter를 눌러도 제출되지 않아요**: 탈퇴 확인 버튼은 비밀번호가 비어 있으면 잠기지만, 입력칸에서 Enter를 누르면 버튼을 거치지 않고 곧장 제출되던 경로가 있었어요. 이제 Enter 제출도 같은 조건으로 막혀, 비가역 동작인 탈퇴를 실수로 진행할 일이 없어요.

### Changed

- **내부 테스트 보강 (사용자 영향 없음)**: 계정 탈퇴 Enter 제출·중복 제출 방지, 견적 작성 수수료 입력 판정의 조합 경계, 구매사 요청조건 뷰의 운영 솔루션 표기에 회귀 방지 테스트를 추가했어요. 문서(SCREEN_DESIGN.md)의 옛 컬럼 서술도 현재 저장 구조에 맞게 바로잡았어요.

## [0.4.22.0] - 2026-07-24

### Changed

- **데이터 조회 계층의 타입 안전망을 복원했어요 (내부 정리)**: 데이터베이스 조회 코드 34개 파일이 타입 검사를 우회하는 형태로 작성돼 있어, 조회 항목이 실수로 빠져도 빌드가 통과하고 화면에서 값이 비는 방식으로만 드러났어요. 이제 전 계층이 컴파일 시점에 검사돼 같은 실수가 코드를 병합하기 전에 잡혀요. 사용자에게 보이는 동작은 달라지지 않아요(전환 과정에서 드러난 실제 어긋남은 채팅 첨부 목록 조회 1곳의 작성 형태뿐이었고, 동작 동일한 정석 형태로 바로잡았어요).

## [0.4.21.0] - 2026-07-24

### Fixed

- **실패한 알림 이메일을 다시 보낼 때 바로 발송돼요 (내부 정리)**: 발송에 실패한 이메일은 자동 재시도가 간격을 두고 뒤로 밀리는데, 수동으로 재발송을 요청해도 그 밀린 예약 시각까지 기다리는 문제가 있었어요. 이제 재발송 요청 즉시 발송 대기열의 맨 앞으로 옵니다.
- **모든 종류의 알림 이메일이 재발송 대상이 돼요 (내부 정리)**: 재발송 허용 목록이 손으로 관리되던 탓에 재견적 요청 등 일부 이메일 유형이 "재발송할 수 없어요"로 잘못 거절됐어요. 이제 목록이 이메일 유형 정의에서 자동으로 만들어져 새 유형이 생겨도 빠지지 않아요.
- **실시간 연결 폭주 보호장치가 설정 실수에도 꺼지지 않아요 (내부 정리)**: 서버 재시작 직후 모든 탭이 한꺼번에 재접속할 때 데이터베이스를 보호하는 상한이, 환경 설정 값이 숫자가 아니면 소리 없이 비활성화되던 것을 고쳤어요. 잘못된 값은 기본 상한으로 안전하게 되돌아가요.
## [0.4.20.0] - 2026-07-24

### Changed

- **승인 대기 중인 결제대행사 담당자 계정은 승인 전까지 서버 기능도 함께 잠겨요**: 결제대행사 회사 계정에 합류를 신청하면 관리자 승인 전까지 '승인 대기' 화면에서 기다리게 되는데, 지금까지는 화면만 막혀 있고 서버에 직접 요청을 보내는 우회로는 열려 있는 구조였어요. 이제 견적 제출·회수, 견적 템플릿, 전자서명 템플릿, 채팅·칸반 보드, 계약 취소·재발송·리마인더까지 서버가 승인 여부를 직접 확인하고 거절해요. 정상적으로 승인된 계정과 운영자 계정이 쓰는 기능은 달라지지 않아요.
- **승인 상태 기록이 손상돼도 안전한 쪽으로 동작해요 (내부 정리)**: 승인 상태 값이 예상 밖의 형태로 저장되는 드문 경우, 예전에는 앱 진입이 열리는 방향이었는데 이제 승인 대기 화면으로 보내는 방향(잠그는 쪽)으로 통일했어요. 정상 데이터에서는 보이는 동작이 달라지지 않아요.

## [0.4.19.0] - 2026-07-23

### Changed

- **접속 표시(초록 점)를 실제 거래 관계가 있는 상대에게만 보여드려요**: 지금까지는 화면에는 안 드러나도, 로그인한 계정이라면 기술적으로 임의 회사의 접속 상태와 "지금 그 회사를 살펴보는 곳이 어디인지"까지 엿볼 수 있는 구조였어요. 봉인 입찰에서는 어떤 결제대행사들이 한 견적 요청에 붙어 있는지 자체가 민감한 정보라서, 이제 대화를 나눴거나 견적 요청으로 초대·참여 요청이 오간 상대만 서로의 접속 표시를 읽을 수 있게 서버에서 잠갔어요. 여러분이 화면에서 보던 초록 점은 원래 전부 관계 있는 상대의 것이라, 보이는 동작은 그대로예요.
- **결제대행사 목록 조회를 견적 요청 작성 화면 전용으로 잠갔어요**: 회사 이름 목록을 내려주는 내부 조회가 로그인만 하면 누구나 부를 수 있었는데, 이제 견적 요청을 작성하는 구매사 계정만 쓸 수 있어요. 견적 요청 작성 시 결제대행사 선택 기능은 달라지지 않아요.

### Fixed

- **로컬 개발용 실시간 서버 구성이 다시 동작해요 (내부 정리)**: 개발 환경에서 실시간 기능을 띄우는 설정이 존재하지 않는 설정 파일을 가리키고 있어 시작조차 되지 않던 것을 고치고, 운영과 같은 보안 설정(구독 검증 비밀값)이 로컬에서도 그대로 이어지도록 맞췄어요. 함께 실시간 보안 구조를 기록한 내부 문서(위협 모델)를 새로 만들고 운영 안내서의 오래된 설명도 바로잡았어요.

## [0.4.18.0] - 2026-07-23

### Changed

- **튜토리얼 시작·재시작 흐름을 자동으로 검증해요 (내부 정리)**: 처음 홈에 들어왔을 때 뜨는 환영 안내에서 체험을 시작하는 길, '나중에 하기' 후 배너로 다시 들어오는 길, 완료 후에는 안내가 더 이상 뜨지 않고 튜토리얼 주소로 가도 홈으로 돌아오는 동작, 그리고 튜토리얼을 나가려 할 때 뜨는 확인창의 세 가지 선택지가 각각 약속대로 동작하는지를 실제 브라우저 테스트로 상시 검증하게 했어요. 화면에서 보이는 동작은 달라지지 않아요.

## [0.4.17.0] - 2026-07-23

### Fixed

- **튜토리얼에서 견적 보내기 확인창을 취소해도 안내가 이어져요**: PG 체험하기의 마지막 단계에서 '견적 보내기'를 누르면 확인창이 뜨는데, 예전에는 이 순간 안내 말풍선이 사라져서 확인창을 취소하면 아무 안내 없이 화면에 남겨졌어요. 이제 확인창의 확정 버튼까지 안내가 이어지고(말풍선과 강조 링이 확인창 위에 제대로 보여요), 취소하면 잠깐 사이에 다시 '견적 보내기' 안내로 되돌아와 끝까지 완주할 수 있어요.
- **튜토리얼 완료·건너뛰기 기록이 유실되지 않아요**: 체험을 마치거나 건너뛴 직후 홈으로 이동하면, 드물게 기록 저장보다 화면 이동이 빨라서 방금 완주했는데도 환영 안내가 다시 뜰 수 있었어요. 이제 기록 저장이 끝난 뒤에 이동하고, 저장에 실패하면 조용히 넘어가는 대신 알림으로 알려드려요. 연결이 느린 환경에서도 버튼이 오래 멈춰 있지 않도록 대기 시간에는 상한을 뒀어요.
- **나가기 확인창에서 '계속 체험하기'를 누르면 확실히 남아요**: 튜토리얼을 나가려다 마음을 바꿔 '계속 체험하기'를 눌렀는데도, 아주 짧은 순간의 엇갈림으로 결국 화면이 이동해버릴 수 있던 문제를 고쳤어요. 버튼을 빠르게 여러 번 눌러도 한 번만 처리돼요.
- **체험 중에도 작성하던 실제 견적 요청 초안이 정확히 보존돼요**: 튜토리얼을 하는 동안 다른 탭에서 실제 견적 요청 초안을 이어서 편집한 경우, 튜토리얼을 마치면 그 최신 편집분이 사라지고 이전 내용으로 되돌아갈 수 있었어요. 이제 항상 가장 최신 초안이 유지돼요.
- **안내 말풍선이 화면 변화를 즉시 따라가요**: 견적 작성 단계에서 같은 버튼이 다음 단계 버튼으로 바뀌는 순간, 이전 단계의 말풍선이 잠깐 엉뚱한 버튼을 가리키던 문제를 고쳤어요.
- **튜토리얼의 템플릿 저장 안내가 헷갈리지 않아요**: 체험 중 견적 템플릿을 저장하면 저장 창은 정상적으로 닫히는데 '저장되지 않아요'라는 안내가 함께 떠서 실패한 것처럼 읽혔어요. 이제 '체험이라 실제로 저장하지는 않았어요'라고 안내해요.

### Changed

- **튜토리얼 나가기 안내창의 링크 판정을 더 안전하게 다듬었어요**: 외부 주소처럼 해석될 수 있는 특수한 형태의 링크를 내부 링크로 잘못 판정하지 않도록 방어를 더했어요. 화면에서 보이는 동작은 달라지지 않아요.
- **화면 낭독기(스크린리더) 사용자를 위한 안내가 확인창이 열려 있어도 이어져요**: 튜토리얼 안내 단계가 바뀔 때 낭독기로 전달되는 공지가, 확인창이 열려 있는 동안에도 끊기지 않도록 했어요.

## [0.4.16.0] - 2026-07-23

### Fixed

- **메시지를 보낼 때 뜨는 말풍선 애니메이션이 딜룸 창 안에 얌전히 머물러요**: 채팅에서 메시지를 보내면 입력창의 글자가 말풍선으로 떠오르며 자리를 잡는 짧은 연출이 나오는데, 견적 딜룸을 창(모달)으로 열어 둔 상태에서는 이 말풍선이 잠깐 창 바깥 영역까지 넘나들 수 있었어요. 이제 연출이 채팅 영역 안쪽으로만 그려지도록 해서 창 위로 삐져나오지 않아요.
- **메시지 목록이 새로 불러와지는 순간에도 말풍선이 두 개로 겹쳐 보이지 않아요**: 메시지를 보낸 직후 서버에서 목록을 다시 받아오는 짧은 순간에, 떠오르는 연출용 말풍선과 실제 말풍선이 잠깐 함께 보이는 경우가 아주 드물게 있었어요. 이제 목록이 새로 오면 진행 중이던 연출을 정리해서 겹쳐 보이지 않아요.
- **한 번에 빠르게 두 통을 보내도 둘 다 애니메이션이 나와요**: 아주 짧은 간격으로 메시지 두 통을 연달아 보내면 마지막 한 통만 떠오르는 연출이 나오고 앞선 메시지는 애니메이션 없이 바로 나타났어요. 이제 두 통 모두 자연스럽게 떠오르는 연출이 나와요.
## [0.4.15.0] - 2026-07-23

### Fixed

- **견적 비교에서 가입비를 더 정확하게 보여드려요**: 견적을 비교하는 화면에서 가입비가 없는 곳은 이제 '0원' 대신 '없어요'로 보여드려요. 가입비는 매달 내는 카드 수수료와 달리 처음 한 번만 내는 비용이라 '지금 조건보다 이만큼 좋아져요' 비교에는 넣지 않는데, 예전에는 그 안내가 개선폭을 보여주는 칸에 놓여 있어서 가입비가 마치 그만큼 좋아진 것처럼 읽힐 수 있었어요. 이제 가입비 항목 아래에 '한 번만 내는 비용이라 위 비교에 넣지 않았어요'라고 적어, 왜 비교에서 빠졌는지 분명히 알 수 있게 했어요.

### Changed

- **로컬 개발 환경이 갑자기 먹통이 되던 문제를 막았어요 (내부 정리)**: 디자인 규칙을 자동으로 검사하는 코드의 설명 주석에 예시로 적어 둔 글자가, 개발 서버의 스타일 처리를 통째로 망가뜨려 모든 화면이 열리지 않던 경우가 있었어요. 예시가 실제 스타일로 잘못 읽히지 않도록 검사 파일을 스타일 수집 대상에서 뺐어요. 실제 배포된 서비스에는 영향이 없던 문제예요.

## [0.4.14.0] - 2026-07-22

### Fixed

- **회색 배경 위의 보조 글씨도 이제 권장 대비를 넘겨요**: 지난 버전에서 흐릿하던 보조 글씨 색을 진하게 바꿨는데, 다시 재어 보니 하얀 배경에서만 기준을 넘고 살짝 회색을 띤 카드나 패널 위에서는 여전히 조금 못 미쳤어요. 견적 작성 단계 표시의 번호, 제안서 미리보기 패널의 안내문, 파일을 끌어다 놓는 중에 뜨는 안내 문구 같은 곳이에요. 보조 글씨 색을 한 번 더 조정해서 앱에서 쓰는 모든 배경 위에서 기준을 넘기도록 했어요. 어두운 화면은 이미 넘고 있어서 그대로 두었어요.

### Changed

- **글자와 배경의 대비를 테스트가 직접 계산해요 (내부 정리)**: 색이 기준을 넘는지 사람이 눈으로 판단하는 대신, 테스트가 색 정의 파일을 읽어 밝기 차이를 계산하고 앱에서 쓰는 모든 배경 조합을 확인해요. 색을 잘못 바꾸면 바로 알 수 있어요.
- **로그인·가입 화면의 입력칸 스타일을 한 곳으로 모았어요 (내부 정리)**: 밑줄 입력칸의 생김새가 15개 파일에 각각 복사돼 있어서, 색 하나를 바꾸려면 15곳을 일일이 고쳐야 했어요. 13곳을 공용 정의를 쓰도록 정리했고, 앞으로 새로 복사하면 테스트가 알려줘요. 보이는 모습은 이전과 같아요.
- **디자인 규칙 자동 검사의 사각지대를 메웠어요 (내부 정리)**: 스타일시트에 직접 쓴 색이나, 여러 줄에 나눠 쓴 스타일은 기존 검사가 놓치고 있었어요. 검사 방식을 코드 구조를 해석하는 방식으로 바꿔서 이제 잡아내요.

## [0.4.13.0] - 2026-07-22

### Fixed

- **링크를 누르면 새 탭이 열린다는 걸 미리 알려드려요**: 첨부파일, 메시지 속 링크, 구매사 홈페이지, 약관, 전자서명 완료본 링크는 누르면 새 탭이 열리는데 그 사실을 미리 알리지 않았어요. 눈으로 보면 아이콘이나 밑줄로 짐작할 수 있지만, 화면을 읽어주는 프로그램(스크린리더)을 쓰시는 분께는 아무 예고 없이 화면이 바뀌었어요. 이제 링크마다 '새 탭에서 열려요'(전자서명 완료본은 '새 탭에서 내려받아요')가 함께 읽혀요. 눈에 보이는 화면은 이전과 똑같아요.
- **가입 화면의 약관 동의 문구가 끊기지 않게 했어요**: 위 안내를 넣으면서 '이용약관 동의' · '개인정보 처리방침 동의' 체크박스를 읽어줄 때 안내 문구가 중간에 끼어들 뻔했어요. 체크박스에는 원래 문구 그대로 읽히고, 안내는 링크에 초점을 맞췄을 때만 들리도록 분리했어요.
## [0.4.12.0] - 2026-07-22

### Fixed

- **흐릿해서 잘 안 보이던 작은 글씨들이 또렷해졌어요**: 첨부파일 용량과 순번, 견적번호, 시각 표시, '첨부파일이 없어요' 같은 안내 문구, 입력칸의 예시 글씨(`your@email.com`), × 삭제 버튼처럼 화면 곳곳의 보조 글씨가 배경과 거의 구분되지 않을 만큼 옅었어요. 밝은 화면에서 재어 보니 글자와 배경의 밝기 차이가 권장 기준의 3분의 1도 안 됐고, 어두운 화면에서도 마찬가지였어요. 눈이 피로하거나 밝은 곳에서 화면을 보실 때 특히 읽기 어려웠던 부분이라 이 글씨들의 색을 한 단계 진하게 바꿨어요. 견적 요청 작성, 견적 제출, 설정, 알림, 딜룸, 랜딩 등 80곳이 바뀌었고 글자 크기와 배치는 그대로예요.
- **눌러지는 버튼인지 알기 어렵던 3곳을 고쳤어요**: 초대 화면의 '거절하기', 견적 작성 중 '초기화', 가맹점 등급 확인의 '수정' 버튼은 마우스를 올리면 색이 진해지도록 만들어져 있었는데, 위 색상 정리를 하면서 평소 색과 마우스를 올렸을 때 색이 같아져 버려 아무 반응이 없는 것처럼 보였어요. 다시 또렷하게 반응하도록 되돌렸어요.

### Changed

- **화면을 읽어주는 프로그램에 불필요하게 읽히던 기호를 정리했어요 (내부 정리)**: 딜룸 상단에서 견적번호와 제목 사이를 나누는 가운뎃점(·)은 눈으로 볼 때만 의미가 있는 장식인데 스크린리더에도 함께 읽히고 있었어요. 이제 읽지 않고 넘어가요. 눈에 보이는 화면은 그대로예요.
- **같은 문제가 다시 생기지 않도록 자동 검사를 추가했어요 (내부 정리)**: 이번에 문제가 된 색을 글씨에 다시 쓰거나, 마우스를 올렸을 때 색이 평소와 같아져 반응이 사라지는 경우를 테스트가 자동으로 잡아내요. 디자인 규칙 문서에도 이 색은 테두리 전용이라는 점과, 지금 기준으로도 짙은 배경 위에서는 아직 권장 대비에 못 미친다는 점을 함께 적어 뒀어요.

## [0.4.11.0] - 2026-07-22

### Fixed

- **화면을 보지 않고도 계약 진행 상황을 알 수 있어요**: 딜룸 '계약' 탭의 서명 타임라인은 '서명 요청을 보냈어요' · '양측 서명' · '계약 완료' 같은 단계를 세로로 보여드리는데, 각 단계가 끝났는지 기다리는 중인지는 작은 점의 **색**으로만 표시하고 있었어요. 그래서 화면을 읽어주는 프로그램(스크린리더)을 쓰시는 분께는 단계 이름만 차례로 읽히고 어디까지 진행됐는지는 전달되지 않았어요. 이제 각 단계마다 '완료' · '진행 중' · '대기' · '실패' · '종료' 같은 상태가 함께 읽혀요. 담당자 단계는 원래도 상태 배지가 읽히고 있어서 그대로예요. 눈에 보이는 화면은 이전과 똑같아요.

## [0.4.10.0] - 2026-07-22

### Fixed

- **탈퇴가 막혔을 때 무엇을 해야 하는지 정확히 알려드려요**: 워크스페이스에 관리자가 회원님 한 명뿐이면 탈퇴가 막히는데, 그동안은 상황과 상관없이 "다른 멤버에게 권한을 위임하세요" 한 마디만 보여드렸어요. 그런데 남은 멤버가 전부 승인 대기 중이거나 화면에 보이지 않는 계정이면 넘길 상대가 아예 없어서, 시키는 대로 할 수가 없는 안내였어요. 이제 넘길 수 있는 멤버가 있으면 그대로 안내하고, 없으면 "팀원을 초대하거나 대기 중인 멤버를 승인해 주세요"라고 다음에 할 일을 알려드려요. 안내에 쓰던 영문 표기(admin)도 다른 화면과 같은 '관리자'로 맞췄어요.
- **화면에 보이지 않는 운영용 계정이 멤버 수에 잡히던 문제를 고쳤어요**: 고객지원을 위한 운영 계정은 멤버 목록에 나오지 않는데도 탈퇴 판정에서는 한 명으로 세고 있었어요. 그래서 실제로는 혼자 쓰는 워크스페이스인데 "다른 멤버에게 권한을 넘기라"며 막히거나, 반대로 탈퇴는 되는 대신 워크스페이스에 실제 관리자가 아무도 없이 남는 일이 있었어요. 이제 멤버 목록과 같은 기준으로 세서, 혼자 쓰던 워크스페이스는 탈퇴할 때 함께 정리돼요.

### Changed

- **탈퇴 가능 여부를 판단하는 규칙을 한 곳으로 모았어요 (내부 정리)**: 탈퇴 화면이 미리 보여주는 경고와 실제로 탈퇴를 막는 서버 검사가 똑같은 규칙을 각자 들고 있었어요. 한쪽만 바뀌면 "괜찮다고 했는데 막히는" 상황이 생길 수 있는 구조라 하나로 합쳤어요. 막는 기준 자체는 이전과 똑같아요.
- **멤버 정보를 읽어오는 코드가 값을 빠뜨리면 바로 드러나도록 했어요 (내부 정리)**: 승인 상태 같은 값을 실수로 조회 목록에서 빼먹어도 지금까지는 아무 경고 없이 넘어가 권한 판단이 조용히 어긋날 수 있었어요. 이제 그런 실수는 빌드 단계에서 바로 잡혀요. 화면과 동작은 이전과 똑같아요.

## [0.4.9.1] - 2026-07-22

### Fixed

- **이유 없이 실패하던 자동 테스트를 고쳤어요**(개발용, 화면 변화 없음): 계정 탈퇴와 견적 재요청 화면의 자동 테스트가 팝업 안 입력칸에 글자를 넣을 때, 팝업이 커서를 도로 가져가는 동작과 타이밍이 겹치면 입력이 조용히 사라져 실패하곤 했어요. 어느 날 갑자기 빨갛게 뜨는데 원인은 "글자를 못 찾았다"로만 보여서 매번 처음부터 원인을 찾아야 했어요. 이제 입력을 커서와 무관한 방식으로 넣어 이런 일이 생기지 않고, 실패할 때도 어느 단계에서 어긋났는지 바로 드러나요. 앱 코드는 한 줄도 바뀌지 않았고 실제 화면·동작은 이전과 똑같아요.
## [0.4.9.0] - 2026-07-22

### Fixed

- **폐업·휴업한 사업자번호로는 바꿀 수 없어요**: 설정에서 사업자 등록번호를 바꿀 때, 가입할 때와 달리 폐업이나 휴업 상태인 번호도 그냥 저장됐어요. 정상 사업자로 가입한 뒤 나중에 폐업 번호로 갈아끼울 수 있었던 셈이에요. 이제 두 화면 모두 같은 기준으로 막고, 왜 안 되는지 안내해요.
- **사업자번호 조회가 오래 매달려 있지 않아요**: 국세청 조회가 몰려서 느려지면 재시도와 대기가 겹치면서 한 번의 조회가 20초 넘게 붙잡혀 있을 수 있었어요. 가입 화면에서 쓰는 기능이라 로그인 없이 부를 수 있고, 그만큼 서버가 쉽게 묶일 수 있는 자리였어요. 이제 조회 한 건이 붙잡을 수 있는 시간에 상한을 두어, 응답이 아주 늦거나 아예 오지 않아도 정해진 시간 안에 정리하고 안내해요. 평소처럼 정상 조회되는 경우에는 달라지는 게 없어요.

### Security

- **로그인 확인을 건너뛰는 주소 목록을 더 정확하게 좁혔어요**(개발용): 인증 검사를 건너뛰어야 하는 주소를 이름의 '앞부분만' 비교하고 있어서, 앞으로 그 이름으로 시작하는 실제 페이지를 만들면 그 페이지가 통째로 로그인 확인을 건너뛸 수 있었어요. 이제 이름 전체가 정확히 맞을 때만 건너뛰어요. 소셜 공유 카드·앱 아이콘처럼 파일 확장자가 붙어 오는 주소는 예전처럼 잘 열려요. 예전에 쓰던 예시 이미지 때문에 남아 있던 항목 다섯 개도 정리했어요.
- **공유 카드·앱 아이콘 주소 아래로는 더 이상 통과시키지 않아요**(개발용): 위 정리 과정에서, 확장자가 붙는 주소를 예외로 열어두다 보니 그 주소 **아래에 붙는 경로**까지 함께 열려 있었어요. 이런 그림 주소는 항상 그 자체로 끝나고 아래에 뭔가 더 붙는 경우가 없어서, 이제 정확히 그 주소일 때만 통과시켜요.

### Changed

- **견적 요청에서 같은 결제수단을 여러 번 보내도 하나로 정리돼요**: 결제수단 목록에 같은 값이 중복으로 담겨 오면 그대로 저장됐어요. 이제 중복은 하나로 접히고, 있을 수 없는 개수가 담겨 오면 저장하지 않아요. 화면에서 체크해서 고르는 평소 사용에는 변화가 없어요.

## [0.4.8.0] - 2026-07-21

### Changed

- **같은 코드가 두 벌씩 있던 자리를 한 곳으로 모았어요 (내부 정리, 화면 변화 없음)**: 견적 템플릿을 화면으로 내려보내는 부분이 딜룸과 템플릿 관리 페이지에 똑같이 두 벌 있었고, 구매사·PG 튜토리얼도 진행 표시·나가기 버튼·완료 축하 연출 같은 뼈대를 통째로 복제하고 있었어요. 한쪽만 고치면 다른 쪽이 조용히 어긋나는 자리들이라 각각 한 곳으로 합쳤어요. 보이는 화면과 동작은 이전과 똑같아요.
- **작은 라벨 글자를 지정하는 방식을 하나로 통일했어요**: 같은 크기·굵기를 내는 방법이 두 가지로 나뉘어 있어서 다음에 화면을 만들 때 어느 쪽을 따라야 할지 애매했어요. 하나로 정리하면서 KPI 항목 이름과 사용자 메뉴의 이름 줄 간격이 아주 미세하게 정돈됐어요.

### Added

- **같은 어긋남이 다시 새어 들어오지 못하게 막는 검사를 추가했어요**(개발용): 라벨 글자 스타일을 통일된 방식 대신 손으로 다시 풀어 쓰면 테스트가 실패해요. 버튼·칩·내비처럼 크기만 가져다 쓰는 경우는 그대로 허용해요.
## [0.4.7.0] - 2026-07-21

### Fixed

- **아직 승인되지 않은 관리자를 일반 멤버로 바꿀 수 있어요**: 승인된 관리자가 한 명뿐인 워크스페이스에서, 아직 승인 대기 중인 관리자의 권한을 내리려 하면 "마지막 관리자는 변경할 수 없어요"라는 엉뚱한 오류가 났어요. 승인 대기 중인 사람은 애초에 관리자 권한을 쓸 수 없으니 내려도 관리자가 사라지지 않는데, 화면이 이를 막고 있었어요. 이제 정상적으로 바뀌고, 승인된 마지막 관리자를 내리는 것은 예전처럼 막아요.
- **두 사람이 동시에 권한을 내려도 관리자가 사라지지 않아요**: 관리자 두 명이 같은 순간에 서로의 권한을 내리면 양쪽 다 "아직 관리자가 남아 있다"고 판단해 둘 다 내려가고, 워크스페이스에 관리자가 한 명도 없는 상태로 잠길 수 있었어요. 멤버 초대·이름 변경·감사 로그가 전부 막히고 앱 안에서는 되돌릴 방법이 없었어요. 이제 권한을 내릴 때 남은 관리자 수 확인과 실제 변경이 한 묶음으로 처리돼 이런 일이 생기지 않아요.

### Changed

- **관리자 판정 기준을 한 곳으로 모았어요**: "권한을 실제로 쓸 수 있는 관리자"인지 따지는 규칙이 코드 다섯 곳에 따로 적혀 있어 이번 문제의 원인이 됐어요. 하나로 합쳐 앞으로 한쪽만 바뀌어 어긋나는 일을 막았어요. 화면에서 보이는 동작은 그대로예요.

## [0.4.6.0] - 2026-07-21

### Added

- **선정하고 나면 계약 화면이 먼저 열려요**: 견적을 선정한 뒤 딜룸을 열면 맨 앞에 `계약` 탭이 생기고 그 탭이 먼저 보여요. 서명이 어디까지 왔는지, 지금 누구 차례인지, 다음에 뭘 하면 되는지가 첫 화면에 있어요. 견적 비교 탭에는 한 줄 요약이 남아 다른 탭을 보는 중에도 진행 상황을 놓치지 않아요. 계약이 없는 견적 요청은 예전과 똑같아요.
- **서명 진행을 한 줄기로 볼 수 있어요**: 발송 → 구매사 서명 → PG 서명 → 완료가 세로 타임라인 하나로 이어져, 누가 언제 서명했고 누가 남았는지 한눈에 읽혀요. 진행률 막대와 참여자 목록으로 나뉘어 같은 이야기를 두 번 하던 것을 합쳤어요.
- **서명이 끝나면 문서를 바로 내려받아요**: 완료된 계약서와 감사추적인증서가 파일 목록으로 놓여 클릭 한 번에 열려요.

### Changed

- **PG는 이제 자기가 할 일을 안내받아요**: 계약서 템플릿을 아직 등록하지 않은 PG에게 "PG사가 계약서를 준비하고 있어요"라는 남 얘기 같은 문구가 뜨던 것을 고쳤어요. 이제 "계약서 템플릿을 등록해 주세요"와 함께 설정 화면으로 가는 버튼이 바로 보여요.
- **서명 상태 문구를 통일했어요**: `서명 거절`·`서명 기한 지남`·`서명 취소`처럼 무슨 일이 있었는지 그대로 읽히는 말로 바꿨어요. 취소 확인 창의 확정 버튼도 닫기 버튼과 헷갈리지 않게 `취소하기`로 구분했어요.
- **여덟 가지 서명 상태가 같은 모양으로 보여요**: 준비 중·진행 중·완료·거절·만료·취소·시작 실패가 모두 상태 안내 → 타임라인 → 버튼이라는 같은 자리를 써서, 상태가 바뀌어도 화면이 출렁이거나 버튼을 눈으로 다시 찾을 일이 없어요.

### Fixed

- **"다시 시작"을 눌렀는데 "다시 발송했어요"라고 뜨던 문제**: 눌린 버튼과 다른 말을 하던 안내를 맞췄어요.
- **전자서명 작업 중 오류가 나면 버튼이 계속 눌리지 않던 문제**: 새로고침해야 풀리던 것을 고쳤어요. 오류 내용도 사람이 읽을 수 있는 말로 안내해요.
- **취소 확인 창을 열어둔 사이 서명이 끝나면 엉뚱한 안내가 뜨던 문제**를 고쳤어요.
- **딜룸 탭이 좁은 화면에서 넘치던 문제**: 탭 줄이 가로로 스크롤돼요. 탭 밑줄과 키보드 포커스 테두리가 잘리던 것도 함께 고쳤어요.
- **긴 상호명이 담당자 정보를 밀어내던 문제**를 고쳤어요.

### Security

- 선정되지 않은 PG에게 낙찰 PG의 계약 상태가 보이지 않도록 화면단에서도 한 번 더 막았어요(서버는 이미 막고 있었어요).

### Infrastructure

- 알 수 없는 서명 상태가 들어와도 딜룸 전체가 멈추지 않고 그 영역만 안내로 대체돼요.
- 전자서명 작업이 예외로 실패하면 오류가 관측 도구에 기록돼요.
- 좌측 레일의 계약 상태 표시를 화면 낭독기에서도 읽을 수 있게 했어요.
- 딜룸 전자서명 화면 테스트를 보강했어요(상태·역할 조합, 값 전달, 중복 클릭 방지, 취소 흐름). 실행 지역에 따라 결과가 달라지던 테스트도 고쳤어요.

## [0.4.5.0] - 2026-07-21

### Fixed

- **이메일 주소 변경이 이제 제대로 완료돼요**: 로그인한 채로 확인 메일의 링크를 열면 홈 화면으로 되돌아가면서 변경이 조용히 취소되던 문제를 고쳤어요. 설정에서 주소를 바꾸고 새 주소로 받은 링크를 그대로 열면 바로 확정돼요.
- **앱을 열 때 색이 튀지 않아요**: 휴대폰 상태 표시줄과, 홈 화면에 추가한 앱의 시작 화면 색이 앱 배경색과 달라 순간적으로 다른 색이 비치던 문제를 고쳤어요.

### Changed

- **화면과 서버가 항상 같은 기준을 보도록 정리했어요**: 결제수단·현재 운영 솔루션처럼 여러 곳에 나뉘어 적혀 있던 목록을 한 곳에서만 관리하도록 바꿨어요. 화면에서는 고를 수 있는데 저장할 때는 거부되는 식의 어긋남이 생기지 않아요. 견적 수수료 입력에서도 진행률 표시와 제출 가능 여부가 같은 기준을 씁니다.

### Security

- **운영자 계정 주소는 이메일 변경으로도 가져갈 수 없어요**: 가입 경로에는 이미 있던 보호가 이메일 변경 경로에만 빠져 있어, 일반 계정이 운영자용 주소를 자기 계정 주소로 바꾸려 시도할 수 있었어요. 같은 규칙을 적용했어요.
- **주소 설정이 반쪽이면 배포가 조용히 넘어가지 않아요**: 구매사·파트너 사이트 주소 중 하나만 설정된 상태에서 파트너 사이트의 검색 노출 차단과 운영자 로그인 보호가 함께 꺼질 수 있었어요. 이제 그런 설정은 즉시 오류로 드러나요(정상 배포와 로컬 개발은 영향 없음).

### Infrastructure

- 같은 값이 두 곳에 나뉘어 적히는 것을 막는 회귀 테스트를 보강했어요 — 공개 페이지 등록 누락(로그인 상태 포함), 게시판 공개 필드, 결제수단·구간 목록, 브랜드 표기, 앱 색상. 문서(README·SCREEN_DESIGN)가 코드와 어긋나던 오픈보드 공개 범위 서술도 정정했어요.

## [0.4.4.0] - 2026-07-21

### Changed

- **화면 곳곳의 한글 라벨이 드디어 같은 글씨체로 보여요**: 표 머리글·폼 라벨·상태 문구 같은 작은 글자들이 숫자 전용 글꼴(JetBrains Mono)로 지정돼 있었는데, 이 글꼴에는 한글 글자가 아예 없어요. 그래서 한글 라벨만 조용히 시스템 기본 글꼴로 대체돼 한 화면 안에서 글씨체가 섞여 보였어요. 76개 파일 205곳을 정리해서 이제 모든 한글이 Pretendard 하나로 보이고, 글자 사이가 불필요하게 벌어지던 것도 함께 정리됐어요. 금액·수수료율·견적 번호·날짜는 그대로 숫자 글꼴과 자릿수 정렬을 유지해요.
- **푸터 메뉴 제목이 자기 아래 링크보다 흐리던 문제를 고쳤어요**: 서비스·법적 고지·고객지원 같은 묶음 제목이 배경과 거의 구분되지 않는 색이라, 정작 그 아래 링크들보다 안 보였어요. 제목이 제목답게 보이도록 대비를 올렸어요.

### Added

- **같은 문제가 다시 새어 들어오지 못하게 막는 검사를 추가했어요**(개발용): 라벨에 숫자 글꼴을 다시 쓰면 테스트가 실패해요. 랜딩·마케팅 화면은 의도적으로 예외이며, 그 예외 목록도 한 파일에서 관리해요.

### Fixed

- **로그인 잠금 안내와 인증번호 만료 시간의 숫자가 흔들리지 않아요**: 남은 시간이 줄어들 때 자릿수 폭이 달라 글자가 미세하게 떨리던 부분을 숫자 전용 표기로 분리했어요.

## [0.4.3.0] - 2026-07-21

### Changed

- **메시지 전송 애니메이션 코드를 한곳으로 모았어요 (내부 정리, 화면 변화 없음)**: 메시지를 보낼 때 입력창의 글자가 말풍선으로 떠오르는 연출이 1:1 채팅과 팀 채팅 두 곳에 거의 같은 코드로 복제돼 있었어요. 이 연출은 "말풍선이 자리를 잡고 화면이 스크롤된 뒤에 위치를 재야 한다" 같은 지키기 까다로운 순서 규칙이 있는데, 그 규칙이 주석으로만 적혀 있어서 한쪽만 고치면 애니메이션이 조용히 멈출 수 있었어요. 이제 규칙을 코드 구조로 강제하는 한 곳에 모아서, 두 화면 모두 같은 동작을 보장해요. 보이는 연출은 이전과 똑같아요.
- **연속으로 메시지를 보내도 뒤 메시지의 연출이 사라지지 않아요**: 앞 메시지의 애니메이션이 끝나는 순간 바로 다음 메시지를 보내면, 뒤 메시지의 연출이 조용히 취소될 수 있는 경로가 있었어요. 회귀 테스트로 막았어요.

## [0.4.2.0] - 2026-07-20

### Changed

- **전자서명이 더 안정적이에요**: 전자서명 서비스(스노우싸인)가 일시적으로 느리거나 예상치 못한 응답을 보내도 화면이 멈추거나 끊기지 않아요. 선정 버튼도 서명 서비스를 기다리지 않고 바로 확정되고, 서명 상태 동기화와 완료 알림도 더 튼튼해졌어요.

### Fixed

- **전자서명 오류를 알기 쉽게 알려드려요**: 리마인더 보내기·취소·다시 발송이나 완료본 내려받기에서 문제가 생기면, 알 수 없는 코드 대신 무엇이 문제인지와 어떻게 하면 되는지 한국어로 안내해요.
- **선정됐는데 서명이 시작되지 않으면 바로 다시 시작할 수 있어요**: 선정은 확정됐지만 전자서명 서비스 문제로 계약 발송이 안 된 경우, 딜룸에 '다시 시작' 버튼과 안내가 표시되고 알림도 받아요.

## [0.4.1.0] - 2026-07-19

### Added

- **선정 후 전자서명까지 서포트비 안에서 이어져요**: 구매사가 견적을 선정하면 곧바로 전자서명 계약이 만들어져 양측 이메일로 발송돼요. 딜룸에서 서명 진행 상태(준비 중·진행 중·완료·거절)를 확인하고, 리마인더 보내기·다시 발송·완료본 다운로드를 할 수 있어요.
- **PG가 자사 계약서를 서명 템플릿으로 한 번만 등록해두면 돼요**: '서명 템플릿' 화면에서 계약서를 등록해두면, 선정되는 순간 그 템플릿으로 계약이 자동으로 만들어져 발송돼요. 아직 등록 전이면 구매사 화면에 'PG사가 계약서 준비 중'으로 표시되고, PG가 등록하는 순간 자동으로 발송돼요.
- **서명은 이메일 링크의 스노우싸인 페이지에서 진행돼요**: 양측이 각자 이메일로 받은 링크에서 서명하고, 모두 서명하면 딜룸에 '서명 완료'로 표시돼요. 완료본은 필요할 때 딜룸에서 바로 내려받을 수 있어요.

## [0.3.8.0] - 2026-07-19

### Added

- **견적 요청 결제수단에 애플페이·삼성페이가 추가됐어요**: 구매사는 견적 작성 시 간편결제 목록에서 애플페이·삼성페이를 선택할 수 있고, PG는 두 수단에 대해 영세·중소 우대수수료 구간별로 수수료를 제시할 수 있어요.

## [0.3.7.0] - 2026-07-19

### Fixed

- **견적 작성 시 자체 개발 선택하면 독립몰 이름을 묻지 않아요**: 현재 운영 솔루션에서 '자체 개발'을 고르면 별도 이름 입력란이 더 이상 나타나지 않아요. '기타'는 그대로 솔루션 이름을 입력할 수 있어요.
- **이전에 저장해둔 견적 작성 임시본도 함께 정리돼요**: '자체 개발'을 이미 선택해둔 상태로 저장된 임시본을 다시 열어도, 예전에 입력했던 이름이 남아 제출되지 않아요.

## [0.3.6.0] - 2026-07-18

### Added

- **PG가 견적에 가입비를 함께 담을 수 있어요**: 견적 작성 1단계에서 최초 1회 가입비(셋업비)를 입력하면 견적 검토와 보낸 견적 요약에 함께 표시돼요. 입력하지 않으면 0원으로 처리돼요.
- **구매사가 견적 비교에서 가입비를 확인할 수 있어요**: 견적 비교와 선정 결과 화면에 각 견적의 가입비가 표시돼요. 정렬이나 개선폭 판정에는 반영되지 않는 참고 정보예요.
- **견적 템플릿에 가입비가 함께 저장돼요**: 템플릿을 저장하거나 불러올 때 가입비도 같이 저장되고 채워져요.
- **용어 도움말에 '가입비' 설명이 추가됐어요**: 가입비 입력란의 정보 아이콘에서 확인할 수 있어요.

## [0.3.5.0] - 2026-07-17

### Added

- **튜토리얼 코치마크가 어디를 클릭해도 바로 따라와요**: 안내된 버튼 대신 다른 곳을 눌러도 — 이전 버튼으로 돌아가거나, 단계 표시를 눌러 건너뛰거나, 안내를 읽지 않고 바로 진행해도 — 코치마크가 0.5초 안에 지금 화면에 맞는 안내로 자동으로 이동해요. 작성 단계에서 입력이 빠져 진행이 막혔을 때도 몇 초 기다리지 않고 바로 이전 안내로 돌아와요.

### Changed

- **코치마크가 이동할 때 화면을 끌어당기지 않아요**: 안내할 버튼이 이미 화면에 보이면 스크롤을 움직이지 않고, 완전히 화면 밖일 때만 그 위치로 이동해요.
- **코치마크 안내가 스크린리더로도 전달돼요**: 안내 단계가 바뀔 때마다 제목과 설명을 보조기기에 알려줘요.

## [0.3.4.0] - 2026-07-16

### Changed

- **튜토리얼에서 화면을 자유롭게 만져볼 수 있어요**: 미리 채워진 값을 직접 지우고 다시 입력해보거나, 안내된 버튼 밖의 다른 요소도 눌러볼 수 있어요. 코치마크는 화면을 막지 않고 다음에 누를 곳만 조용히 안내해요. 아무것도 바꾸지 않으면 이전처럼 클릭만으로 3분 만에 완주할 수 있어요.
- **튜토리얼에서 무엇을 만져도 실제 데이터가 만들어지지 않아요**: 파일 첨부·견적서 업로드·견적 템플릿 저장은 전부 체험용으로만 동작하고 서버에 저장되지 않아요. 저장이 필요한 동작을 누르면 "튜토리얼에서는 저장되지 않아요"라고 알려드려요.

### Added

- **튜토리얼 도중 다른 화면으로 이동하려면 먼저 확인을 받아요**: 사이드바 등 다른 곳으로 나가려고 하면 "튜토리얼을 나갈까요?" 창이 떠서 계속 체험하기 / 나중에 하기 / 건너뛰기 중 고를 수 있어요. '나중에 하기'를 고르면 홈에서 다시 시작을 안내받고, '건너뛰기'는 완료로 기록돼 다시 권하지 않아요.
- **입력을 지워서 진행이 막혀도 안내가 끊기지 않아요**: 다음 버튼이 눌리지 않는 상태면 말풍선에 "입력이 비었거나 형식이 달라요" 힌트가 뜨고, 막힌 클릭으로 안내가 앞서 나가면 몇 초 뒤 이전 버튼으로 되돌아와 안내를 이어가요.

### Removed

- 튜토리얼의 키보드 입력 잠금과 화면 클릭 차단(투명 실드), 밖-클릭 반짝임 안내를 제거했어요 — 자유 체험 방식으로 대체됐어요.

## [0.3.3.0] - 2026-07-15

### Changed

- **튜토리얼 안내가 화면을 어둡게 덮지 않아요**: 코치마크 말풍선 뒤 배경이 밝게 그대로 보여서, 실제 화면을 눈으로 확인하면서 안내를 따라갈 수 있어요. 안내 중 다른 곳 클릭이 동작하지 않는 동작 방식은 그대로예요.
- **눌러야 할 곳이 은은하게 숨 쉬듯 강조돼요**: 스포트라이트 테두리가 부드럽게 깜빡이며 시선을 안내하고, 안내된 곳 밖을 클릭하면 눌러야 할 위치가 한 번 반짝여서 어디를 눌러야 하는지 알려줘요. 기기의 동작 줄이기 설정을 켜면 움직임이 멈춰요.

## [0.3.2.0] - 2026-07-15

### Changed

- **튜토리얼에서 '건너뛰기'를 누르면 바로 완료 화면으로 이동해요**: 코치마크의 건너뛰기가 안내 말풍선만 닫고 그 자리에 남겨두는 대신, 구매사·PG 튜토리얼 모두 마지막 완료 단계로 즉시 점프해요. 완료로 기록되어 홈의 환영 안내가 다시 나타나지 않아요.
- **Esc 키로는 튜토리얼이 끝나지 않아요**: 명령 팔레트를 닫으려는 등 다른 의도로 Esc를 눌러도 튜토리얼이 완료 처리되지 않아요. 건너뛰기는 코치마크의 버튼 클릭으로만 동작해요.

## [0.3.1.0] - 2026-07-12

### Fixed

- **등록되지 않은 사업자번호를 입력하면 이제 정확한 안내를 받아요**: 미등록 번호를 조회했을 때 "조회 중 오류가 발생했어요. 잠시 후 다시 시도해주세요" 대신 "사업자번호를 찾지 못했어요"라고 알려드려요. 시스템 문제로 오해하고 기다릴 필요 없이 번호를 바로 확인하고 고칠 수 있어요.
- **파트너 가입 중 조회 시스템에 문제가 있으면 원인을 구분해서 알려드려요**: 이전에는 서버 장애나 일시적 혼잡도 전부 "사업자번호를 찾지 못했어요"로 보여 내 번호가 틀린 것처럼 보였어요. 이제 시스템 오류("조회 중 오류가 발생했어요")와 혼잡("요청이 너무 많아요")을 구분해 안내해요.
- **조회 중에 번호를 고쳐도 이전 번호의 결과가 끼어들지 않아요**: 조회가 진행되는 동안 입력을 수정하면, 뒤늦게 도착한 이전 번호의 결과가 화면과 제출 값을 덮어쓰던 문제를 고쳤어요. Enter 연타로 같은 조회가 중복 실행되던 것도 막았어요.
- **설정에서 비영리단체 등 지원되지 않는 사업자 유형을 조회하면 저장 전에 미리 알려드려요**: 이전에는 조회가 성공한 것처럼 보인 뒤 저장 시점에 원인을 알 수 없는 오류가 났어요.

### Added

- **사업자번호 조회가 혼잡할 때 자동으로 다시 시도해요**: 국세청 조회가 일시적으로 혼잡하면 약 3초 안에 최대 3번 자동 재시도한 뒤 결과를 알려드려요. 혼잡이 길게 이어질 상황(쿼터 소진 등)으로 확인되면 헛되이 기다리게 하지 않고 바로 안내해요.

### Changed

- 사업자번호 조회에 쓰는 서비스 키가 요청 주소(URL)에 노출되지 않도록 표준 인증 헤더 방식으로 바꿨어요. 키 누락·만료 같은 조회 시스템 장애는 이제 운영 모니터링에 즉시 잡혀요.

## [0.3.0.0] - 2026-07-10

### Added

- **사이드바·랜딩 페이지 로고가 새로고침 시 한 번 그려지듯 등장해요**: 페이지를 새로 열거나 새로고침하면 왼쪽 위 로고 마크가 선으로 그려진 뒤 채워지는 등장 애니메이션이 재생돼요. 라우트 이동만으로는 다시 재생되지 않아요. 기기에서 동작 줄이기(감소된 모션) 설정을 켜두셨다면 애니메이션 없이 바로 완성된 모습으로 보여요.

### Fixed

- 동작 줄이기 설정을 켜둔 상태로 새로고침했을 때, 서버가 보낸 화면과 브라우저가 그리는 화면이 순간적으로 달라 로고가 잠깐 깜빡일 수 있던 문제를 고쳤어요. 이제 항상 같은 방식으로 그려지고 애니메이션 여부만 달라져요.
- 로고 아이콘과 "서포트비" 글자의 높이가 미묘하게 어긋나 보이던 문제를 고쳤어요.

## [0.2.79.0] - 2026-07-10

### Changed

- **튜토리얼이 '읽는 안내'에서 '따라 클릭하는 체험'으로 바뀌었어요**: 이제 아무것도 입력할 필요 없이 모든 내용이 미리 채워져 있고, 화면이 다음에 누를 버튼만 밝게 비추며 "여기를 눌러 …"로 안내해요. 안내된 실제 버튼을 클릭하면 자동으로 다음 안내로 넘어가요 — 구매사(작성→제출→도착→비교·선정)와 PG(초대→조건 확인→견적 작성·제출) 모두 클릭만으로 완주할 수 있어요.
- **PG 견적 작성 체험도 무입력이 됐어요**: 정산조건·구간별 카드 수수료까지 예시 값이 미리 채워져 있어 확인하며 넘어가기만 하면 돼요.
- **튜토리얼 중에는 미리 채워진 값을 키보드로 지우거나 바꿀 수 없어요**: 체험이 항상 완주 가능한 상태로 유지돼요. 버튼은 Tab/Enter로도 조작할 수 있어 키보드 사용자도 건너뛰기·나가기가 가능해요.
- **코치마크 스포트라이트가 다크모드에서도 또렷하게 보여요**: 어두운 배경에선 주변을 살짝 밝혀 구멍이 도드라지도록 반전돼요. 단계 전환도 부드러운 페이드로 바뀌었어요.

### Fixed

- 화면을 스크롤하는 중에 스포트라이트가 늦게 따라와 안내된 버튼 클릭이 가끔 무시되던 문제를 고쳤어요 — 이제 구멍이 항상 버튼 위치에 정확히 붙어 있어요.
- 안내 대상 버튼이 화면에서 잠시 사라지거나 교체돼도 코치마크가 허공에 멈추지 않고 자동으로 복구되거나 다음 안내로 넘어가요.

## [0.2.76.0] - 2026-07-07

### Added

- **처음 오시면 3분 튜토리얼로 서비스를 직접 체험할 수 있어요**: 첫 홈 방문 시 환영 안내가 뜨고, '체험 시작하기'를 누르면 실제 화면 그대로 전체 흐름을 안전하게 연습해볼 수 있어요. 구매사는 견적 요청 작성 → 견적 도착 → 비교·선정까지, PG사는 초대 수신 → 요청 조건 확인 → 견적 작성·제출까지 체험해요. 모든 데이터는 연습용이라 실제로 아무것도 전송되지 않아요.
- **화면 위 단계별 안내(코치마크)가 생겼어요**: 튜토리얼 중 지금 봐야 할 영역을 밝게 비추고 말풍선으로 다음 행동을 알려줘요. '건너뛰기'로 언제든 넘길 수 있어요.
- **'나중에 하기'를 눌러도 다시 시작할 수 있어요**: 홈에 '3분 만에 서비스를 둘러보세요' 한 줄 안내가 남아 언제든 재시작할 수 있고, 완주하면 자동으로 사라져요.

### Changed

- **기존 '샘플로 둘러보기' 카드는 새 튜토리얼로 대체됐어요**: 홈의 샘플 체험 카드와 샘플 딜룸이 전체 여정 튜토리얼로 통합됐어요. 예전에 샘플을 체험하셨던 분도 새 튜토리얼 안내를 한 번 보게 돼요.

### Fixed

- **튜토리얼이 실제 작성 중인 견적 요청 임시 저장을 절대 건드리지 않아요**: 체험 중 어떤 상황(탭 강제 종료, 여러 탭 사용)에서도 실제 작성 데이터가 안전하게 보존돼요.
- 환영 안내를 Esc나 바깥 클릭으로 닫아도 다음 방문에 다시 뜨지 않아요.
## [0.2.78.0] - 2026-07-07

### Changed

- **공식 브랜드 표기가 '서포트비'로 바뀌었어요**: 화면 로고·사이드바 워드마크, 랜딩 페이지 문구, 안내 메일·문자 제목, 검색엔진 안내 문서(llms.txt)에 쓰이던 '서포트 B' 표기를 '서포트비'로 통일했어요. 기존 '서포트 B' 표기는 검색·AI 인용용 별칭으로 계속 인식돼요. 아이콘 'B' 마크는 그대로예요.

## [0.2.77.1] - 2026-07-07

### Added

- **화면 사용 패턴을 더 잘 이해할 수 있게 됐어요 (사용자 화면 변화 없음)**: Microsoft Clarity 세션 리플레이·히트맵 분석 태그를 추가했어요. 실제 운영 환경에서만 동작하고, 로컬 개발 중에는 로드되지 않아요.

## [0.2.77.0] - 2026-07-07

### Added

- **'서포트비'로 검색해도 서포트 B를 찾을 수 있어요**: ChatGPT·퍼플렉시티 같은 AI 검색과 일반 검색엔진이 한글 음차 표기 '서포트비'(그리고 '서포트B', 'Support B', 'Supporter B')를 공식 브랜드 '서포트 B'의 별칭으로 인식하도록 했어요. AI 크롤러용 소개 문서(llms.txt·llms-full.txt), 검색엔진용 구조화 데이터(JSON-LD alternateName), 메타 키워드에 별칭을 명시하고, 랜딩 푸터의 브랜드 표기에도 '서포트 B(서포트비) CORP.'로 병기했어요.

### Fixed

- **랜딩 페이지 구조화 데이터 삽입을 더 안전하게 만들었어요 (화면 변화 없음)**: 검색엔진용 JSON-LD를 페이지에 넣을 때 특수 문자를 이스케이프하도록 해, 앞으로 FAQ 등 문구에 어떤 내용이 들어와도 스크립트 태그가 깨지지 않아요.

## [0.2.75.4] - 2026-07-07

### Changed

- **브라우저 저장 데이터 키 이름을 새 브랜드명으로 정리했어요**: 화면 테마(다크/라이트), 작성 중인 견적 요청 임시 저장, 게스트 체험 후 가입 이동 흐름이 브라우저에 저장하는 값의 내부 이름이 옛 브랜드명을 쓰고 있었어요. 이제 현재 브랜드명으로 통일했어요. 사용자가 보는 화면·조작 방법은 그대로예요.

## [0.2.75.3] - 2026-07-07

### Changed

- **견적 요청 메모 예시 문구를 정리했어요**: 견적 요청 작성 시 "견적 요청 세부 내용" 입력란의 예시 문구에서 "가입비/연회비 면제 요청" 항목을 뺐어요. 나머지 예시(신규 견적 요청·결제 전환율 최적화·수수료 최소화·정산주기 단축)는 그대로예요.

## [0.2.75.2] - 2026-07-07

### Fixed

- **사이드바 좌측 로고·워크스페이스 영역 정렬을 정돈했어요**: "서포트 B" 로고의 B 마크와 그 아래 워크스페이스 아바타가 서로 다른 좌측 여백을 써서 살짝 어긋나 보이던 것을, 두 영역의 좌측 여백을 맞춰 아이콘·텍스트 열이 한 줄로 정렬되도록 고쳤어요. 랜딩 페이지 로고 크기·간격은 그대로예요.

## [0.2.75.1] - 2026-07-07

### Fixed

- **아직 승인되지 않은 팀 멤버에게 알림이 쌓이지 않아요**: 워크스페이스 합류 승인을 기다리는 중인 멤버는 앱에 들어올 수 없는데도 견적 초대·제출·선정·채팅 등의 인앱 알림이 계속 쌓였어요. 이제 모든 인앱·이메일 알림이 승인된 멤버에게만 발송되도록 통일했어요. 승인이 끝나면 그 이후의 알림부터 정상적으로 받아요.
- **팀 채팅 멘션 대상 목록이 실제 알림 수신자와 일치해요**: 승인 대기 중인 멤버가 팀 채팅의 @멘션 목록에 보였지만 멘션해도 알림이 조용히 사라졌어요. 이제 멘션 목록에 승인된 멤버만 표시돼 멘션하면 반드시 알림이 가요.
- **초대가 아무에게도 닿지 않는 경우를 운영에서 감지할 수 있어요 (내부 관측성, 화면 변화 없음)**: 승인된 멤버가 아직 없는 PG 워크스페이스에 견적 요청을 보내면 겉으론 발송 성공이지만 아무도 받지 못하는 상태였어요. 이런 경우 서버 경고 로그를 남겨 운영에서 확인할 수 있게 했어요.

### Removed

- **알림 발송 코드의 오용 여지를 정리했어요 (내부 정리, 화면 변화 없음)**: 승인 상태를 거르지 않는 수신자 조회 함수와 이제 의미 없어진 필드를 삭제하고, 남은 함수에는 용도 경고 주석을 달아 미래의 회귀를 막았어요.
## [0.2.75.0] - 2026-07-07

### Fixed

- **PG 파트너 주소로 접속해도 이제 주소가 구매사 도메인으로 바뀌지 않아요**: `partner.support-b.com`에 접속하면 주소창이 `support-b.com/pg-landing`으로 바뀌어 버리는 문제가 있었어요. 인증 라이브러리(next-auth)가 서버에 설정된 대표 주소(AUTH_URL)로 요청 주소를 덮어쓰는 탓에, 파트너 도메인으로 들어온 요청의 내부 이동·리다이렉트가 전부 구매사 도메인 기준으로 만들어지던 잠복 버그였어요. 이제 실제로 접속한 도메인을 기준으로 처리해서, 파트너 랜딩은 주소 그대로 보이고 로그인 이동도 접속한 도메인을 유지해요.
- **배포 방식이 서버에 옛 환경설정을 "박제"하던 구조를 고쳤어요 (내부 배포 스크립트, 화면 변화 없음)**: 배포 스크립트가 환경설정 파일 전체를 프로세스 관리자(PM2)에 복사해 두는 구조라, 이후 설정 파일을 고쳐도 서버가 계속 옛 값(구 도메인 등)으로 돌던 문제가 있었어요. 이제 서버가 부팅할 때마다 설정 파일을 직접 읽도록 바꿔서, 설정 파일 수정 + 재시작만으로 반영돼요.

## [0.2.74.1] - 2026-07-07

### Fixed

- **성능 테스트(k6) 자동화가 매번 실패하던 문제를 고쳤어요 (내부 CI 전용, 화면 변화 없음)**: 첨부파일 저장소를 R2로 옮긴 뒤 서버가 부팅할 때 R2 접속 정보 네 가지가 필수로 요구되도록 바뀌었는데, 성능 테스트 자동화 설정에는 이 값들이 빠져 있어서 테스트 서버가 매번 뜨자마자 멈췄어요. 실제 파일은 건드리지 않는 테스트 전용 가짜 값을 넣어 서버가 정상적으로 뜨도록 고쳤어요.

## [0.2.74.0] - 2026-07-07

### Changed

- **소셜·채팅 링크 미리보기 이미지가 현재 브랜드로 새로 만들어졌어요**: 링크를 카카오톡·슬랙 등에 붙여넣을 때 뜨는 미리보기 이미지(OG 이미지)가 예전 원형 마크·구 워드마크·구 도메인 표기를 쓰던 옛날 디자인이었어요. 현재 브랜드 마크와 "서포트 B" 워드마크를 쓰는 다크 테마로 새로 만들었어요.
- **코드·문서·테스트 전반의 도메인 표기를 새 도메인으로 통일했어요 (내부 정리, 화면 변화 없음)**: 실 서비스 도메인 전환에 맞춰 코드 주석·환경변수 예시·문서·테스트 픽스처의 도메인 문자열을 일괄 갱신했어요. 실제 운영 반영(DNS·서버 환경변수·OAuth 콘솔)은 별도 배포 절차로 진행돼요.

### Fixed

- **리팩토링 안전망 테스트를 추가했어요**: 브랜드 마크 SVG가 앱 곳곳의 원본 좌표값과 어긋나지 않도록 지키는 드리프트 가드 테스트, 그리고 next-auth 관련 자동 테스트가 실제로는 문제 없는 코드인데도 수집 실패로 실패 처리되던 환경 설정 문제를 고쳤어요. 사용자가 겪는 화면 동작에는 변화가 없어요.

## [0.2.73.2] - 2026-07-07

### Fixed

- **B 로고의 왼쪽 획들이 어디서 봐도 반듯한 직각이에요**: 로고를 크게 보면 가운데 가로 획과 위·아래 획의 왼쪽 끝에 미세한 단차와 둥근 모서리가 남아 있었어요. 로고 원본 도형 자체를 다듬어 세 획의 왼쪽 끝을 같은 세로선에 정확히 직각으로 맞췄고, 앱 화면 로고는 물론 브라우저 탭 아이콘(favicon)과 홈 화면 추가 아이콘(apple-touch-icon)도 다듬어진 마크로 다시 만들었어요.

## [0.2.73.1] - 2026-07-07

### Changed

- **화면에 보이는 브랜드 표기가 "서포트 B"로 통일됐어요**: 브라우저 탭 타이틀, 검색결과 미리보기, 이메일·SMS 인증문자, 안내 메일 헤더/푸터, 랜딩 Footer 저작권 표기 등에 남아있던 "Supporter B"·"서포터비" 표기가 이미 적용돼 있던 워드마크 표기 "서포트 B"로 통일됐어요. 화면 동작이나 기능 변화는 없어요.

## [0.2.73.0] - 2026-07-07

### Changed

- **견적 요청·재요청 초대 이메일이 PG사 담당자 전원에게 가요**: 지금까지는 구매사가 견적을 요청하거나(최초 발송·임시저장 후 발송·참여 요청 수락) 재요청할 때 초대 이메일이 PG 워크스페이스의 관리자에게만 발송됐어요. 이제 가입 승인된 멤버 전원이 이메일을 받아서, 실무 담당자가 in-app 알림만 보고 견적 요청을 놓치는 일이 줄어요. 가입 승인 대기 중인 멤버는 이전과 같이 제외돼요.

## [0.2.72.0] - 2026-07-06

### Added

- **회원가입에서 회사 이메일 사용을 권장해요**: 구매사·PG 가입 1단계 이메일 입력칸 아래에 "회사 이메일을 입력해주세요" 안내를 항상 보여주고, gmail·naver 같은 개인 이메일을 입력하면 그 자리에서 주황색 경고("기업 메일 없는 사업장이나 공동 도메인 이메일이 없는 분들은 별도 심사 과정이 추가될 수 있어요.")로 바뀌어 알려줘요. 안내일 뿐 가입을 막지는 않아서 개인 이메일로도 그대로 진행할 수 있어요. 이미 가입된 이메일 등 오류가 표시 중이거나, 초대 링크로 들어와 이메일이 고정된 경우에는 안내를 띄우지 않아요.

## [0.2.71.0] - 2026-07-06

### Changed

- **새 "B" 브랜드 로고로 바뀌었어요**: 앱·랜딩 헤더와 사이드바의 로고 마크, 브라우저 탭 아이콘(favicon), 홈 화면 추가 아이콘(apple-touch-icon)이 새 마크로 교체됐어요. 로고 마크는 벡터(SVG)라 어떤 크기에서도 선명하고, 라이트/다크 테마 색을 자동으로 따라가요. 'Supporter B' 워드마크 텍스트와 사이드바 접힘 애니메이션은 그대로예요.

## [0.2.70.0] - 2026-07-06

### Changed

- **코드베이스 구조를 더 찾기 쉽게 재정비했어요 (화면 변화 없음)**: 사용자에게 보이는 동작·화면은 그대로예요. 내부적으로 ① 두 곳에 흩어져 있던 훅을 `lib/hooks/` 한 곳으로 모으고 동형이던 브레이크포인트 훅 두 개를 공용 팩토리로 통합했어요 ② `lib/` 루트에 흩어져 있던 유틸(금액 포맷·전화번호·마감시각·멘션·RFP 상태)을 주제별 폴더로 옮겼어요 ③ 컴포넌트 파일명을 PascalCase 관례로 통일했어요 ④ 4개 파일에 복제돼 있던 운영 솔루션 라벨을 단일 출처(`lib/rfp/solutions.ts`)로 합쳤어요 ⑤ 자주 수정되는 랜딩 카피·비교표 예시값·위저드 라벨을 컴포넌트에서 분리해 `*-data.ts` 사이드카로 추출했어요.

### Added

- **리팩토링 안전망 테스트를 추가했어요**: 브레이크포인트 훅 팩토리(SSR 데스크톱 우선 스냅샷 계약 포함)와 솔루션 라벨 단일 출처의 드리프트 가드 테스트를 추가했어요.

## [0.2.69.0] - 2026-07-06

### Changed

- **랜딩페이지 연출이 '동작 줄이기' 설정과 무관하게 항상 재생돼요**: 히어로 헤드라인 스크램블 전환, 배경 ASCII 필드 커서 반응, 통계 숫자 카운트업, 절감 계산기 슬라이더 힌트, PG 데모 마법사 자동재생이 방문자의 OS '동작 줄이기(prefers-reduced-motion)' 설정을 더 이상 확인하지 않고 항상 재생돼요. 서비스를 처음 접하는 방문자가 브랜드 연출을 온전히 볼 수 있도록 한 랜딩·마케팅 화면 한정 의도적 선택이며, 로그인 후 실제 업무 화면의 동작 줄이기 설정 존중은 그대로예요.

### Fixed

- **히어로 배경 애니메이션의 죽은 분기 코드를 정리했어요**: 위 변경으로 항상 참이 된 조건 분기를 제거해 코드를 단순화했어요.


### Changed

- **신규 계약 견적을 낼 때 있을 수 없는 항목을 더 이상 묻지 않아요**: 견적 유형을 '신규 계약'으로 고르면, 이전에 PG 계약이 없어 존재할 수 없는 값들(전년도 연간 PG 총 거래액·현재 카드 수수료·현재 월 정산한도·현재 보증보험·현재 정산주기)을 입력 화면과 최종 확인 화면 양쪽에서 숨겨요. 그중 유일하게 필수였던 전년도 연간 PG 총 거래액도 신규 계약에서는 필수 입력에서 빠져요. 이제 처음 PG 계약을 맺는 사업자도 없는 값을 억지로 채우지 않고 바로 견적을 보낼 수 있어요. 갱신 계약은 지금처럼 전년도 거래액을 입력받아요.

## [0.2.67.0] - 2026-07-05

### Changed

- **샘플 견적 체험이 더 이상 실제 데이터에 쌓이지 않아요**: 가입할 때마다 DB에 샘플 견적 요청·데모 워크스페이스·데모 입찰이 계속 생성되던 구조를 걷어내고, 화면에서만 동작하는 가상 샘플 체험으로 바꿨어요. 구매사는 샘플 딜룸에서 3개 견적 비교부터 선정(축하 화면 포함)까지 전체 흐름을 체험할 수 있고, PG는 요청 조건 열람과 견적 작성·제출 투어를 체험할 수 있어요. 체험을 마치거나 숨기면 다시 나타나지 않아요(유저 단위 저장 — 워크스페이스를 새로 만들어도 다시 뜨지 않아요).
- **운영 데이터 정리**: 기존에 누적된 샘플 견적·데모 워크스페이스(샘플페이 A/B/C·샘플 쇼핑몰)·데모 계정을 일괄 삭제하는 마이그레이션(`docs/migrations/2026-07-cleanup-sample-data.sql`)이 포함되어 있어요. `rfps.is_sample`·`workspaces.is_demo`·`workspaces.sample_seeded_at` 컬럼은 제거되고 `users.onboarding`(jsonb)이 추가돼요. 배포 순서는 `docs/DEPLOY_LIGHTSAIL.md` 참조.

## [0.2.66.0] - 2026-07-05

### Changed

- **첨부파일 저장소가 Cloudflare R2로 이전됐어요**: 견적 요청·견적·채팅에 올리는 첨부파일이 이제 데이터베이스(Postgres bytea) 대신 Cloudflare R2 오브젝트 스토리지에 저장돼요. DB 백업이 첨부파일 용량과 분리돼 가벼워져요. 다운로드 주소와 권한 검사(봉인 견적 접근 제어 포함)는 기존과 동일하게 동작해요.
- **첨부파일 업로드가 브라우저에서 R2로 바로 올라가요**: 서버가 짧은 유효기간의 서명 URL을 발급하면(선언한 파일 크기·형식이 서명에 포함) 브라우저가 R2에 직접 업로드하고, 완료 시점에 서버가 파일 시그니처(매직바이트)를 검증한 뒤에야 첨부가 노출돼요. 검증 전(pending) 첨부는 어디에도 보이지 않고 연결도 안 되며, 1시간 넘게 방치되면 자동 청소돼요(`/api/cron/sweep-uploads`).
- **다운로드·PDF 미리보기도 R2에서 직접 받아요**: `GET /api/files/{id}`가 권한 검사 후 15분짜리 서명 URL로 302 리다이렉트해요. 파일 바이트가 앱 서버를 거치지 않아 동시 다운로드가 서버 대역폭과 완전히 분리되고, PDF 구간(Range) 로딩은 R2가 네이티브로 처리해요.
- **운영 환경에서 저장소 설정이 빠지면 배포 즉시 알 수 있어요**: R2 접속 정보 4종 중 하나라도 빠진 채 프로덕션을 기동하면 첫 파일 요청에서야 실패하는 대신 서버 부팅 시점에 바로 에러로 멈춰요(PM2 crash로 가시화).

### Removed

- **Postgres bytea 첨부 저장 백엔드(`attachment_blobs`)를 제거했어요**: 기존 첨부 데이터는 폐기하기로 결정돼 마이그레이션 없이 전환해요. 배포 시 1회 데이터 정리 절차는 `docs/DEPLOY_LIGHTSAIL.md` 런북을 따라요.
- **서버 경유 멀티파트 업로드 라우트(`POST /api/files/upload`)를 제거했어요**: 모든 업로드 진입점(채팅 컴포저·견적 요청 첨부·견적서 업로드)이 presigned 직행 업로드 공용 헬퍼로 전환됐어요.

### Infrastructure

- 로컬 개발도 프로덕션과 동일하게 실제 R2 버킷을 사용해요 — 폴백 백엔드는 의도적으로 없어요. 단위 테스트는 mock(InMemoryStorage 주입)으로 동작하고, 실제 바이트가 필요한 e2e PDF 스펙은 R2 설정이 없으면 자동으로 건너뛰어요.
- `@aws-sdk/client-s3`·`@aws-sdk/s3-request-presigner` 의존성 추가, R2 환경변수 예시(`.env.production.example`)와 배포 런북 갱신(버킷 CORS 설정·sweep-uploads crontab 등록 절차 포함).
- 직행 업로드에는 R2 버킷 CORS 설정(PUT 허용)이 필수예요 — 절차는 `docs/DEPLOY_LIGHTSAIL.md` §첨부파일 저장소 전환을 따라요.

## [0.2.66.0] - 2026-07-06

### Changed

- **PG 랜딩 페이지가 더 간결해졌어요**: 고객사 유형 섹션의 캐러셀 컨트롤을 없애고 스크롤 가능한 정적 카드 리스트로 바꿨어요. 프로세스 섹션도 함께 정리해서 페이지 계층이 더 명확해졌어요.
- **PG 히어로 제품 창이 공용 핀 씬으로 통합됐어요**: PG 메인 화면 제품 윈도우가 구매사와 같은 스크롤 핀 컴포넌트를 쓰도록 리팩터링했어요. 헤더와 목업도 재사용하면서 코드 일관성이 높아졌어요.
- **PG 랜딩 CTA가 파트너 가입 링크로 변경됐어요**: "시작하기" 버튼이 기존 signup 플로우 대신 파트너 가입 링크로 바뀌었어요.

### Fixed

- **좁은 모바일 화면에서 랜딩 헤더 텍스트가 더 이상 단어 중간에서 끊기지 않아요**: 320~375px 폭에서 헤더의 주요 문구(제목, 안내)가 단어 경계에서 줄바꿈되도록 수정했어요.
- **모바일에서 데모 앱 영역이 더 이상 누르기 어렵지 않아요**: 데모 제품 윈도우의 터치 영역이 개선되어 모바일 사용자가 더 쉽게 상호작용할 수 있어요.

## [0.2.65.4] - 2026-07-03

### Fixed

- **랜딩 히어로 배경이 모바일에서도 손가락을 따라 반응해요**: 다크 오프닝 씬의 커서 트레일 연출(ASCII 문자 필드)이 마우스가 있는 기기에서만 켜지고 터치 기기에서는 정적 배경만 보이던 문제를 고쳤어요. 이제 손가락으로 화면을 쓸어 넘기면 그 궤적을 따라 배경 문자가 반응해요. "동작 줄이기"를 켠 경우에는 그대로 정적 배경을 유지해요.

## [0.2.65.3] - 2026-07-03

### Fixed

- **마스터(운영자) Google 로그인이 파트너 주소에서 시작해도 더 이상 간헐적으로 실패하지 않아요**: `partner.supporter-b.com/login/ops`에서 로그인을 시작하면 OAuth 보안 쿠키(PKCE)가 파트너 호스트에만 심기고 Google 콜백은 구매사 호스트로 돌아와, `pkceCodeVerifier` 검증 실패(`error=Configuration`)로 로그인이 실패하던 문제를 고쳤어요. 이제 파트너 호스트의 `/login/ops`는 구매사 호스트로 자동 이동해 항상 콜백과 같은 호스트에서 로그인을 시작해요.
- **Google OAuth 클라이언트가 반쪽만 설정되면 마스터 로그인 화면이 아예 열리지 않아요**: `AUTH_GOOGLE_ID`만 있고 `AUTH_GOOGLE_SECRET`이 비어 있으면, 죽은 로그인 버튼(클릭 시 Configuration 에러) 대신 `/login/ops`가 404로 닫히고 Google 프로바이더 등록도 건너뛰도록 두 게이트(프로바이더 등록·화면 노출)를 ID+SECRET 쌍 확인으로 맞췄어요.

## [0.2.65.2] - 2026-07-03

### Fixed

- **모바일에서 절감액 계산기 글자가 더 이상 단어 중간에서 끊기지 않아요**: 랜딩 "직접 계산해 보세요" 계산기가 좁은 화면(320~375px)에서 라벨·금액·차트 문구가 단어 중간에서 줄바꿈되던 문제를 고쳤어요. 카드 여백을 모바일에서만 줄이고, 숫자·라벨이 안 끊기도록 정리했어요. 데스크톱 화면은 그대로예요.

## [0.2.65.1] - 2026-07-03

### Fixed

- **모바일에서 절감액 계산기 위를 스크롤할 때 화면이 걸리지 않아요**: 랜딩 "직접 계산해 보세요" 계산기의 슬라이더가 세로 스와이프(스크롤)까지 붙잡아, 손가락이 슬라이더 띠를 지날 때 페이지가 안 넘어가고 슬라이더 값만 튀던 문제를 고쳤어요. 슬라이더의 `touch-action`을 `none`에서 `pan-y`로 바꿔, 세로 스크롤은 그대로 페이지에 전달하고 가로 드래그만 값 조정으로 처리해요.

## [0.2.65.0] - 2026-07-03

### Fixed

- **랜딩 헤더가 로그인 여부에 따라 다시 제대로 바뀌어요**: 랜딩 페이지가 완전히 정적으로 미리 생성되면서(v0.2.63.0), 로그인한 방문자도 헤더에 계속 "로그인" 버튼만 보이고 "앱으로 이동" 링크가 나타나지 않던 문제를 고쳤어요. 이제 페이지가 열린 뒤 브라우저에서 실제 로그인 상태를 다시 확인해서, 로그인한 방문자에게는 "앱으로 이동" 링크를, 아닌 방문자에게는 그대로 "로그인" 버튼을 보여줘요.

## [0.2.64.0] - 2026-07-03

### Changed

- **랜딩 히어로 배경에서 마우스를 움직이면 커서 궤적이 단색 하늘색 대신 셀마다 하늘·시안·보라가 은은하게 섞인 색으로 반짝여요**: 다크 오프닝 씬의 ASCII 문자 필드 배경에서, 커서가 지나간 자리에 깨어나는 문자와 그 은은한 빛이 이전에는 전부 같은 색이었는데, 이제 위치별로 살짝 다른 색조가 섞여 궤적이 조금 더 생동감 있게 반짝여요. 다크 모드로 바꿔도 자연스럽게 맞는 색으로 따라가요.

### Fixed

- **일부 화면(로그인, 테마 전환, 견적 작성 등)의 자동 테스트가 실제로는 문제 없는 코드인데도 실패로 보이던 문제를 고쳤어요**: 테스트 환경 설정의 근본 원인을 찾아 고쳐서, 관련 없어 보이던 여러 화면의 테스트가 다시 정확하게 통과해요. 사용자가 직접 겪는 화면 동작에는 변화가 없어요.

## [0.2.63.0] - 2026-07-03

### Changed

- **랜딩 페이지(구매사/PG 모두)가 완전히 정적으로 미리 생성돼요**: 이전에는 접속한 호스트(구매사/PG)를 요청마다 서버에서 확인해 화면을 나눠 그렸는데, 이제 그 분기를 진입 단계에서 미리 처리해서 두 랜딩 모두 빌드 시점에 정적으로 만들어져요. 눈에 보이는 화면은 그대로고, 캐시가 더 잘 걸려서 처음 열릴 때 조금 더 빨라져요.

## [0.2.62.0] - 2026-07-03

### Changed

- **랜딩 히어로 헤드라인의 순환 문구가 타이핑 효과 대신 작은 ASCII 기호로 흩어졌다가 다시 조립되는 효과로 바뀌었어요**: "협상의 주도권을" 등 5개 문구가 바뀔 때마다 한 글자씩 지워지고 다시 찍히던 타이핑 애니메이션이, 글자가 작은 기호(`· : + # ₩` 등)로 잠깐 흩어졌다가 원래 자리로 조립되는 효과로 바뀌었어요. 배경의 ASCII 문자 필드와 같은 기호 팔레트를 써서 시각적으로 통일감이 있어요. 접근성 설정에서 "동작 줄이기"를 켜둔 경우에는 흩어지는 애니메이션 없이 문구만 바로 바뀌어요.

## [0.2.61.0] - 2026-07-03

### Fixed

- **랜딩 견적 비교표가 창 크기가 아니라 표 자체 너비가 바뀔 때는 스크롤 힌트를 놓치던 문제를 막았어요**: 이제 표 폭이 어떤 이유로 바뀌든 스크롤 가능 여부 힌트가 항상 정확해요. 실제 표에서만 스크롤 컨테이너 표식을 남기고, 미리보기 목업에는 남기지 않아 앞으로 페이지 전체 자동 검사가 헷갈리지 않아요.
- **랜딩 데모 창을 화면을 다 못 본 상태에서도 계속 최적 배율로 유지해요**: 데모 창 폭이 바뀌는 경로가 새로 생겨도 진입 확대 배율이 항상 화면 안에 맞게 재계산돼요.
- **랜딩 데모 가이드 커서가 화면 밖으로 스크롤된 뒤에도 계속 움직이던 문제를 막았어요**: 데모가 화면에서 벗어나면 커서 추적을 멈추고, 다시 스크롤해 들어오면 자연스럽게 이어서 움직여요. 배터리·성능 낭비를 줄였어요.
- **PG 데모의 안내 커서 위치·문구 로직을 구매사 데모와 같은 방식으로 정리했어요**: 화면 뒤에서 로직이 통일돼 앞으로 두 데모가 어긋날 일이 줄어요.

## [0.2.60.1] - 2026-07-03

### Fixed

- **랜딩 데모 창이 좁은 화면에서 화면 밖으로 살짝 잘려 보이던 문제를 막았어요**: 데모가 화면에 들어올 때 창이 확대되는데, 창 폭에 여유가 많지 않은 화면(태블릿·창을 다 펴지 않은 노트북 등)에서는 확대된 만큼 화면 경계를 넘어 가장자리가 잘려 보였어요. 이제 화면에 맞는 만큼만 확대돼서 어떤 화면 폭에서도 잘리지 않아요.

## [0.2.60.0] - 2026-07-02

### Fixed

- **랜딩 견적 비교표를 좁은 화면에서 스와이프할 때 어디까지 더 볼 수 있는지 알기 어려웠어요**: 이제 표를 옆으로 넘기면 열 경계에 맞춰 자연스럽게 정렬되고, 더 볼 내용이 있으면 오른쪽 끝에 옅은 힌트가 떠요. 끝까지 넘기면 힌트가 사라지고, 다시 앞으로 돌아가면 다시 나타나요.
- **히어로 미리보기 창의 예시 비교표에서 스크롤 힌트가 계속 떠 있던 문제를 막았어요**: 실제로는 스크롤할 수 없는 미리보기 창이라 힌트가 사라지지 않고 계속 보이던 부분을 고쳤어요.

## [0.2.59.0] - 2026-07-02

### Fixed

- **랜딩 페이지 미리보기 데모에서 사이드바를 접거나 펼 수 없어요**: 모바일 화면에서 데모 상단의 사이드바 토글 버튼을 누르거나 ⌘/Ctrl+B를 눌러도 더 이상 반응하지 않아요. 예전에는 눌렀을 때 실제 앱의 사이드바 패널이 데모 창 밖으로 튀어나와 화면을 가리던 문제가 있었어요.

## [0.2.58.0] - 2026-07-02

### Changed

- **구매사 랜딩 첫 화면이 몰입형 다크 오프닝으로 바뀌었어요**: 어두운 화면에서 헤드라인이 단어별로 떠오르고, 스크롤을 내리면 견적 비교 화면(제품 창)이 아래에서 떠올라 자리 잡은 뒤 배경이 밝게 걷혀요. 헤더도 다크 씬 위에서는 투명한 라이트 톤이었다가 씬을 지나면 원래 모습으로 돌아와요.
- **다크 씬 배경이 커서에 반응하는 ASCII 문자 필드가 됐어요**: 마우스가 지나간 자리를 따라 문자들이 · : + * # 로 깨어나 파랗게 빛나고 2~3초에 걸쳐 잔상처럼 식어요. 배경에는 은은한 블룸이 깔려 단조로운 검정이 아니에요. 터치 기기·동작 줄이기 설정에서는 차분한 정적 배경만 보여요.

### Fixed

- **모바일에서 스크롤할 때 다크 씬 아래로 밝은 띠가 보일 수 있던 문제를 막았어요**: 주소창이 접히며 화면이 커져도 씬 높이가 함께 늘어나요.
- **보는 중에 라이트/다크 테마가 바뀌어도 배경 문자 색이 즉시 따라와요**: 시스템 자동 전환 시 문자가 배경색에 묻혀 보이지 않을 수 있던 문제를 고쳤어요.
- **보이지 않을 때는 배경 연출이 완전히 쉬어요**: 섹션 링크로 바로 진입했거나 탭을 벗어난 동안, 히어로를 지나간 뒤에는 배경 애니메이션이 멈춰 배터리를 아껴요.

## [0.2.57.1] - 2026-07-02

### Fixed

- **랜딩 데모의 안내 커서가 이제 눌러야 할 항목을 정확히 가리켜요**: 데모가 화면에 들어올 때 창이 살짝 확대되면서 안내 커서가 실제 클릭 위치보다 오른쪽·아래로 어긋나 있던 문제를 고쳤어요. 커서가 사이드바 항목·목록·버튼의 정중앙에 정확히 얹혀요.
- **모바일에서도 데모를 끝까지 둘러볼 수 있어요**: 좁은 화면에서는 사이드바가 접히면서 데모가 중간에 막히던 문제를 고쳤어요. 이제 사이드바를 눌러야 하는 단계에서는 화면 안에 '다음 화면 보기' 버튼이 나타나 흐름을 끝까지 따라갈 수 있고, 진입 확대는 가로 잘림을 막기 위해 모바일에서 꺼져요.

## [0.2.57.0] - 2026-07-02

### Changed

- **랜딩 페이지 주요 섹션이 스크롤에 반응하도록 바뀌었어요**: 구매사·파트너 랜딩에서 '이런 불편함' 문제 카드가 스크롤에 따라 한 장씩 쌓이며 나타나요. 구매사 '해결' 섹션은 저절로 넘어가던 방식 대신 스크롤에 맞춰 포인트와 비교표 강조가 이동해요.
- **제품 미리보기 데모에 안내 커서가 생겼어요**: 데모가 화면에 들어오면 창이 살짝(110%) 확대되고, 안내 커서가 다음에 누를 항목을 힌트 문구와 함께 가리켜요. 커서가 가리키는 항목을 누르면 다음 화면으로 넘어가며 견적 요청부터 견적 확인·선정까지의 흐름을 직접 눌러보며 따라갈 수 있어요.

## [0.2.56.0] - 2026-07-02

### Changed

- **알림 발송 코드를 단일 `notify()` API로 통합했어요 (내부 리팩터링)**: 인앱 알림과 이메일 알림을 내보내던 코드가 서비스마다 따로 흩어져 있던 것을, 받는 사람과 채널(인앱·이메일)을 명시적으로 골라 부르는 하나의 진입점으로 모았어요. 견적 요청·견적 도착·선정·재요청·채팅·팀 채팅 등 12곳의 알림 흐름이 모두 이 방식으로 바뀌었어요. 사용자에게 보이는 동작은 이전과 완전히 동일해요 — 알림이 오는 방식·내용·시점 모두 그대로예요. 앞으로 알림 기능을 더 안전하고 빠르게 손볼 수 있는 토대예요.

## [0.2.55.0] - 2026-07-01

### Fixed

- **파트너(PG) 랜딩 페이지 이미지가 깨져 보이던 문제를 고쳤어요**: 로그인하지 않은 방문자가 파트너·구매사 랜딩 페이지를 볼 때, 소개 이미지들이 로그인 화면으로 튕겨나가며 깨져 보였어요. 이제 익명 방문자도 랜딩 페이지의 모든 이미지를 정상적으로 볼 수 있어요.

## [0.2.54.7] - 2026-07-01

### Changed

- **테마를 바꾸면 전환 효과가 생겼어요**: 라이트/다크 모드를 바꿀 때 클릭한 버튼 위치에서 원이 펼쳐지며 새 테마로 전환돼요. 사용자가 움직임 줄이기(prefers-reduced-motion)를 설정했거나 지원하지 않는 환경에서는 기존처럼 즉시 전환돼요.

## [0.2.54.6] - 2026-06-30

### Changed

- **PG를 추가할 때 검색창 대신 PG사 칩을 바로 눌러요**: 견적 요청 상세의 'PG 관리'에서 PG를 추가하는 방식을, 검색 드롭다운을 열어 고르던 데서 견적 요청 작성 화면과 똑같이 PG사 칩을 바로 눌러 추가하는 방식으로 바꿨어요. 추가할 수 있는 PG사가 한눈에 칩으로 보이고, 누르면 바로 '대기중'으로 쌓여요. 이미 초대한 PG는 칩 목록에서 빠져서 헷갈리지 않아요.

## [0.2.54.5] - 2026-06-30

### Changed

- **견적 요청 보낼 때 PG사가 처음부터 전체 선택돼 있어요**: 견적 요청 작성 마지막 PG 선택 단계에서 초대할 PG사가 기본으로 모두 선택된 상태로 시작해요. 매번 하나씩 고르거나 '전체 선택'을 누르지 않아도 되고, 빼고 싶은 곳만 해제하면 돼요. 일부나 전체를 해제하면 그 선택은 그대로 유지돼요 — 단계를 옮기거나 새로고침해도 다시 전체 선택으로 되돌아가지 않아요. 견적 요청을 보낸 뒤 새 요청을 시작하면 다시 전체 선택으로 시작해요.

## [0.2.54.4] - 2026-06-30

### Changed

- **선정한 곳의 안내 문구와 담당자 연락처를 한곳에서 봐요**: PG를 선정하면 화면 맨 위에 따로 떠 있던 "○○를 선정했어요 · 담당처와 연락을 이어나가보세요" 안내를, 견적 비교 화면의 담당자 연락처(이메일·전화) 카드 바로 위로 옮겼어요. 이제 안내 문구와 실제 연락처를 한 번에 보고 바로 연락할 수 있어요.

## [0.2.54.3] - 2026-06-30

### Changed

- **AI 크롤러가 `/llms.txt`를 자동으로 찾을 수 있어요**: `<head>`에 `<link rel="alternate" type="text/plain" href="/llms.txt">` 링크를 추가했어요. ChatGPT·Perplexity 같은 AI 크롤러가 표준 방식으로 제품 설명 파일을 자동 발견해요.
- **프록시 우회 경로 보안 강화**: `/robots.txt`, `/sitemap.xml`, `/llms.txt` 등 정적 파일 경로를 프록시 제외 패턴에서 정확히 매칭하도록 수정했어요. 이전에는 `llms.txt`가 정규식에서 점(`.`)을 임의 문자로 취급해 `/llmsXtxt` 같은 경로도 우회 대상에 포함될 수 있었어요. 이제 파일명의 점은 점으로만 매칭해요.

## [0.2.54.2] - 2026-06-30

### Removed

- **워크스페이스 테이블에서 사용하지 않는 `has_logo` 컬럼을 정리했어요**: 로고 유무를 `logo_updated_at` 단일 컬럼으로 관리하도록 전환한 뒤 남아있던 `has_logo` 컬럼을 스키마에서 제거했어요. 배포 후 `pnpm db:push`로 DB에 적용하면 돼요. 사용자에게 보이는 변화는 없어요.

## [0.2.54.1] - 2026-06-30

### Fixed

- **견적 정산주기 형식을 서버에서도 한 번 더 확인해요**: PG가 보낸 견적의 정산주기(`D+1`·`W+2`·`M+1` 같은 형식)가 올바른지 서버가 직접 검증하도록 했어요. 지금까지 작성 화면에서는 막고 있었지만 서버는 빈 값만 걸러서, 화면을 거치지 않은 비정상 값(`D+0`·자유 텍스트 등)이 그대로 저장될 여지가 있었어요. 이제 견적 제출 경로에서 형식이 어긋난 값은 저장되지 않아요. 서버 검증은 작성 화면과 똑같이 "양의 정수"만 요구하고 상한을 두지 않아, 화면에서 통과한 값이 제출 단계에서 거절되는 일이 없어요. 형식 규칙을 한 곳(단일 출처)으로 모아 견적 제출과 견적 템플릿 저장이 똑같은 기준을 쓰도록 정리했고, 견적 비교에서 '당일 정산(D+0)' 같은 구매사 현재 조건은 예전처럼 그대로 비교돼요. (작성 화면 동작은 그대로라 일반 사용자가 보는 화면 변화는 없어요 — 데이터 정확성·안정성 개선)

## [0.2.54.0] - 2026-06-30

### Changed

- **'선정 완료'와 '마감'을 '마감' 하나로 모아서 보여드려요**: 구매사 견적 요청 화면에서 선정이 끝났든 그냥 마감됐든 모두 '마감'으로 묶어 한눈에 보여드려요. 보드는 '진행중·마감' 두 칸으로 단순해지고, 각 견적의 결과는 칩으로 구분해요 — 선정완료·미선정·취소. 목록·사이드바·필터·홈 지표에서도 '선정 완료'가 '마감'에 합쳐지고, '마감'을 누르면 선정완료 건까지 함께 보여드려요. 진행 중인 견적 카드를 '마감'으로 끌어다 놓으면 상세 화면으로 옮겨가 거기서 선정하거나 취소할 수 있어요. (화면에 보이는 방식만 바뀌고 데이터·서버 동작은 그대로예요) **배포 후 1회 실행**: 기존 워크스페이스의 칸반 '선정 완료' 컬럼을 정리하려면 `tsx scripts/remove-awarded-kanban-columns.ts`를 실행하세요. 멱등 스크립트라 중복 실행해도 안전해요.

## [0.2.53.1] - 2026-06-30

### Fixed

- **견적 비교 화면에서 PG 프로필 카드에도 로고가 보여요**: 구매사가 견적을 비교할 때 활성 PG의 이름 옆 아바타(클릭하면 뜨는 신원 카드 포함)가 로고 대신 이니셜만 보여주던 문제를 고쳤어요. 비교 탭에는 로고가 보이는데 프로필 카드만 이니셜이던 차이를 없애, 이제 업로드된 로고가 프로필 카드에도 똑같이 표시돼요.

## [0.2.53.0] - 2026-06-30

### Added

- **견적 작성 중 입력이 안 맞으면 바로바로 알려드려요**: PG가 견적을 작성할 때 정산주기·수수료가 비어 있거나 값이 범위를 벗어나면 그 자리에서 즉시 안내해요. ① '견적 보내기'가 이유 없이 회색으로 막히던 동작을 없앴어요 — 누르면 빠진 항목이 있는 단계로 데려가고 무엇이 비었는지 토스트로 알려줘요. ② 정산주기 칸에 '필수' 표시가 붙고, 보내기를 시도한 뒤에도 비어 있으면 빨갛게 강조해요. ③ 수수료는 한 칸 이상 채워야 한다는 안내를 단계 위에 보여줘요. ④ 수수료율은 100%를 넘게 입력할 수 없도록 막아 잘못된 값이 서버까지 가지 않아요(가상계좌 건당 금액은 상한 없이 자유롭게 입력해요). ⑤ 서버가 거절하면(첨부 견적서 오류 등) 그 원인이 있는 단계로 이동해서 보여줘요. 구매사 견적 요청 작성 화면의 실시간 검증 방식을 PG 견적 작성에도 똑같이 적용했어요.

## [0.2.52.0] - 2026-06-30

### Changed

- **'참여 가능한 견적'(오픈 게시판)을 잠시 닫았어요**: PG 회원이 보던 '참여 가능한 견적' 게시판을 화면에서 임시로 숨겼어요. 사이드바·홈 탐색 섹션·검색(⌘K)에서 사라지고, 주소로 직접 들어가도 "참여 가능한 견적을 잠시 닫았어요" 안내 화면이 보여요. 구매사 화면에서도 견적 요청 작성 시 '오픈 게시판에 노출하기' 선택과 노출 상태 표시가 함께 숨겨져요. 데이터와 서버 동작은 그대로라 다시 켜면 그동안 등록된 견적도 즉시 노출돼요. 초대 기반 견적 요청과 이미 들어온 참여 요청 처리에는 영향이 없어요. (단일 플래그로 제어하는 임시 숨김)

## [0.2.51.4] - 2026-06-30

### Changed

- **로컬 lint·타입체크가 훨씬 빨라졌어요 (개발자용)**: `pnpm lint`에 ESLint 증분 캐시(`--cache`)를 켜서 재실행이 280초→14초로 줄었어요. pre-commit 훅도 매번 전체를 검사하던 방식에서 **스테이지된 변경 파일만** 검사하도록 바꿨어요 — eslint는 바뀐 파일만 보고, 타입체크는 `.ts/.tsx` 변경이 있을 때만 전체를 돌리고 없으면 건너뛰어요(타입체크는 whole-program이라 안전하게 파일 단위로 쪼갤 수 없어 전체 검사를 유지). worktree마다 생기는 `.eslintcache`는 git에서 무시해요. (사용자 화면 변화 없음 — 개발 도구 개선)

## [0.2.51.3] - 2026-06-30

### Added

- **GitHub Actions에서 k6 성능 테스트가 자동으로 실행돼요**: 격리된 Docker 환경에서 Postgres를 새로 세팅하고, smoke 테스트(2VU·1분)는 PR마다, load 테스트(10VU·20분 램프)는 main 브랜치 push마다 실행해요. 고정 UUID 시드(구매사 1개·PG 10개·RFP 10개)로 일관된 재현이 가능하고, Server Action 해시를 빌드 산출물에서 자동 추출해 인증된 사용자처럼 POST할 수 있어요. (사용자 화면 변화 없음 — CI 인프라 추가)

## [0.2.51.2] - 2026-06-29

### Fixed

- **파일 업로드 시 불필요한 DB 조회 3회를 제거했어요**: `chat`·`bid_note`·`team_message` 첨부 업로드 경로에서 멤버십을 DB에 재확인하던 `isMember` 호출 3개를 삭제했어요. 45b44e43 커밋에서 `removeMember`가 세션버전을 즉시 올리고 `isSessionRevoked()`로 만료된 세션을 차단하는 정책으로 전환됐는데, 다운로드 경로·`permissions.ts`는 업데이트됐지만 업로드 경로만 누락됐어요. 이번 수정으로 업로드마다 발생하던 DB 왕복 3회가 사라지고, download/upload ACL 정책이 일관되게 유지돼요.
- **워크스페이스에서 멤버를 제거하면 세션이 즉시 무효화돼요**: 그동안 멤버를 제거해도 기존 JWT 세션이 만료되기 전까지 파일·채널 접근이 이론상 가능했어요. 이제 멤버를 제거하는 즉시 세션 버전이 올라가 이후 모든 파일 요청이 차단되고, Centrifugo 실시간 WS 연결도 끊어져요. 파일 접근 권한 검사도 단순해졌어요 — 서명된 JWT의 워크스페이스 클레임을 멤버십 증명으로 신뢰하고, 요청마다 DB 멤버십을 재조회하지 않아요.

## [0.2.51.1] - 2026-06-28

### Fixed

- **딜룸·메시지함·견적 비교에서 PG·구매사 로고가 이니셜 대신 실제 이미지로 보여요**: PR#246에서 워크스페이스 로고를 DB로 전환한 후, 여러 데이터 경계에서 `logoUpdatedAt`이 컴포넌트까지 전달되지 않아 로고 대신 이니셜만 표시됐어요. 이번 수정으로 딜룸 채팅 레일 헤더, 견적 비교 탭(구매사 화면), 메시지함 스레드 헤더 모두 업로드된 로고를 정상적으로 표시해요.

## [0.2.51.0] - 2026-06-28

### Added

- **AI 검색엔진이 서포터비를 더 잘 이해하도록 llms.txt를 추가했어요**: ChatGPT·Claude·Perplexity 같은 생성형 AI가 사이트를 읽을 때 참고하는 `/llms.txt`·`/llms-full.txt` 파일을 새로 제공해요. 구매사 사이트(supporter-b.com)와 PG 파트너 사이트(partner.supporter-b.com)가 각각 자기 청중에 맞는 서비스 소개·이용 절차·자주 묻는 질문을 깔끔한 텍스트로 담아, AI가 서포터비를 정확하게 소개하고 인용할 수 있어요. 무료 정책·수수료 절감 통계·봉인 입찰 같은 핵심 사실도 함께 담겨요.

### Changed

- **검색·AI 노출이 더 잘 되도록 sitemap과 robots를 정비했어요**: 사이트맵이 그동안 3개 주소만 담았는데, 이제 공개 페이지 전체와 새 AI 텍스트 파일까지 포함해요. robots 설정에 주요 AI 크롤러(GPTBot·ClaudeBot·PerplexityBot 등)를 명시적으로 허용하고, 구매사·PG 두 사이트가 각각 자기 주소를 가리키도록 정리했어요. (사용자 화면 변화는 없어요.)
## [0.2.50.0] - 2026-06-28

### Changed

- **선정이 끝나면 견적서 화면이 '선정 결과'로 또렷하게 바뀌어요**: 그동안 선정 후 담당자 연락처가 탭 위에 작은 카드로 떠 있었는데, 이제 견적 작성 화면 자체가 결과로 승격돼요. 선정된 PG에게는 "✓ 이 견적이 선정됐어요"와 함께 구매사 담당자 연락처(아바타·이름·이메일·전화)가 한 카드로 정리되고, 이메일·전화 옆 복사 버튼으로 바로 복사할 수 있어요. 구매사 화면에도 "○○를 선정했어요" 결과 패널이 떠서 선정한 PG 담당자와 바로 이어져요. 선정되지 않은 PG에게는 "이번엔 선정되지 않았어요" 안내가 차분한 톤으로 보이고(연락처 없음), 화면 상단 상태 칩도 '선정됨' 또는 '선정 마감'으로 또렷하게 바뀌어요. 연락처 교환 범위(선정된 PG↔구매사만)와 봉인입찰 경계는 그대로예요.
## [0.2.49.1] - 2026-06-28

### Fixed

- **올린 본인이 견적서·첨부를 보낼 때 'INVALID_ATTACHMENT' 오류로 막히던 문제를 고쳤어요**: 운영 계정으로 PG 워크스페이스에 들어가 견적서를 첨부해 보내면 매번 'INVALID_ATTACHMENT'가 뜨며 제출이 막혔어요. 첨부 검증이 '올린 사람이 워크스페이스 멤버인가'를 따졌는데, 운영 계정은 멤버 명단에 없어서 자기가 올린 파일도 거부됐던 거예요. 이제 팀 채팅·메모와 같은 기준('올린 본인인가')으로 검증해서 정상적으로 보낼 수 있어요. 상대방 채팅 첨부에도 같은 문제가 있어 함께 고쳤어요. 또 이 오류가 날 때 코드 대신 "첨부한 견적서를 확인할 수 없어요. 다시 올려주세요." 안내가 보여요.

## [0.2.49.0] - 2026-06-28

### Added

- **견적 요청 PG 선택·검토 화면에 PG사 로고가 보여요**: 견적 요청을 만들 때 'PG 선택'(3단계) 칩과 '검토'(4단계)의 초대 PG 목록에서 각 PG사 이름 옆에 해당 워크스페이스 로고가 함께 떠요. 로고를 등록한 PG는 사진으로, 아직 없는 PG는 회사명 이니셜로 표시돼서 어떤 PG인지 한눈에 알아볼 수 있어요. 사이드바·메시지 상대방 카드에서 쓰던 로고와 같은 이미지라 어디서 보든 일관돼요.
## [0.2.48.0] - 2026-06-28

### Changed

- **이미 있는 PG사를 선택해 가입하면 그 회사의 '관리자'로 합류해요**: 그동안 기존 PG사를 골라 합류한 담당자는 일반 멤버로 들어와 팀원 초대·역할 변경 같은 워크스페이스 관리를 할 수 없었어요. 이제 관리자(admin)로 합류해요. 단, 기존처럼 **관리자 승인을 받은 뒤에** 입장하는 흐름은 그대로예요(승인 전에는 대기 화면에서 기다려요).

### Fixed

- **승인 전 미승인 관리자가 관리자 기능을 쓰지 못하도록 막았어요**: 위 변경으로 합류 담당자가 관리자가 되면서, 승인을 받기 전에는 관리자 전용 동작(팀원 강퇴·초대·역할 변경·워크스페이스 이름 변경·활동 기록 열람)이 서버에서 거부되도록 했어요. 화면이 아니라 서버에서 막기 때문에 직접 요청을 보내도 우회할 수 없어요. 미승인 관리자는 관리자 수 집계와 관리자 대상 알림·메일 수신자에서도 제외돼요.

## [0.2.47.0] - 2026-06-27

### Added

- **선정이 끝나면 견적서 화면에서 담당자 연락처를 바로 교환해요**: 견적이 선정되면 구매사 딜룸에는 '선정한 PG 담당자 연락처', 선정된 PG 딜룸에는 '구매사 담당자 연락처' 카드가 떠요. 담당자 이름·이메일·전화번호가 보이고, 이메일을 누르면 메일 작성이, 전화번호를 누르면 전화 연결이 바로 열려서 플랫폼 밖에서 바로 이어갈 수 있어요. 연락처 교환은 **선정된 PG와 구매사 사이에서만** 일어나고, 선정되지 않은 PG에게는 연락처 없이 "이번엔 선정되지 않았어요" 안내만 보여줘요. 전화번호를 등록하지 않은 담당자는 이메일만 보여주고, 탈퇴한 담당자의 연락처는 노출하지 않아요.

## [0.2.46.0] - 2026-06-27

### Changed

- **선정이 끝난 견적은 '메시지'(통합 메시지함)에서도 미선정 상대와의 채팅이 닫혀요**: 그동안 견적 상세(딜룸)에서는 선정 후 미선정 PG와 구매사 사이 채팅 입력이 막혔지만, '메시지' 화면에서는 같은 대화를 열어 계속 보낼 수 있었어요. 이제 메시지 화면에서도 선정으로 종결된 대화는 입력창이 비활성화되고 "견적 선정이 끝나 이 대화는 종료됐어요" 안내가 떠요(구매사·미선정 PG 양쪽 모두). 선정된 상대와의 대화는 그대로 열려 있어요.

## [0.2.45.0] - 2026-06-27

### Changed

- **가상계좌 수수료를 '건당 얼마'(정액)로 입력·표기해요**: 가상계좌는 결제 건당 고정 금액으로 부과되는데, 그동안은 카드처럼 퍼센트(%) 요율로만 받을 수 있었어요. 이제 견적 작성·견적 템플릿에서 가상계좌 수수료를 '원' 단위 건당 금액으로 입력하고(예: 건당 300원), 견적 검토·비교·제출 요약·템플릿 목록에서도 "건당 300원"처럼 원으로 보여줘요. 카드·간편결제 등 다른 결제수단은 기존 % 요율 그대로예요.

## [0.2.44.3] - 2026-06-26

### Changed

- **수수료율 설명에 부가세·실수령 예시를 추가했습니다**: 수수료율 ⓘ 인포 아이콘 설명에 부가세 10%까지 포함하면 실효 수수료가 3.3%가 되고(1만 원 결제 시 330원 차감·9630원 정산) 얼마가 정산되는지 구체적인 예시를 더했어요.

## [0.2.44.2] - 2026-06-26

### Changed

- **보증보험 설명에 예상 비용 안내를 추가했습니다**: 견적 작성·견적 제출 화면의 보증보험 ⓘ 인포 아이콘 설명에 비용 감(거래액의 1% 내외, 월 1000만원 기준 10만원 내외)을 덧붙여, 부담 규모를 한눈에 가늠할 수 있게 했어요.

## [0.2.44.1] - 2026-06-26

### Changed

- **견적 요청 세부 내용 입력 예시 문구를 다듬었습니다**: 작성 2단계 '견적 요청 세부 내용' 칸의 안내 예시를 더 구체적인 5줄로 바꿨어요(결제 전환율 최적화·카드/계좌 수수료 최소화·정산주기 단축·가입비/연회비 면제 등). 예시가 모두 보이도록 입력칸 높이도 한 줄 늘렸습니다.

## [0.2.44.0] - 2026-06-26

### Changed

- **견적 요청을 보낼 때 핵심 정보를 빠짐없이 받습니다**: 작성 2단계에서 아래 항목을 필수로 입력해야 다음 단계로 넘어가고, 견적을 보낼 수 있어요.
  - **견적 유형**(신규/갱신), **주요 판매 상품**, **전년도 연간 PG 총 거래액** — 이제 제목·홈페이지·결제수단과 함께 필수 항목이에요.
  - 비워 두면 해당 항목에 '필수' 표시와 안내 문구가 뜨고, 채우면 '입력 완료'로 바뀌어요.
  - **연간 PG 총 거래액**은 0보다 큰 금액만 받아요(0원·빈값은 입력 오류로 안내).
  - 임시저장은 지금처럼 비워 둬도 됩니다 — 필수 확인은 견적을 보낼 때만 적용돼요.

## [0.2.43.0] - 2026-06-26

### Changed

- **견적 요청 작성 마지막 단계가 입력한 정보를 빠짐없이 보여줍니다**: 4단계 이름을 '보내기 확인' → **'최종 견적 요청 정보 확인'** 으로 바꾸고, 견적 요청 요약을 제출 전 전체 검토 화면으로 다듬었습니다.
  - 비워 둔 항목도 숨기지 않고 **'미입력'** 으로 표시해 무엇이 빠졌는지 한눈에 확인할 수 있어요.
  - 그동안 요약에 없던 **견적 유형(신규/갱신)** 행을 추가했습니다.
  - 상세 요청사항·첨부파일도 비어 있을 때 빈 상태('미입력' / '첨부파일이 없어요')를 함께 보여줍니다.

## [0.2.42.1] - 2026-06-23

### Changed

- **코드 내부 구조 정리 (동작 변경 없음)**: 액션·서비스·레포지터리 계층 전반의 중복 코드를 공통 모듈로 통합했습니다.
  - `ActionResult<T>` 단일 타입 정의 (`lib/server/actions/_result.ts`) — 6개 도메인 파일의 동일 타입 선언 제거
  - `requireBuyerActor` / `requirePgActor` / `requireActiveWorkspace` 세션 헬퍼 공통화 (`lib/server/actions/_session.ts`)
  - `normalizeEmail` / `bucket15Min` 서비스 유틸 (`lib/server/services/_service-utils.ts`), `CHAT_DIGEST_WINDOW_MS` / `chatDigestBucket` 채팅 상수 (`lib/server/services/_chat-constants.ts`)
  - `PaymentFeesSchema` (Zod) 단일 출처 (`lib/rfp/payment-fees-schema.ts`) — 견적 제출·템플릿 저장 공유
  - `normalizeAvatarColor` / `VALID_AVATAR_COLORS` 추출 (`lib/server/repositories/drizzle/_avatar-color.ts`)
  - `toIso` 날짜 변환 헬퍼 추출 (`lib/server/repositories/drizzle/_repo-utils.ts`)
  - `isEditable` DOM 체크 함수 추출 (`lib/hooks/keyboard-utils.ts`) — 3개 단축키 훅 공유
  - Centrifugo 연결 토큰 라우트 TTL 만료 후 DB 재조회 테스트 추가

## [0.2.42.0] - 2026-06-23

### Changed

- **견적 채팅 화면 시각 폴리시**: 메시지 주변을 더 또렷하고 깔끔하게 다듬었어요.
  - **대화 목록** — 안 읽은 대화는 이름·미리보기를 진하게 표시(파란 점은 빼고 굵기로 구분), 지금 보고 있는 대화는 왼쪽에 파란 강조 바, 연결된 견적을 `코드 · 제목` 한 줄로 정리, '팀 채팅' 줄을 따로 분리했어요.
  - **대화 헤더** — 상대가 접속 중이면 '온라인'을 함께 보여주고, 어떤 견적에 대한 대화인지 코드·제목을 헤더에 노출하며, 입력 중 표시를 통통 튀는 점으로 바꿨어요.
  - **메시지·상태** — 날짜 구분선을 은은한 칩으로, 첨부 파일 목록과 대화 로딩을 펄스 스켈레톤으로, 빈 상태를 아이콘+안내 문구로 통일했어요.

### Added

- 타이핑·인라인 로딩을 나타내는 `TypingDots`, 날짜 구분 칩 `DateDivider` 공용 컴포넌트.

> 배포 메모: 프론트엔드 시각 변경만(`components/messages/*`). DB·환경 변수·빌드 설정 변경 없어요. 로딩 스켈레톤은 dev #290이 도입한 `components/ui/skeleton.tsx`를 재사용해요.

## [0.2.41.1] - 2026-06-23

### Added

- **견적 비교 화면에 '기준 구간' 헤더가 생겼어요**: 영세·중소1·중소2·중소3·일반 구간 토글이 이제 '기준 구간' 라벨이 달린 헤더 밴드 형태로 견적 비교 화면 상단에 항상 표시돼요. 지금 보고 있는 수수료가 어느 구간 기준인지 한눈에 알 수 있어요.
- **구간을 바꾸면 구간별 수수료 수치가 반짝 강조돼요**: 기준 구간을 바꾸는 순간, 카드 수수료와 결제수단별 요율 수치가 위에서 아래로 순서대로 배경색 플래시 효과로 "방금 값이 바뀌었다"는 걸 눈에 띄게 알려줘요. 모션 감소 설정을 켜 놓으셨다면 플래시 없이 즉시 바뀌어요.

> 배포 메모: 프론트엔드 전용 변경이에요 — `next build` 후 `pm2 restart bidit`로 적용돼요. DB 마이그레이션·환경 변수 변경 없어요.

## [0.2.41.0] - 2026-06-23

### Added

- **견적을 선정하면, 선정되지 않은 PG와의 대화가 자동으로 닫혀요**: 어떤 PG를 최종 선정하면 그 견적의 딜룸에서 선정되지 않은 PG와의 채팅 입력창이 비활성화되고 "견적 선정이 끝나 이 대화는 종료됐어요."라고 안내가 떠요. 구매사 화면에서는 선정되지 않은 PG로 전환할 때, PG 화면에서는 본인이 선정되지 않았을 때 보여요. 선정된 PG와의 대화와 팀 채팅은 그대로 열려 있고, 같은 PG와 진행 중인 다른 견적 대화도 영향을 받지 않아요.

> 배포 메모: 프론트엔드 전용 변경이에요 — `next build` 후 `pm2 restart bidit`로 적용돼요. DB 마이그레이션·환경 변수 변경 없어요. (대화 닫힘은 화면 안내 기준이며, 해당 견적 딜룸 범위에 한정돼요.)

## [0.2.40.1] - 2026-06-23

### Changed

- **(내부 문서) 디자인 시스템 문서를 코드와 정합화**: 사용자에게 보이는 변화는 없어요. 코드에 이미 일관되게 구현돼 있던 컴포넌트·토큰·규칙(스페이싱·z-index·포커스/disabled·아바타 팔레트·Toast·EmptyState·폼 입력 등)을 `DESIGN.md`에 빠짐없이 문서화하고, 코드·최신 스펙과 어긋나 있던 규칙(로딩 모션 허용·버튼 높이 32px·elevation 0–5)을 바로잡았어요. 디자인 작업의 단일 기준 문서를 최신화한 작업이에요(233→414줄).

> 배포 메모: 마크다운 문서만 변경(`DESIGN.md`·`CLAUDE.md`). `tokens.css` 등 코드·빌드·배포·DB·환경 변수 변경 없어요.

## [0.2.40.0] - 2026-06-23

### Added

- **견적 요청을 작성할 때, 꼭 채워야 하는 항목이 한눈에 보여요**: 제목·홈페이지·결제수단·초대할 PG·마감일처럼 보내려면 반드시 필요한 항목 옆에 `필수` 표시가 떠 있다가, 값을 채우면 바로 `입력 완료`로 바뀌어요. 예전에는 '다음'을 눌러야만 무엇이 빠졌는지 알 수 있었는데, 이제 작성하는 중에 바로 확인할 수 있어요. 옆 단계의 완료 체크도 그 단계의 필수 항목을 다 채웠을 때만 켜져요(전에는 결제수단을 비워둬도 체크가 켜지던 문제도 함께 고쳤어요).

### Changed

- **홈페이지 주소가 필수 항목이 됐어요**: 오픈 게시판에서 다른 PG사가 어떤 곳인지 알아볼 수 있도록 홈페이지 주소를 꼭 입력하도록 바꿨어요. 비워두면 '홈페이지 주소를 입력해주세요', 형식이 맞지 않으면 '홈페이지 주소 형식을 확인해주세요'로 안내해요. 실제로 존재하지 않는 주소(예: 가짜 도메인)는 보낼 때 서버가 한 번 더 확인해서, 그 자리에서 바로 고칠 수 있게 알려드려요.

> 배포 메모: 프론트엔드 + 서버 입력 검증 변경이에요 — `next build` 후 `pm2 restart bidit`로 적용돼요. DB 마이그레이션·환경 변수 변경 없어요.

## [0.2.39.0] - 2026-06-22

### Added

- **PG사가 견적 작성 화면을 다시 열면, 쓰던 내용을 묻지 않고 그대로 불러와요**: 예전에는 "이전에 작성 중이던 내용이 있습니다 — 불러오기 / 무시"를 매번 눌러야 했어요. 이제 작성하던 내용이 있으면 자동으로 채워지고, "이전에 작성하던 내용을 그대로 불러왔어요" 토스트로 알려드려요. 처음부터 새로 쓰고 싶으면 사이드바의 '초기화'로 한 번에 비울 수 있어요(확인을 거쳐 정산조건·수수료·올린 견적서까지 모두 비워요). 빈 화면을 잠깐 열어 둔 것만으로 다음에 안내가 뜨던 불편도 함께 고쳤어요.
- **견적 템플릿을 아직 저장한 게 없어도 '템플릿 불러오기'가 항상 보여요**: 예전에는 저장한 템플릿이 있을 때만 1단계에 선택칸이 나타났어요. 이제 0개여도 그 자리에 "자주 쓰는 정산조건·수수료를 템플릿으로 저장하면 다음부터 한 번에 불러올 수 있어요"라는 안내와 '템플릿 관리' 링크가 떠서, 어디서 만들 수 있는지 바로 알 수 있어요. 템플릿을 고르면 불러왔다는 토스트로 확인돼요.

> 배포 메모: 프론트엔드 전용 변경이에요 — `next build` 후 `pm2 restart bidit`로 적용돼요. DB 마이그레이션·환경 변수 변경 없어요.

## [0.2.38.0] - 2026-06-22

### Added

- **아바타를 누르면 상대가 누구인지 바로 볼 수 있어요**: 채팅에서 메시지를 보낸 담당자, 팀원 목록, 멤버 설정 등 사람 아바타를 누르면 이름·사진·온라인 여부·이메일이 작은 카드로 떠요. 상대 회사 담당자라면 카드에서 바로 '메시지 보내기'로 대화를 시작할 수 있어요. 회사(구매사/PG) 아바타 카드에도 지금 온라인인지 표시가 생겼어요.

### Security

- **신원 카드는 아는 상대만 보여줘요**: 사람 카드 정보는 서버에서 관계(본인·같은 팀·대화 중인 거래 상대)를 확인한 뒤에만 내려줘요. 관계가 없으면 이메일은 물론 그 사람이 있는지조차 알려주지 않아요. 운영/시스템 계정은 모든 화면에서 숨기는 규칙대로 신원 카드에도 노출되지 않아요.

> 배포 메모: 프론트엔드 + 서버 액션 변경이에요 — `next build` 후 `pm2 restart bidit`로 적용돼요. DB 마이그레이션·환경 변수 변경 없어요. 실시간 온라인 점은 기존 presence 채널을 그대로 써요(Centrifugo 재시작 불필요).

## [0.2.37.2] - 2026-06-22

### Fixed

- **견적 요청을 만들 때 결제수단을 빠뜨리고 넘어갈 수 없어요**: 견적 요청 작성 2단계에서 결제 받을 결제수단을 하나도 고르지 않으면 '다음'으로 넘어가지 않고, 결제수단 항목에 바로 안내가 떠요. 예전에는 결제수단 없이 마지막 발송 단계까지 갔다가 그제서야 막혔는데, 이제 제목·홈페이지처럼 작성하는 단계에서 바로 알 수 있어요.

> 배포 메모: 프론트엔드 전용 변경이에요 — `next build` 후 `pm2 restart bidit`로 적용돼요. DB 마이그레이션·환경 변수 변경 없어요.

## [0.2.37.1] - 2026-06-22

### Fixed

- **PG사 견적 화면에서 함께 초대된 경쟁 PG사 정보가 새어나갈 수 있던 문제를 막았어요(봉인입찰 보안)**: 구매사가 한 견적 요청에 여러 PG사를 초대하면, 초대받은 PG사가 견적 상세 화면의 전송 데이터(화면엔 안 보이지만 RSC payload에 담겨 있던 값)에서 함께 초대된 경쟁 PG사 목록과 수를 읽어낼 수 있었어요. 게다가 워크스페이스 검색 API(`/api/workspaces/search?type=pg`)가 비로그인 상태에서도 PG사 상호 목록을 돌려줘, 둘을 조합하면 경쟁사 실명까지 알아낼 수 있었어요. 이제 PG사에 보내는 데이터에서 경쟁사 목록·낙찰 입찰·구매사 내부 메타를 서버에서 제거하고, 검색 API도 로그인을 필수로 바꿨어요. 앞으로 새 견적 필드가 실수로 PG사에 노출되지 않도록 컴파일타임 가드도 더했어요.

> 배포 메모: 프론트엔드·서버 로직만 바뀌었어요 — `next build` 후 `pm2 restart bidit`로 적용돼요. DB 마이그레이션·환경 변수 변경 없어요.

## [0.2.37.0] - 2026-06-22

### Added

- **메시지를 보내면 입력한 글이 말풍선으로 떠올라요**: 채팅에서 전송하면 입력창의 글이 그대로 위로 떠올라 말풍선으로 바뀌며 자기 자리에 안착해요(카카오톡 스타일). 상대방 채팅과 팀 채팅 모두에서, 그리고 메시지함과 딜룸 어디서나 동작해요. 내가 보낸 메시지에만 적용되고, 시스템에서 '모션 줄이기'를 켜두었다면 애니메이션 없이 바로 표시돼요.

> 배포 메모: 프론트엔드 전용 변경이에요 — `next build` 후 `pm2 restart bidit`로 적용돼요. DB 마이그레이션·환경 변수 변경 없어요.

## [0.2.36.2] - 2026-06-22

### Fixed

- **마감일 설정 시 KST 기준으로 정확히 마감돼요**: 견적 요청 작성·재요청에서 "6월 30일"을 선택하면 이제 한국시간 6월 30일 23:59:59에 정확히 마감돼요. 이전엔 서버가 KST로 전환된 후에도 UTC 자정 기준으로 저장해서 실제로는 다음날 오전 8:59까지 열려 있었어요.
- **견적 보드 마감 필터도 KST 달력일로 맞췄어요**: '이번 달', '마감 임박', '기간 경과' 필터가 이전엔 서버 현지 시각에 의존해서 말일 견적이 다음 달로 분류되는 경우가 있었어요. 이제 항상 한국시간 달력 기준으로 정확하게 분류해요.
- **D-day 표시도 KST 달력일 기준이에요**: D-day 숫자가 이전엔 ms 시간 차이 계산이라 당일 KST 오후에 "D-1"로 보일 수 있었어요. 이제 한국시간 달력일 차이로 계산해요.

> 배포 메모: 프론트엔드·서버 로직만 바뀌었어요 — `next build` 후 `pm2 restart bidit`로 적용돼요. DB 마이그레이션 없어요. 기존 저장된 마감일(UTC 끝, `T23:59:59Z`)은 그대로 유지해요.

## [0.2.36.1] - 2026-06-22

### Fixed

- **JetBrains Mono 폰트가 이제 숫자에 실제로 적용돼요**: `JetBrainsMonoVariable.ttf`가 앱에 등록되지 않아서 `--font-mono` 토큰을 쓰는 `.md-numeric`(₩ 금액·수수료율·RFP 번호·마감일) 51개 사이트가 전부 Pretendard로 렌더링되던 문제를 수정했어요. `styles/tokens.css`의 `--font-mono` 값도 Pretendard에서 JetBrains Mono로 바로잡았어요. `font-mono tabular-nums`를 직접 쓰던 7개 파일은 `.md-numeric` 클래스로 통일하고, `Footer.tsx` 인라인 `fontFamily` 스타일 9곳은 Tailwind 유틸로 교체했어요. 이메일 인증 템플릿의 OTP 코드 표시도 `MONO_STACK` 상수를 재사용하도록 수정했어요.

## [0.2.36.0] - 2026-06-22

### Changed

- **오픈 게시판 노출은 견적 요청을 처음 낼 때만 선택할 수 있어요**: 게시판 공개 여부는 이제 견적 요청 작성 시(마지막 검토 단계)에만 설정할 수 있고, 이후에는 바꿀 수 없어요. 딜룸 PG 관리 탭과 상세 화면 상단에서 현재 공개 상태를 칩으로 확인할 수 있고, 칩에 마우스를 올리면 상세 설명이 표시돼요.
## [0.2.35.1] - 2026-06-22

### Changed

- 채팅 컴포넌트 내부 정리(리팩터링) — 화면 동작은 그대로예요. 상대방 채팅과 팀 채팅이 서로 복제하고 있던 코드(첨부 파일 칩 목록, 메시지 묶음·날짜 구분선 계산, 입력창 자동 높이 조절, 샘플 안내 문구)를 공용 부품으로 합쳤어요. 눈에 보이는 변화는 없고, 앞으로 채팅 UI를 고칠 때 여러 곳이 아니라 한 곳만 고치면 되도록 정돈한 작업이에요.

> 배포 메모: 화면(프론트엔드)만 바뀌었어요 — `next build` 후 `pm2 restart bidit`로 적용돼요. DB·env 변경 없어요.

## [0.2.35.0] - 2026-06-22

### Changed

- **견적 비교가 우리 회사 영중소 구간을 먼저 보여줘요**: 견적 비교 화면의 구간 셀렉터(영세·중소1~3·일반)가 이제 항상 '일반'으로 시작하지 않고, 구매사 본인의 가맹점 등급에 맞는 구간을 기본으로 골라 그 구간 수수료를 먼저 보여줘요. 등급이 아직 없으면 예전처럼 '일반'으로 시작해요.

### Removed

- 내부적으로 같은 의미였던 두 등급 타입(가맹점 등급 `MerchantGrade`, 견적 수수료 구간 `MerchantTier`)을 하나(`MerchantTier`)로 통합했어요. 영세 식별자가 한쪽은 `small`, 한쪽은 `sole`로 갈라져 있던 걸 `sole`로 일원화하고, 등급 값 목록을 단일 출처(`MERCHANT_TIERS`)에서 가져오도록 정리했어요. 사용자 화면 동작은 그대로예요.

> 배포 메모: **`db:push` 보다 먼저** `docs/migrations/rename-merchant-grade-small-to-sole.sql`(= `ALTER TYPE merchant_grade RENAME VALUE 'small' TO 'sole'`)을 실행해야 해요. `db:push`는 enum 값 rename을 못 해서, 먼저 안 돌리면 push가 부분 실패하고 기존 `small` 등급 row가 고립돼요. 이 ALTER는 제자리 메타데이터 변경이라 기존 row가 자동으로 `sole`로 읽혀요(데이터 재작성·다운타임 없음). 스크립트는 idempotent라 `small`이 이미 없으면 no-op이에요. ⚠️ 별도 레포 `admin-supporter-b`가 `merchant_grade`를 읽으면 동일 변경을 같이 맞춰야 해요.

## [0.2.34.0] - 2026-06-22

### Changed
- 정산주기·배송기간 단위 선택을 D(일)·W(주)·M(개월) 통합 컴포넌트(`DayOffsetInput`)로 일원화. RFP 작성 위저드(step 2), PG 견적 제출 위저드(정산 주기 단계), 견적 템플릿 드로어 모두 동일한 단위 선택 UI를 사용.

## [0.2.33.1] - 2026-06-22

### Changed

- **프로필에서 가맹점 등급은 이제 보기 전용이에요**: 설정 > 프로필에서 가맹점 등급을 직접 고르고 바꾸던 기능을 없앴어요. 등급은 그대로 보이지만, 변경은 견적 요청을 보낼 때 진행돼요. 등급 편집기가 빠진 자리는 워크스페이스 정보를 한 컬럼으로 깔끔하게 정리해 화면이 더 짧고 보기 편해졌어요. (PG 프로필 화면은 그대로예요.)

> 배포 메모: 화면(프론트엔드)만 바뀌었어요 — `next build` 후 `pm2 restart bidit`로 적용돼요. DB·env 변경 없어요. 등급 값 자체와 견적 요청 위저드·필터·표시 동작은 그대로예요. 부수적으로, 예전 버전 머지 과정에서 실수로 커밋됐던 임시 파일(`.merge_file_*` — 버려진 package.json 사본)을 정리하고 재발 방지용 `.gitignore` 가드를 추가했어요.

## [0.2.33.0] - 2026-06-21

### Added

- **상대방이 지금 접속 중인지 한눈에 보여요**: 채팅 상대가 앱을 열어두고 있으면(어느 화면이든) 아바타에 초록 점이 떠요. 인박스 대화 목록, 홈의 최근 메시지, 채팅 상세 헤더에서 모두 확인할 수 있어요. 상대가 앱을 닫으면 점이 사라져요(정상 종료는 즉시, 비정상 종료는 최대 ~1분 안에). 접속 상태는 점으로만 보여주고 "마지막 접속 시각"은 표시하지 않아요.

### Fixed

- **채팅 입력 중 표시(…타이핑)가 다시 작동해요**: 실시간 서버 설정에 클라이언트 publish 권한이 빠져 있어 입력 중 표시가 조용히 안 뜨던 문제를 함께 고쳤어요.

> 배포 메모: 실시간 서버(Centrifugo) 설정에 `presence` 네임스페이스가 추가되고 `chat`에 publish 권한이 켜져요 → **`docker compose up -d centrifugo`로 컨테이너를 재생성**한 뒤 앱을 재배포하세요. DDL·env 변경은 없어요(`db:push` 불필요). ⚠️ presence 네임스페이스는 v6 클라이언트 권한 4종(`allow_subscribe_for_client`·`allow_publish_for_subscriber`·`allow_presence_for_subscriber`·`allow_history_for_subscriber`)이 모두 있어야 점이 떠요(하나라도 빠지면 무에러로 점이 안 보여요) — `deploy/__tests__` 드리프트 가드가 config에 4종 키 존재를 검증해요. 정적 가드는 "키가 있다"까지만 보장하므로, **배포 직후 실제로 점이 뜨는지 1회 육안 확인**하세요(Centrifugo가 키를 실제로 적용하는지는 정적 검사로 알 수 없어요). 또한 배포 재시작 시 전 탭이 동시에 재접속하므로, connection-token 라우트는 동시 발급이 상한(`CENTRIFUGO_TOKEN_MAX_INFLIGHT`, 기본 25, 선택)을 넘으면 503 + 지터된 Retry-After로 흘려보내 Postgres 풀을 보호해요(클라가 자동 분산 재시도). 필수 env 추가는 없어요.
## [0.2.32.0] - 2026-06-21

### Fixed

- **로그인했는데 화면이 계속 튕기면 자동으로 로그아웃돼 다시 로그인할 수 있어요**: 드물게 예전 로그인 정보(쿠키)가 깔끔히 지워지지 않으면 화면이 `홈 ↔ 로그인` 사이를 무한히 오가며 어디에도 못 들어가는 일이 있었어요. 이제 로그아웃이 여러 번 반복되면(루프 감지) ① 브라우저가 실제로 들고 있는 모든 로그인 쿠키 변종을 찾아 확실히 지우고, ② 그래도 안 풀리면 로그인 화면으로 보내 거기서 멈추도록 해 무한 반복을 끊어요. 로그인 화면에는 "로그인 정보가 만료되어 다시 로그인해 주세요" 안내가 떠요. 다시 로그인하면 새 로그인 정보로 교체돼 정상으로 돌아와요. 운영에서 버전을 올린 직후(재배포) 이 증상이 생겨도 스스로 복구돼요.

> 배포 메모: 코드 변경뿐이라 `next build` 후 `pm2 restart bidit`로 적용돼요. DDL·env 변경 없어요. 미들웨어(proxy) 변경이 포함돼 빌드가 필요해요.

## [0.2.30.0] - 2026-06-21

### Changed

- **회사(워크스페이스) 로고를 바꾸면 모든 화면에 즉시 반영돼요**: 로고 주소에 버전을 함께 담아(`?v=…`) 브라우저가 옛 로고를 계속 들고 있지 않도록 했어요. 기존에는 변경·삭제 후 최대 1시간 동안 옛 로고가 보일 수 있었어요. 로고는 지금처럼 공개로 유지돼요(오픈 게시판 노출). 사용자 프로필 사진(v0.2.29.0)과 동일한 캐시 버스트 방식이에요.

> 배포 메모: 추가형 스키마 변경이라 `pnpm db:push`로 안전하게 적용돼요(`workspaces.logo_updated_at` 컬럼 — nullable, 기본값 없음). **push 직후·코드 배포 전에 `pnpm backfill:logo-updated-at`를 1회 실행하세요** — 안 하면 사이드바·PG 가입 회사 선택에서 기존 로고가 잠시 이니셜로 보여요(설정/프로필·상대방 카드는 영향 없음). `has_logo` 컬럼은 이번엔 그대로 두고(미사용) 후속 PR에서 제거해요.
## [0.2.29.1] - 2026-06-21

### Fixed

- **운영 로그가 Axiom으로 정상 전송돼요(내부 관측성)**: 프로덕션(Turbopack 번들)에서 `import.meta.url`이 숫자 모듈 ID로 치환돼 `@axiomhq/pino` transport 해석이 매번 실패하고 stdout으로만 떨어지던 문제를 고쳤어요. 앵커가 문자열이 아니면 앱 루트(`process.cwd()`)로 폴백해 transport가 정상적으로 붙어요. 사용자 화면에는 변화가 없고, 코드만 변경돼요(DDL·env 없음).

> 배포 메모: 코드 변경뿐이라 `next build` 후 `pm2 restart bidit`로 적용돼요. 적용 후 `pm2 logs bidit`에 `[logger] @axiomhq/pino transport unavailable` 경고가 더는 안 뜨면 정상이에요.

## [0.2.29.0] - 2026-06-21

### Added

- **내 프로필 사진을 직접 올리고 바꿀 수 있어요**: 설정 > 프로필에서 PNG·JPEG 사진(5MB 이하)을 올리거나 삭제할 수 있어요. 올린 사진은 헤더·사이드바의 내 메뉴, 팀 멤버 목록, @멘션 자동완성, 상대방·팀 채팅 메시지까지 앱 곳곳에 바로 반영돼요. 사진을 올리지 않으면 지금처럼 이름 이니셜이 보여요.
- **사진을 바꾸면 모든 화면에 즉시 반영돼요**: 사진 버전을 주소에 함께 담아(`?v=…`) 브라우저가 옛 사진을 계속 보여주지 않도록 했어요. 변경·삭제 직후 새로고침 없이 최신 상태가 보여요.

> 배포 메모: 추가형 스키마 변경이라 `pnpm db:push`로 안전하게 적용돼요(`user_avatar_blobs` 테이블 + `users.avatar_updated_at` 컬럼 — nullable, 기본값 없음). 백필·env 변경은 없어요. 기존 사용자는 사진이 없는 상태(이니셜)로 시작해요.

## [0.2.28.1] - 2026-06-20

### Fixed

- **견적 요청 작성창을 열었을 때 예전에 고른 PG사가 지금은 선택 불가 상태여도 자동으로 정리돼요**: 저장된 임시저장 데이터를 화면 진입 시점에 서버 목록과 대조해, 현재 참여할 수 없는 PG사를 즉시 제거하고 안내 메시지를 보여드려요.
- **마감일이 이미 지났으면 초기화해드려요**: 브라우저에 저장된 마감일이 현재 시각보다 이전이면 자동으로 비워서 과거 날짜로 견적이 발송되는 실수를 막아요.
- **24시간이 지나 서버에서 삭제된 첨부파일은 목록에서 제거돼요**: 임시저장된 첨부파일 ID를 서버에서 검증해, 이미 정리된 파일은 목록에서 빼고 안내 메시지를 보여드려요.

## [0.2.27.13] - 2026-06-20

### Fixed

- **사이드바가 접힌 상태에서 알림 배지가 있어도 벨 아이콘이 정확히 중앙에 정렬돼요**: 기존에는 배지 래퍼 span이 flex 레이아웃에 영향을 줘서 `gap` 때문에 아이콘이 왼쪽으로 밀렸어요. 이제 배지를 아이콘 래퍼 안에 겹쳐 렌더링하고 행 배지 래퍼를 `display:none`으로 완전히 제거해 아이콘이 항상 정중앙에 위치해요.
## [0.2.28.0] - 2026-06-20

### Security

- **Next.js 보안 패치 적용 (16.2.4 → 16.2.9)**: Middleware/Proxy 우회, SSRF, DoS 등 고위험 취약점을 포함한 12개 보안 권고사항을 해소했어요. `eslint-config-next`도 동일 버전으로 맞췄어요.

## [0.2.27.12] - 2026-06-20

### Added

- **검색창(⌘K)에서 설정과 로그아웃에 바로 접근할 수 있어요**: 팔레트 하단 '계정' 그룹에 설정·로그아웃이 항상 표시되고, 이름이나 초성(예: ㅅㅈ → 설정)으로 검색할 수 있어요. 로그아웃은 실수로 눌리는 걸 막기 위해 팔레트 내에서 한 번 더 확인한 뒤 실행돼요.

## [0.2.27.11] - 2026-06-20

### Added

- **접속 중에 새 알림이 오면 화면 우하단에 toast로 바로 알려드려요**: 알림 페이지를 보고 있지 않아도, 실시간으로 도착한 새 알림의 제목이 toast로 떠서 놓치지 않아요. 사이드바 미읽음 배지도 함께 올라가요.

### Changed

- **알림 toast가 한꺼번에 몰려도 차분하게 보여드려요**: 짧은 시간에 알림이 여러 건 도착해도 toast가 줄줄이 쌓이지 않도록 묶어서 보여주고, 알림 제목이 길어도 toast 폭이 늘어나지 않게 정리했어요. 이미 알림 목록을 보고 있을 때는 같은 알림을 toast로 다시 띄우지 않아요.

## [0.2.27.10] - 2026-06-20

### Fixed

- **채팅 입력창 placeholder 텍스트가 수직으로 정렬되지 않던 문제를 고쳤어요**: 상대방 채팅, 팀 채팅 입력창에서 "메시지를 입력하세요…" placeholder가 입력창 상단에 치우쳐 보이던 문제를 고쳤어요. 패딩을 조정해 placeholder와 입력 텍스트가 입력창 안에서 수직 중앙 정렬되어 표시돼요.

## [0.2.27.9] - 2026-06-20

### Fixed

- **재요청(requote) 후 선정 시 중복 알림이 오던 문제를 고쳤어요**: 구매사가 특정 PG사에 재요청을 보내고 해당 PG사를 최종 선정했을 때, 같은 PG사에 '선정됐어요'와 '이번엔 선정되지 않았어요' 알림이 동시에 도착하던 문제를 고쳤어요. 원인은 재요청 시 이전 라운드 제안의 상태가 변경되지 않아 낙찰 처리 시 이전 라운드가 '탈락 제안'으로 잘못 분류된 것이었어요. 이제 선정 처리 시 제안 ID가 아닌 워크스페이스 단위로 중복을 제거해, 재요청을 거친 탈락 PG사도 알림이 1건만 전송돼요.

## [0.2.27.8] - 2026-06-20

### Fixed

- **담당자 합류 거부 시 '계정 이용 정지' 화면이 잘못 표시되던 문제를 고쳤어요**: 관리자가 담당자 계정 합류 요청을 거부했을 때, '계정 이용 정지' 안내 화면으로 이동하던 문제를 고쳤어요. 이제 '합류 요청이 거부됐어요' 안내와 로그아웃 버튼이 바로 표시돼요(기존에는 10초를 기다려야 거부 화면으로 전환됐어요).
- **가입 이메일 중복 확인 시 운영자 계정 이메일 노출 문제를 고쳤어요**: 가입 1단계 이메일 중복 확인에서 운영자(마스터) 계정 이메일을 입력하면 `MASTER_EMAIL` 오류 코드가 반환돼 비로그인 상태에서도 운영자 이메일 목록을 유추할 수 있었어요. 이제 일반 중복 이메일과 동일한 오류 코드(`EMAIL_TAKEN`)를 반환해요.

## [0.2.27.7] - 2026-06-20

### Fixed

- **실시간 채팅이 전혀 연결되지 않던 문제를 고쳤어요(운영 설정)**: 채팅 구독을 보호하는 비밀 헤더 설정이 Centrifugo v6에서는 인식되지 않는 옛 형식(`static_http_headers`)으로 적혀 있어, 헤더가 전송되지 않고 구매사·PG사 양쪽 모두 모든 채팅 구독이 거부되던 문제를 고쳤어요(메시지가 새로고침해야만 보였어요). v6 형식(`channel.proxy.subscribe.http.static_headers`)으로 바꾸고, 비밀값은 `.env.production`의 `CENTRIFUGO_PROXY_SECRET` 한 곳에서만 관리하도록 docker-compose가 `CENTRIFUGO_VAR_PROXY_SECRET`로 자동 연결하게 했어요(v6.3.0+ 환경변수 주입). 한쪽만 설정돼 채팅이 멈추는 실수를 막는 드리프트 가드 테스트도 추가했어요. (운영 배포: `docker compose -f docker-compose.prod.yml up -d centrifugo`로 컨테이너 재생성 후 `pm2 restart bidit`.)

## [0.2.27.6] - 2026-06-20

### Fixed

- **PG사 화면에서 실시간 채팅이 연결되지 않던 문제를 고쳤어요**: PG사(파트너) 화면(`partner.supporter-b.com`)에서 채팅 실시간 연결(WebSocket)이 열리지 않아 메시지가 즉시 오가지 않던 문제를 고쳤어요. 두 도메인(구매사·PG사)이 같은 실시간 서버에 붙는데, 허용 도메인 목록에 PG사 주소가 빠져 있어 연결이 막혔던 거예요. 이제 PG사 주소도 허용 목록에 포함돼 양쪽 모두 실시간 채팅이 정상 작동해요. (배포 시 Centrifugo 컨테이너 재시작이 필요합니다.)

## [0.2.27.5] - 2026-06-20

### Fixed

- **운영자 계정 이메일로는 가입할 수 없도록 막고 안내해요**: 운영자(마스터) 계정 이메일로 PG사·구매사 가입을 시도하면 계정·심사 신청만 만들어진 채 마지막 자동 로그인 단계에서 "가입을 완료하지 못했어요"가 떠 다음으로 넘어가지 못하던 문제를 고쳤어요. 운영자 계정은 설계상 비밀번호 로그인이 막혀 있어 가입을 끝까지 진행해도 로그인할 수 없었거든요. 이제 가입 첫 단계(이메일 입력)와 마지막 단계에서 "이 이메일로는 가입할 수 없어요. 다른 이메일을 사용해 주세요." 안내가 바로 표시되고, 계정이 잘못 만들어지지 않아요. (운영자 계정은 기존처럼 Google 로그인 전용입니다.)

## [0.2.27.4] - 2026-06-20

### Fixed

- **폐업·휴업 사업자로 PG사 가입이 되던 문제를 서버에서도 막았어요**: 기존에는 화면에서만 폐업·휴업 사업자 가입을 차단했는데, 서버에서도 동일하게 검증하도록 추가했어요. 이제 API를 직접 호출하거나 우회해도 폐업·휴업 상태의 사업자 번호로는 가입할 수 없어요.
- **PG사 가입 완료 후 화면 전환이 멈추던 문제를 고쳤어요**: 워크스페이스 초대 경로로 PG사에 가입할 때, 가입 완료 후 화면이 홈으로 넘어가지 않는 경우가 있었어요. Next.js 컴파일 타이밍 문제를 피하기 위해 모든 경로에서 전체 페이지 이동 방식으로 통일했어요.

## [0.2.27.3] - 2026-06-20

### Fixed

- **PG사 가입이 마지막 단계에서 멈추던 문제를 해결했어요**: PG(파트너) 호스트에서 가입을 완료할 때 자동 로그인이 실패하며 "가입을 완료하지 못했어요"가 떠 다음 화면으로 넘어가지 못하던 문제를 고쳤어요. 가입 직후 자동 로그인을 로그인과 동일한 서버 측 경로로 처리하도록 바꿔, 호스트가 달라도 정상적으로 로그인되고 로그인의 무차별 대입 방지(레이트리밋)도 그대로 적용돼요.

## [0.2.27.2] - 2026-06-20

### Changed

- **폐업·휴업 사업자는 구매사 가입을 할 수 없어요**: 사업자등록번호 조회 결과가 폐업 또는 휴업 상태인 경우, 조회 결과 패널에 상태를 강조 표시하고 오류 메시지를 안내해요. 정상 영업 중인 사업자번호를 입력해야 다음 단계로 진행할 수 있어요.

## [0.2.27.1] - 2026-06-18

### Changed

- **PG사 브랜드 로고를 워크스페이스 프로필 사진으로 전환**: PG사 브랜드 로고가 이제 정적 이미지 파일이 아닌 워크스페이스 프로필 사진(DB blob)으로 관리돼요. PG 가입 2단계 선택 버튼은 물론, 사이드바 워크스페이스 스위처·메시지 상대방 카드 등 PG사가 표시되는 모든 곳에서 로고가 일관되게 노출돼요. 배포 후 `pnpm backfill:pg-logos`를 1회 실행해 로고를 DB에 주입하세요(멱등).

### Removed

- **불필요한 정적 이미지 파일 삭제**: `public/` 디렉터리 하위의 미사용 Next.js 기본 이미지(`file.svg`, `globe.svg`, `next.svg`, `window.svg`) 및 PG사 로고 원본 파일을 삭제했어요. 폰트 파일은 유지돼요.

## [0.2.27.0] - 2026-06-18

### Added

- **PG사 선택 버튼에 공식 브랜드 로고 표시**: PG 가입 2단계(소속 PG사 선택) 화면에서 각 PG사 버튼에 해당 PG사의 공식 로고가 함께 표시돼요. KG이니시스·KICC(이지페이)·NHN KCP·나이스페이먼츠·다날·토스페이먼츠·헥토파이낸셜 7개 PG사가 지원되며, 이미지 로드 실패 시에는 회사명 텍스트만 그대로 표시돼요.

## [0.2.26.4] - 2026-06-18

### Fixed

- **가입 1단계 이메일 blur 중복 검사**: 이메일 입력 후 다른 필드로 이동(blur)하면 즉시 중복 여부를 확인해요. 기존에는 비밀번호·약관까지 채운 뒤 "다음"을 눌러야만 확인할 수 있었어요. 이미 가입된 이메일을 입력하면 바로 "이미 가입된 이메일입니다. 로그인하시겠어요?" 안내가 뜨고, 해당 상태에서는 "다음" 버튼도 비활성화돼요. 구매사·PG사 가입 1단계 모두 적용돼요(PG 초대 경로는 이메일이 고정이라 blur 검사 생략).

## [0.2.26.3] - 2026-06-18

### Changed

- **Centrifugo 설정 파일을 YAML로 전환**: `deploy/centrifugo/config.json` → `config.yaml`. JSON은 주석을 지원하지 않아 운영 설정 안내를 인라인으로 남길 수 없었는데, YAML 전환으로 `#` 주석을 직접 파일에 담을 수 있어요. `CENTRIFUGO_PROXY_SECRET` 활성화 방법·주의사항이 `config.yaml`에 주석으로 포함됐어요. `docker-compose.prod.yml`·`docs/DEPLOY_LIGHTSAIL.md`·`deploy/centrifugo/README.md`의 참조도 함께 업데이트했어요.
- **`chat_conversation_reads` FK 제약조건명 수정**: Postgres 63바이트 한도를 초과하는 자동 생성 이름을 짧은 명시 이름(`ccr_conversation_id_fk`, `ccr_user_id_fk`)으로 교체했어요.

> 배포 메모: `pnpm db:push`를 실행해 FK 제약조건명을 갱신하세요(`DROP CONSTRAINT` → `ADD CONSTRAINT`, 데이터 손실 없음).

## [0.2.26.2] - 2026-06-18

### Changed

- **견적 현재조건을 문서(`current_terms`) 단독 저장으로 정리(클린 컷오버)**: 현재조건 브리프의 개별 컬럼 8개와 `current_fee_visible_to_pg` 컬럼을 제거하고, 버전드 JSONB 문서와 `hidden_from_pg` 목록만 저장소로 남겼어요. 작성·조회·비교 등 사용자 화면 동작은 그대로예요(서버가 flat 입력을 문서로 조립하고, 읽을 때 다시 펼쳐 보여줘요). 점진 마이그레이션용 백필 스크립트·코드는 모두 정리했어요. 봉인입찰 경계(PG는 숨긴 항목을 못 본다)는 그대로 유지돼요.

> 배포 메모: 이 변경은 **운영 데이터를 초기화(wipe)하는 배포**를 전제로 해요(파괴적 컬럼 DROP). DB 초기화 → `db:push`(컬럼 제거 반영) → 코드 배포 순으로 진행하면 돼요. 초기화하므로 백필은 필요 없어요. (데이터를 보존해야 한다면 컬럼 DROP 전 백필 실행이 선행돼야 했지만, 이번엔 초기화 전제예요.)

## [0.2.26.1] - 2026-06-18

### Changed

- **견적 현재조건의 읽기·공개 권위를 문서로 완전 전환(Phase E)**: 현재조건은 이제 `current_terms` 문서에서만 읽고(개별 컬럼 폴백 제거), PG 공개여부는 `hidden_from_pg` 목록으로만 판정해요(레거시 boolean 폴백 제거). 사용자 화면 동작은 그대로예요. 봉인입찰 경계(PG는 숨긴 항목을 못 본다)는 그대로 지켜지고, 값을 쓰는 유일 경로가 문서와 숨김목록을 한 번에 기록하므로 어긋날 틈이 없어요.

> 배포 메모: 이 변경은 **백필이 끝난 뒤** 배포하세요(`pnpm backfill:current-terms`). 백필 전에 배포하면 아직 문서가 비어 있는 기존 견적 요청의 현재조건이 구매사 본인 화면에서 비어 보일 수 있어요(누출은 아니에요 — 안 읽는 값은 새지 않아요). 개별 컬럼은 롤백 안전을 위해 여전히 같이 기록되며, 컬럼 쓰기 중단·제거는 Phase F에서 진행해요.

## [0.2.26.0] - 2026-06-18

### Added

- **견적 현재조건을 확장 가능한 문서로 일반화한 기반(Phase A~D)**: 구매사 견적 요청의 '현재 조건' 정보(수수료율·정산한도·보증보험·정산주기·배송주기·솔루션·연거래량)를 개별 컬럼에서 버전드 JSONB 문서(`current_terms`)로 모으는 토대를 깔았어요. 앞으로 현재조건 항목을 추가할 때 DB 마이그레이션 없이 타입·검증 두 곳만 고치면 돼요. 검증된 `bids.paymentFees`(우대수수료 구간) 패턴을 그대로 일반화했어요.
- **PG별 항목 공개여부의 일반화(`hidden_from_pg`)**: '현재 카드 수수료만 숨김' 같은 항목별 비공개를 boolean 컬럼 하나가 아니라 숨김 경로 목록으로 다루도록 바꿔, 앞으로 숨길 수 있는 항목을 늘려도 컬럼 추가가 필요 없어요. 어떤 항목을 숨기든 서버에서 PG 응답에서 값을 지우고, 숨김 정책 메타데이터 자체도 PG에게 노출하지 않아요.
- **견적(bid) 미래 차원 슬롯(`quote_terms`)**: 정산일 옵션·롤링 리저브 같은 비수수료 견적 차원을 나중에 DB 변경 없이 담을 빈 문서 슬롯을 추가했어요.
- **현재조건 백필 스크립트**: 기존 견적 요청의 개별 컬럼을 새 문서로 채우는 멱등·재실행 안전 스크립트(`pnpm backfill:current-terms`)를 추가했어요.

### Changed

- **견적 현재조건 읽기 권위를 문서로 전환**: 상세·비교 화면이 현재조건을 새 문서에서 읽되, 아직 백필되지 않은 기존 데이터는 개별 컬럼으로 자동 폴백해요. 사용자에게 보이는 동작은 그대로예요(동작 보존). 봉인입찰 경계(PG는 경쟁사 정보를 못 본다)는 오픈 게시판 노출 항목과 PG 응답 모두에서 그대로 지켜져요.

> 배포 메모: 추가형 스키마 변경(`current_terms`·`hidden_from_pg`·`quote_terms` 컬럼 — 모두 기본값 있음)이라 `db:push`로 안전하게 적용돼요. 읽기 전환 배포 전에 `pnpm backfill:current-terms`를 1회 실행하세요. 개별 컬럼 쓰기 중단·제거(Phase E·F)와 작성 위저드의 문서화는 후속 PR로 진행해요.

## [0.2.25.5] - 2026-06-17

### Removed

- **rfp_bids 칸반 보드 완전 제거**: PR#97(견적 비교 재설계)에서 딜룸 UI로 대체된 후 어떤 라우트에도 마운트되지 않던 rfp_bids 보드 관련 코드를 전면 제거했어요. `BidCard` 컴포넌트, `CardType 'bid'`, `ColumnKind 'rfp_bids'`, `BidRepo.setBoardColumn`, 관련 시드·테스트가 모두 삭제됐어요. 배포 시 별도 DB 마이그레이션이 필요해요(PR 본문 참조).

## [0.2.25.4] - 2026-06-17

### Changed

- **칸반 보드 렌더링 최적화**: 컬럼 메뉴 열기·router.refresh 같은 보드 내부 상태 변경 시, 관련 없는 컬럼의 카드가 불필요하게 다시 그려지지 않아요. 컬럼·카드 컴포넌트가 `React.memo`를 제대로 활용하게 됐어요.

## [0.2.25.3] - 2026-06-17

### Fixed

- **재요청 PG 인박스 견적 표시 안정화**: 재요청(round 2 이상)이 존재할 때 인박스·칸반·홈 대시보드에서 항상 최신 라운드 견적이 올바르게 표시돼요. 이전에는 데이터베이스가 견적을 반환하는 순서에 따라 오래된 라운드 견적이 보일 수 있었어요.

## [0.2.25.2] - 2026-06-17

### Changed

- **채팅·팀 메시지 다이제스트 이메일 배치 발송**: 마감 시간 윈도가 동시에 끝나는 수신자가 많을 때도 이메일이 더 안정적으로 나가요. 기존엔 수신자마다 개별 API 호출이었는데, 이제 한 번에 묶어(최대 100통/요청) Resend 로 전송해요. 발송 실패 시 재시도·영구실패 분류 동작은 그대로예요.

### Fixed

- **이메일 중복 발송 방지 키 개선**: 서버가 재시작되거나 발송 직후 오류가 생겨 다이제스트가 재처리될 때, 본문이 달라진 경우(그 사이 새 메시지가 도착한 경우)에도 이메일이 영구 실패로 잘못 처리되던 문제를 수정했어요. 이제 중복 방지 키가 발송 내용도 함께 반영해 재처리가 필요한 경우 올바르게 새 이메일을 전송해요.
- **PG 인박스 응답 속도 개선**: 보드 뷰(`칸반` 탭) 진입 시 동일한 데이터를 두 번 불러오던 중복 요청을 제거했어요. 이제 견적 요청 3개 쿼리가 한 번만 실행돼 응답이 더 빠르게 느껴져요.

## [0.2.25.1] - 2026-06-17

### Fixed

- **칸반 키보드 드래그 핸들**: 카드마다 전용 드래그 핸들(GripVertical)이 생겼어요. 핸들에 포커스한 뒤 Space 또는 Enter 를 눌러 카드를 집고, 화살표 키로 다른 컬럼으로 이동할 수 있어요. 카드를 누르면 상세가 열리는 동작은 영향 없어요.

## [0.2.25.0] - 2026-06-17

### Fixed

- **Auth / Signup 버그 5건 일괄 수정**: 가입 플로우 전반의 안전성·신뢰성 개선이에요.
  - **[정합성 P2] 고아 user 행 방지**: `completeSignup` 의 `wsName` 누락 검사를 `transaction()` 호출 **앞**으로 끌어올렸어요. 기존엔 INSERT 후 `{ok:false}` 반환 시 postgres-js 가 tx 를 commit 해 워크스페이스 없는 미인증 user 행이 생길 수 있었어요. `signupViaInvite` 의 초대 claim 실패도 동일 문제 — throw-to-rollback 패턴으로 tx 를 확실히 롤백해요.
  - **[테스트 P2] invite·canonical 가입 통합 테스트 추가**: `signupViaInvite`(초대 성공·PHONE_NOT_VERIFIED·INVITE_INVALID·INVITE_EXPIRED·INVITE_EMAIL_MISMATCH·EMAIL_TAKEN) 6개, `joinCanonicalPgWorkspace`(성공·PHONE_NOT_VERIFIED·INVALID_CANONICAL_WORKSPACE·EMAIL_TAKEN) 4개, throw-to-rollback 회귀 1개 — 총 11개 테스트로 두 가입 경로를 박제했어요.
  - **[UX P3] OTP 입력 칸 Enter 키 → 폼 조기 제출 방지**: 인증번호 입력 후 Enter 를 누르면 폼이 제출되던 버그를 수정했어요. 이제 6자리가 모두 입력되어 있으면 Enter 가 `handleVerify` 를 호출하고, 부족하면 아무 일도 하지 않아요(폼 제출도 없음).
  - **[보안 P3] ApprovalWaitingScreen cross-host 내비 수정**: 승인 폴링 성공 시 `router.push('/home')` 대신 `window.location.assign('/home')` 으로 통일했어요. `EmailVerifyScreen` 과 같은 패턴이에요(v0.2.19.0 선례). 다른 호스트에서 `/pending-approval` 에 착지한 유저의 CORS 잠재 경로를 닫아요.
  - **[UX P3] 사파리 비공개 모드 sessionStorage 차단 안내**: `isSignupStorageAvailable()` 헬퍼로 sessionStorage 를 사전 감지해요. 차단 시(사파리 비공개 등) 프로필 단계에서 "브라우저 설정을 확인해주세요" 메시지와 함께 진행을 막아 기존의 '첫 가입 화면으로 무한 redirect' 를 방지해요.

### Changed

- **AUTH: unique-violation → EMAIL_TAKEN 매핑 DRY + 컨스트레인트 특정화**: `AuthService` 의 `users.email` unique violation → `EMAIL_TAKEN` 매핑 코드(4곳 중복)를 `mapUniqueViolationToEmailTaken<T>` 단일 헬퍼로 추출했어요. 아울러 `users_email_unique` 컨스트레인트에만 적용되도록 좁혀, 다른 테이블·컬럼의 23505 오류(예: `workspaces.canonical_pg_key`)가 `EMAIL_TAKEN` 으로 오진단되지 않아요. 동작 동일, 3470 green.

## [0.2.24.2] - 2026-06-17

### Fixed

- **견적 제안 수수료 툴팁 양끝 오버플로 + 접근성**: BidStepFees 의 수수료 입력 표에서 첫 열·마지막 열 툴팁이 테이블 밖으로 잘리는 문제를 고쳤어요. `tooltipAlign` prop(`start`|`center`|`end`)으로 위치를 제어하고, `useId` + `aria-describedby` 로 힌트 텍스트와 입력 필드를 스크린리더가 연결할 수 있게 했어요.
- **종결·선정 컬럼 최신 전이 시각 기준 정렬**: 구매사 칸반의 종결·선정 컬럼과 PG 칸반의 낙찰·미선정 컬럼이 가장 최근에 상태가 바뀐 견적이 먼저 오도록 정렬해요. 기존에는 생성 시각 기준이었어요. 아울러 `rfps.updated_at` 이 전이 시 실제로 갱신되지 않던 버그를 함께 수정했어요 — `transition()` 이 SET 에 `updated_at = now()` 를 포함하도록 고쳤어요.

## [0.2.23.1] - 2026-06-16

### Changed

- **내부 구조 정리 — 실시간 채팅 구독 로직 공용화 (개발자 전용, 사용자 화면·동작 변화 없음)**: 컴포넌트 리팩토링 마지막 항목(D10). 상대방 채팅(`useChatChannel`)과 팀 채팅(`useTeamChannel`)이 똑같이 들고 있던 Centrifugo 채널 구독 생명주기 — 연결·구독·수신 라우팅·연결상태 추적·정리(특히 재마운트 시 핸들러 중복 등록을 막는 `removeSubscription` 처리) — 를 공용 훅 `useCentrifugoSubscription` 으로 모았어요. 채널별 고유 동작(상대방 채팅의 타이핑 표시·온라인 프레즌스·타이핑 전송)은 그대로 해당 훅에 남겨 두고, 두 훅이 공유하던 연결/구독 보일러플레이트만 공유해요. 알림 스트림은 다른 전송 방식(SSE/EventSource, 단일 소비처)이라 의도적으로 합치지 않았어요(YAGNI). 동작·실시간 신호는 그대로(채팅 채널 훅 27개 + 소비 컴포넌트 107개 테스트 통과), 단위 테스트 8개 추가, 전체 3358개 통과.
## [0.2.24.1] - 2026-06-16

### Fixed

- **채팅 보안·신뢰성 일괄 보강 (개발자 전용 — 사용자 체감 변화 없음)**: 실시간 채팅 도메인의 보안·정합성·신뢰성 이슈 6건을 한꺼번에 해소했어요.
  - **[보안 P1] Centrifugo 구독 프록시 비밀 헤더**: `/api/centrifugo/subscribe` POST 엔드포인트에 `CENTRIFUGO_PROXY_SECRET` 환경 변수 기반 공유 비밀 헤더 검증을 추가했어요. `Buffer.alloc` + `timingSafeEqual` 로 상수시간 비교해 타이밍 오라클을 차단해요. 환경 변수 미설정 시 기존처럼 동작해요(하위 호환). 배포 시 Centrifugo 의 `subscribe_proxy` 정적 헤더(`X-Centrifugo-Proxy-Secret`)와 값을 맞춰야 해요.
  - **[보안 P1] rfpId 태그 접근권 검증**: 채팅 메시지 전송 시 클라이언트가 제공한 `rfpId` 를 무검증 영속하던 문제를 수정했어요. 이제 `canWorkspaceAccessRfp` 헬퍼로 buyer-소유/PG-초대 여부를 확인하고, 접근 불가 RFP 태그는 조용히 드롭(메시지는 정상 전송)해요. 교차 테넌트 UUID 오염·존재 오라클을 차단해요.
  - **[신뢰성 P2] self-echo tempId 상관 관계**: 라이브 self-echo 매칭이 "첫 번째 pending 말풍선" 기준이라 동일 유저가 두 탭에서 동시 전송 시 echo 가 다른 탭 pending 을 오인 승격하던 문제를 해결했어요. 클라이언트가 생성한 `tempId` 를 액션 → Centrifugo 브로드캐스트 → `applyLiveEcho` 까지 왕복시켜 정확 매칭해요.
  - **[신뢰성 P2] 대화 스레드 캐시 unmount 무효화**: `ThreadPane` 이 unmount 될 때 `invalidateThread` 를 호출하도록 수정했어요. 이전엔 소프트 재방문 시 스테일 스냅샷이 재생됐어요.
  - **[정합성 P2] RecipientCard `rfpContext` 타입 분리 — UUID 노출 제거**: `RfpContext` 에 전송용 `id`(UUID) 를 별도 필드로 분리했어요. 이전엔 `code` 필드가 UUID 와 사람용 RFP 코드를 이중 사용해 `RecipientCard` 에 UUID 가 그대로 표시됐어요.
  - **공유 헬퍼 `canWorkspaceAccessRfp` 추출**: `TeamChatService.authorize` 와 subscribe 라우트에 중복됐던 RFP 접근 규칙을 `lib/server/rfp-access.ts` 로 단일화했어요.

## [0.2.24.0] - 2026-06-16

### Changed

- **메시지 화면 3단 레이아웃 + 검색 + RFP 컨텍스트 패널 (xl 이상)**: 메시지(`/messages`) 화면을 대화폭 데스크톱에서 더 쓰기 좋게 개편했어요. 1280px 이상(xl) 화면에서 ① 대화 목록 패널 → ② 스레드 패널 → ③ RFP 컨텍스트 패널 순서의 3단 레이아웃이 펼쳐져요. xl 미만에서는 스레드 패널 내부가 채팅·RFP·파일 탭으로 전환돼 같은 정보를 탭 형태로 볼 수 있어요. 대화 목록 상단엔 검색 입력창을 추가했어요 — 상대방 이름·견적 코드·미리보기 텍스트를 실시간으로 필터링해요. 상대방 대화 행에 연결 견적 코드 칩(RFP 코드 + 제목)을 노출하고, 마지막 메시지 시각을 오늘이면 시각(오전/오후 H:MM), 이번 주면 요일, 그 이전이면 M/D 날짜로 상대적으로 표시해요. 전체·상대방·팀 필터 탭은 그대로예요.
## [0.2.23.3] - 2026-06-16

### Changed

- **README 전면 재작성 — 아키텍처 중심 문서로 개편 (개발자 문서 전용, 제품 변화 없음)**: 로컬 세팅 가이드를 제거하고 프로젝트 아키텍처를 한눈에 파악할 수 있는 문서로 재작성했어요. 시스템 아키텍처(Caddy → Next.js/Centrifugo → PostgreSQL), 서버 레이어(Actions → Services → Repositories), Transactional Outbox Pattern, 봉인 입찰 보안 경계, Repository Boundary 린트 강제, 테스트 전략(PGlite 기반 TDD)을 Mermaid 다이어그램과 함께 정리했어요.

## [0.2.23.2] - 2026-06-16

### Changed

- **문서 정리 — SCREEN_DESIGN.md 를 현행 딜룸 구조에 맞게 동기화 (개발자 문서 전용, 제품 변화 없음)**: Phase 4 딜룸 전면 개편 때 사라진 컴포넌트·스토어 참조가 SCREEN_DESIGN.md 에 남아 있던 걸 현행 코드 기준으로 고쳤어요. 삭제된 `RfpDetailContent`·`ChatRail`·`ChatRailToggle`·전역 `useChatRailStore`·`StatutoryCardFeeNotice` 참조를 제거하고, 현행 딜룸 구조(`DealRoomModal` `@modal` 인터셉트 + 정식 페이지 `DealRoomFull`/`DealRoomShell`, `BuyerDealRoomBody`/`PgDealRoomBody`, `DealRoomCenter`/`DealRoomActionRail`, `DealRoomChat`→`ChatPanel`, lg 미만 `DealRoomChatFab` 하단 시트, 상대 출처는 딜룸 스코프 `DealRoomContext`)로 B4·P3 화면 행·라우트 맵·채팅 다이어그램을 갱신했어요. 코드 변경 없음 — 문서만.

## [0.2.23.0] - 2026-06-16

### Fixed

- **워크스페이스를 바꿔도 이전 워크스페이스 알림이 남던 문제**: 여러 워크스페이스를 쓰는 사용자가 같은 호스트에서 워크스페이스를 전환했을 때, 알림 목록·읽지 않은 개수가 이전 워크스페이스 것으로 남아 있을 수 있었어요. 알림 구독을 워크스페이스 단위로 다시 초기화하도록 고쳤고(`useNotifications`), 사이드바가 현재 워크스페이스를 알림 훅에 전달하도록 연결했어요. 전환 도중 이전 워크스페이스의 응답이 늦게 도착해 새 워크스페이스를 덮어쓰는 경합(TOCTOU)도 함께 막았어요(요청 시점의 워크스페이스를 고정해 어긋난 응답은 버림).

### Changed

- **내부 구조 정리 — 큰 화면 컴포넌트 4개 분해 + 렌더 성능 손질 (개발자 전용, 사용자 화면·동작 변화 없음)**: 컴포넌트 리팩토링 Phase 5-7. 한 파일에 너무 많은 일이 몰려 있던 화면 컴포넌트들을 React 베스트프랙티스대로 작은 응집 단위로 쪼갰어요(동작·모양 그대로, 공개 props 그대로). 칸반 보드(`KanbanBoard` → 컬럼·카드·드래그 오버레이·접근성 안내로 분리), 멤버 설정(`MembersPanel` → 멤버 행·초대 폼·대기 초대 행), 견적 비교(`FocusComparison` → 탭 스트립·수수료 매트릭스·비교 행·메모/PDF·선정 CTA로 분리, **봉인 입찰 데이터 경계 그대로 유지**), PG 견적 작성 위저드(`BidWizard` → 단계마다 수많은 props 를 내려주던 방식을 화면 단위 컨텍스트로 교체). 자주 다시 그려지던 잎 컴포넌트엔 `React.memo` + 안정 콜백을 적용했어요. 칸반 보드는 접근성 향상으로 스크린리더가 드래그 상태를 한국어로 읽어줘요(키보드 드래그 자체는 Enter 클릭 충돌 위험 때문에 후속 과제로 유지). 새 단위 테스트 다수 추가, 전체 테스트 3350개 통과. 보드 컬럼/카드 단위 memo 최적화의 완전한 발현(데이터+안정 renderCard 패턴)은 동작 회귀 위험이 있어 별도 perf 패스로 분리(TODOS.md).

## [0.2.22.13] - 2026-06-16

### Changed

- **내부 구조 정리 — 채팅 낙관적 전송 로직 공용화 (개발자 전용, 사용자 화면·동작 변화 없음)**: 컴포넌트 리팩토링 Phase 3 마무리. 상대방 채팅(ThreadView)과 팀 채팅(TeamThreadView)이 똑같이 들고 있던 "낙관적 전송" 처리 로직 — 보내는 즉시 회색 말풍선을 띄우고(전송 중), 서버 응답이 오면 실제 메시지로 확정 교체하고, 실시간으로 같은 메시지가 먼저 도착하면 중복을 막고, 실패하면 말풍선을 거두는 — 의 순수 계산 부분을 공용 모듈(`optimistic-thread.ts`: `promoteSentMessage`·`removeMessage`·`applyLiveEcho`)로 모았어요. 채널 구독·전송 액션·실패 시 입력 복원 순서·말풍선 모양 만들기는 두 화면 차이가 있어 각자 그대로 두고, 메시지를 어떻게 합치고 확정하느냐는 계산만 공유해요(팀 채팅의 서버 첨부 교체 같은 화면별 차이는 인자로 보존). 봉인 입찰 경계(서로 다른 대화·팀 채팅의 데이터/채널 분리)는 그대로예요 — 공용 모듈은 id·전송중 표시·시각만 다루고 어느 대화에 속하는지는 각 화면이 소유해요. 동작·모양은 그대로, 단위 테스트 13개 추가, 전체 테스트 3319개 통과.

## [0.2.22.12] - 2026-06-16

### Changed

- **내부 구조 정리 — 견적 딜룸 상태를 전역 스토어에서 화면 단위 컨텍스트로 (개발자 전용, 사용자 화면·동작 변화 없음)**: 컴포넌트 리팩토링 Phase 4. 견적 딜룸의 "지금 보고 있는 상대방(PG)" 상태를 전역 싱글턴 스토어(`useChatRailStore`)에서 딜룸 단위로 스코프된 React 컨텍스트(`DealRoomContext`)로 옮겼어요. 기존엔 한 딜룸의 가운데 비교 화면이 전역에 상대방을 써두고 우측 채팅이 읽는 방식이라, 딜룸을 옮길 때 이전 상대방이 남지 않도록 수동 초기화에 의존했는데(누수 위험), 이제 컨텍스트가 딜룸별로 마운트(`key`)돼 자동으로 깨끗하게 초기화돼요. 동작·화면은 그대로(전체 테스트 3306개 통과). 봉인 입찰 경계와 무관한 클라이언트 UI 상태 정리예요.

## [0.2.22.11] - 2026-06-16

### Changed

- **내부 구조 정리 — 채팅 말풍선 컴포넌트 공용화 (개발자 전용, 사용자 화면·동작 변화 없음)**: 컴포넌트 리팩토링 3g. 상대방 채팅(ThreadView)과 팀 채팅(TeamThreadView)이 각자 들고 있던 메시지 말풍선 마크업(좌우 정렬·말풍선·첨부·타임스탬프/전송중 점)을 공용 `MessageBubble` 컴포넌트로 모았어요. 본문은 슬롯으로 주입해(상대방=URL 자동링크, 팀=멘션 렌더) 각 화면의 차이를 보존하고, 발신자 헤더·날짜 구분선·견적 칩·읽음 표시 같은 화면별 요소는 그대로 각자 그려요. `React.memo` 로 감싸서 메시지를 입력하는 동안 대화 목록 전체가 다시 그려지지 않도록 성능도 개선했어요(렌더러를 안정적으로 고정). 동작·모양은 그대로, 단위 테스트 5개 추가, 전체 테스트 3305개 통과.

## [0.2.22.10] - 2026-06-16

### Changed

- **내부 구조 정리 — 팀 채팅 멘션 입력 로직 분리 (개발자 전용, 사용자 화면·동작 변화 없음)**: 컴포넌트 리팩토링 3f. 팀 채팅(TeamThreadView)에 인라인으로 얽혀 있던 `@`멘션 컨트롤러(드롭다운 열기/후보 목록·방향키 네비게이션·선택·캐럿 복원·전송 시 토큰 환원)를 `useMentionPicker` 훅으로 분리했어요. 멘션 자동완성·키보드 동작·동명이인 표시·캐럿 위치는 모두 그대로예요(순수 멘션 로직은 기존 mention-input 그대로 재사용). 단위 테스트 8개로 열기/네비/선택/닫기/전송 본문 환원을 고정했어요. 전체 테스트 3300개 통과.

## [0.2.22.9] - 2026-06-16

### Changed

- **내부 구조 정리 — 채팅 초안 보존 로직 분리 (개발자 전용, 사용자 화면·동작 변화 없음)**: 컴포넌트 리팩토링 3e. 상대방 채팅(ThreadView)이 직접 들고 있던 "작성 중이던 메시지를 대화별로 localStorage 에 보존(대화 전환 후에도 유지, 비우면 삭제)" 로직을 `useStringDraft(key)` 훅으로 분리했어요. 기존처럼 디바운스 없이 즉시 기록해요(짧은 텍스트라 비용이 작고 타이밍 의존 테스트 플레이크도 피함). 동작은 그대로, 단위 테스트 4개로 복원·기록·삭제를 고정했어요. 전체 테스트 3292개 통과.

## [0.2.22.8] - 2026-06-16

### Changed

- **내부 구조 정리 — 채팅 대화 해소 로직 분리 (개발자 전용, 사용자 화면·동작 변화 없음)**: 컴포넌트 리팩토링 3d. 채팅 패널이 "상대방을 고르면 기존 대화를 찾아 연다(없으면 첫 메시지를 보낼 때만 새로 만든다)"는 읽기 전용 해소 로직을 `useConversationLookup` 훅으로 분리했어요. **봉인입찰 불변식**(열람만으로는 대화를 만들지 않음 — 관심 신호가 새지 않도록)을 단위 테스트 5개로 못박았어요(읽기 전용 lookup 만 호출하고 생성은 절대 안 함, 실패 시 재시도, 첫 전송 시 대화 id 기록). 동작은 그대로, 전체 테스트 3288개 통과.

## [0.2.22.7] - 2026-06-16

### Changed

- **내부 구조 정리 — 채팅 자동 스크롤 로직 중복 제거 (개발자 전용, 사용자 화면·동작 변화 없음)**: 컴포넌트 리팩토링 3c. 상대방 채팅(ThreadView)과 팀 채팅(TeamThreadView)이 각자 복제하던 "새 메시지가 오면 하단으로 따라가되, 위로 올려 과거 글을 읽는 중이면 끌어내리지 않는다"는 자동 스크롤 로직을 공용 `useStickToBottom` 훅으로 모았어요. 상대방 채팅의 "새 메시지" 안내 pill 은 옵션(`withPill`)으로 유지하고, 팀 채팅은 기존대로 pill 없이 따라가기만 해요. 동작은 그대로, 하단 근처 판정 로직을 순수 함수로 분리해 단위 테스트 6개로 고정했어요. 전체 테스트 3283개 통과.

## [0.2.22.6] - 2026-06-15

### Changed

- **내부 구조 정리 — 채팅 입력창 중복 제거 (개발자 전용, 사용자 화면·동작 변화 없음)**: 컴포넌트 리팩토링 3b. 상대방 채팅(ThreadView)과 딜룸 새 대화 컴포저(ChatPanel)가 각자 인라인으로 복제하던 입력창 로직(내용에 따라 높이가 늘어나는 자동 높이 + 한글 IME 조합 중 Enter 가 메시지를 끊지 않도록 막는 안전 전송)을 공용 `ChatComposerTextarea` 컴포넌트로 모았어요. 멘션 입력이 결합된 팀 채팅(TeamThreadView)은 대상에서 제외했어요(별도 작업). 동작·스타일은 그대로(전송 후 높이 리셋 포함), 단위 테스트 5개로 IME/Shift+Enter 동작을 고정했어요. 전체 테스트 3277개 통과.

## [0.2.22.5] - 2026-06-15

### Changed

- **내부 구조 정리 — 채팅 첨부 업로드 로직 중복 제거 (개발자 전용, 사용자 화면·동작 변화 없음)**: 컴포넌트 리팩토링 3차분. 상대방 채팅(ThreadView)·팀 채팅(TeamThreadView)·새 메시지 작성(MessageComposeSheet) 세 곳이 각자 복제하던 파일 첨부 업로드 상태머신(파일 검증·임시 행 스테이징·업로드·서버 응답 스왑·제거)을 공용 훅 `useComposerAttachments` 한 곳으로 모았어요. 봉인입찰 권한 경계(첨부 소유 종류 `ownerKind`/`ownerId`는 채팅별로 달라요 — 상대방 vs 팀)는 훅 호출 시 그대로 주입해 서버 권한 검증을 한 치도 바꾸지 않았고, 각 화면의 낙관적 전송·되돌리기 흐름도 그대로 보존했어요. 업로드 에러 문구도 화면별로 동일하게 유지(작성 드로어의 413/문구 차이 포함). 단위 테스트 7개로 검증, 전체 테스트 3272개 통과.

## [0.2.22.4] - 2026-06-15

### Changed

- **내부 구조 정리 — 견적 수수료 매핑 중복 제거 (개발자 전용, 사용자 화면·동작 변화 없음)**: 컴포넌트 리팩토링 2차분. 입찰 위저드(BidWizard)·견적서 템플릿 편집기(QuoteTemplateDrawer)·템플릿 목록(QuoteTemplateList)이 각자 복제하고 있던 수수료 변환 로직(요율 %↔소수 변환 `fmtPct`은 3곳, 수수료맵 인코딩·정산주기 파싱·템플릿 디코딩은 2곳)을 순수 함수 모듈 `lib/quote/template-fees.ts` 한 곳으로 모았어요. 봉인입찰 제출 경로의 금액 산식이라 동작을 한 줄도 바꾸지 않았고(두 호출처의 차이 — 구버전 단일요율 템플릿을 전 구간으로 전개할지 — 는 명시적 옵션으로 보존), 단위 테스트 16개로 round-trip·legacy 전개를 고정했어요. 전체 테스트 3265개 통과.

## [0.2.22.3] - 2026-06-15

### Changed

- **내부 구조 정리 — 프론트엔드 중복 컴포넌트 제거 (개발자 전용, 사용자 화면·동작 변화 없음)**: 멀티에이전트 분석으로 찾은 중복을 단일 출처로 모으는 리팩토링 1차분이에요. 채팅 스레드 캐시 두 개를 공통 팩토리(`createSuspensePromiseCache`)로, 샘플 삭제 배너 두 벌(구매사·PG)을 공용 컴포넌트로(삭제 액션은 권한이 달라 분리 유지), `ChatPanel` 안에 복제돼 있던 팀 채팅 페인을 별도 파일로 합쳤어요. 계약 유형 라벨·색(`CONTRACT_TYPE_LABELS`)은 3곳 중복을 `lib/types`로, 견적 상태 칩 맵(정식 페이지·인터셉트 모달·목록표 3중복)은 새 `lib/rfp-status`로 일원화했고, 키보드 단축키 키캡(`Kbd`/`ModifierShortcut`)을 vendored shadcn 슬롯에서 프로젝트 프리미티브로 옮겼어요. 동작은 이전과 완전히 똑같아요(전체 테스트 3249개 통과, 순 −193줄).

## [0.2.22.2] - 2026-06-15

### Changed

- **리포지토리 경계를 ESLint 로 강제 — 신규 직접 DB 접근을 lint 가 차단해요 (개발자 전용, 사용자 화면·동작 변화 없음)**: 직전 변경(v0.2.22.1)으로 모든 DB 접근을 한 곳(`lib/server/repositories`)으로 모았는데, 이제 그 경계를 ESLint 규칙(`repo-boundary/db-access`)과 독립 드리프트 가드 테스트로 못박았어요. 누군가 실수로 레포 밖에서 DB 를 직접 import 하면 `pnpm lint` 가 빨갛게 떠요. 더 이상 안 쓰는 `db` 파라미터(헬퍼에 넘기기만 하던 것)들을 정리해 예외 목록을 9개에서 4개로 줄였고, 예외는 단일 출처(`db-boundary-allowlist.mjs`)에 명문화했어요. 동작은 이전과 완전히 똑같아요(전체 테스트 3234개 통과).

## [0.2.22.1] - 2026-06-15

### Changed

- **내부 구조 정리 — 모든 DB 접근을 리포지토리 계층으로 일원화 (사용자 화면·동작 변화 없음)**: 그동안 서비스·서버 액션·페이지·API 곳곳에서 데이터베이스를 직접 다루던 코드가 흩어져 있었는데, 이제 모든 DB 읽기·쓰기를 한 곳(`lib/server/repositories`)을 통하도록 정리했어요. 신규 리포지토리 6개와 ~70개 메서드를 추가하고, 보드/견적서 템플릿/이메일 인증 로직을 서비스 계층으로 옮겨 액션을 얇게 만들었어요. 동작은 이전과 완전히 똑같고(전체 테스트 3232개 통과), 화면이나 사용하는 방식은 바뀐 게 없어요. 봉인 입찰(sealed-bid) 첨부 접근 권한은 동작 고정 테스트로 한 줄도 안 바뀌게 보존했어요. (경계 강제용 ESLint 규칙은 후속 작업으로 분리.)

## [0.2.22.0] - 2026-06-15

### Fixed

- **알림 이메일이 한꺼번에 많이 나갈 때도 빠짐없이 도착해요**: 견적 요청을 여러 결제대행사에 한 번에 보내거나, 입찰 도착·선정 알림이 여러 담당자에게 동시에 나갈 때 그동안 일부 이메일이 발송에 실패하던 문제가 있었어요. 이제 한 번에 묶어서 보내(메일 서비스의 속도 제한에 걸리지 않도록) 빠짐없이 전달하고, 일시적인 오류로 실패하면 잠시 뒤 자동으로 다시 시도해요. 잘못된 주소처럼 다시 보내도 소용없는 건만 빠르게 걸러내요. 메일이 조금 늦거나 끝내 실패해도 앱 안의 알림(벨)은 평소처럼 그대로 떠요.

## [0.2.21.0] - 2026-06-15

### Added

- **팀 채팅에서 `@`로 팀원을 멘션할 수 있어요**: 견적별 팀 채팅에 `@`를 입력하면 같은 팀(워크스페이스) 멤버 목록이 떠요. 이름 초성으로 빠르게 찾을 수 있고, 프로필(이니셜 아바타)과 이름이 함께 보여요. 같은 이름을 가진 팀원이 있으면 합류일자를 옆에 보여줘서 헷갈리지 않게 했어요. `@전체`로 팀원 모두를 한 번에 부를 수도 있어요. 멘션된 사람에게는 전용 알림이 가고(다른 팀으로는 절대 새지 않아요), 보낸 메시지에서 멘션은 이름으로 강조 표시돼요 — 본인이 멘션되면 더 눈에 띄게 보여요.
## [0.2.19.2] - 2026-06-15

### Removed

- **레포지토리 팩토리에서 쓰지 않는 메모리 백엔드 선택 코드를 정리했어요** (개발 도구): 예전에 인메모리 저장소 구현이 있던 시절의 잔재(`__backend` 마커·`REPO_BACKEND` 환경변수·테스트 전용 `__getBackend` 헬퍼)가 남아 있었어요. 지금은 Drizzle 구현만 쓰고 단위 테스트는 PGlite로 실제 DB를 검증하기 때문에 더 이상 의미가 없어, 관련 죽은 코드와 문서 설명을 모두 지웠어요. 서비스 동작 변화는 없어요.

## [0.2.19.1] - 2026-06-14

### Changed

- **여러 작업을 동시에 배포할 때 버전 충돌이 자동으로 풀려요** (개발 도구): 서로 다른 세션에서 `/ship`을 동시에 돌리면 같은 다음 버전을 집어 `VERSION`·`package.json`·`CHANGELOG`가 매번 충돌했어요. 이제 git 머지 드라이버를 등록해서 `VERSION`과 `package.json`은 더 높은 버전으로, `CHANGELOG`는 양쪽 항목을 모두 남기도록 자동 정리해요. `pnpm install` 시 자동 등록돼요.

## [0.2.19.0] - 2026-06-14

### Fixed

- **가입할 때 이메일 인증을 빠뜨리지 않도록 했어요**: 그동안 일부 가입 경로(특히 결제대행사가 이미 등록된 회사 워크스페이스로 합류하는 경우)에서는 이메일 인증 화면을 한 번도 보지 못한 채 바로 서비스를 쓸 수 있었어요. 이제 이메일 인증을 아직 마치지 않았다면 워크스페이스 승인 상태와 상관없이 인증 화면으로 안내하고, 인증을 끝내야 서비스로 들어가요. 인증을 마치면 곧장 알맞은 화면(서비스 홈 또는 입점 심사 대기)으로 이동해요.

## [0.2.18.1] - 2026-06-14

### Changed

- **구매사 홈에서 '견적 요청하기' 버튼이 더 크고 또렷해졌어요**: 예전에는 새 견적을 만드는 버튼이 화면 위쪽 구석에 작게 있어서 눈에 잘 띄지 않았어요. 이제 진행중·마감 임박·견적 검토대기·선정 완료 카드 바로 아래에, 가로 폭을 꽉 채운 큰 버튼으로 자리해서 새 견적 요청을 한눈에 시작할 수 있어요.

## [0.2.18.0] - 2026-06-14

### Added

- **메시지함에서 팀 채팅까지 한곳에서 봐요**: 지금까지 딜룸(견적 상세) 안에서만 볼 수 있던 '팀 채팅'을 이제 메시지함에서도 확인해요. 왼쪽 목록 위의 **[전체 · 상대방 · 팀]** 필터로 상대방(구매사↔PG) 대화와 우리 팀끼리 나눈 견적별 팀 채팅을 한 목록에서 골라봐요. 팀 채팅을 누르면 메시지함 안에서 바로 읽고 답할 수 있고, 딜룸의 '팀 채팅' 탭에서도 **'메시지함에서 열기'**로 곧장 넘어와요.
- **팀 채팅에도 안 읽음 표시와 알림이 생겼어요**: 팀원이 새 팀 메시지를 남기면 목록에 안 읽음 점이 뜨고, 인앱 알림과 이메일 알림으로도 알려줘요(메시지를 쓴 본인은 빼고 같은 워크스페이스 팀원에게만, 몇 분 단위로 모아서). 대화를 열면 자동으로 읽음 처리돼요. 홈 화면 메시지 위젯에도 팀 채팅이 함께 나와요.

### Changed

- **어떤 견적 관련 대화인지 메시지함에서도 한눈에**: 메시지함의 상대방 대화에서 메시지마다 관련 견적(RFP) 칩이 다시 보여요. 한 상대와 여러 건을 주고받아도 어떤 견적 이야기인지 헷갈리지 않아요.

## [0.2.17.1] - 2026-06-14

### Changed

- **견적 요청서 '현재 조건' 입력칸의 예시 안내가 숫자로 또렷해졌어요**: 현재 월 정산한도·보증보험 입력칸의 흐린 예시 문구가 `월 1억`·`3000만원`처럼 적혀 있어서, 막상 어떻게 넣어야 할지 헷갈릴 수 있었어요. 이 칸들은 숫자만 넣으면 천 단위 콤마가 자동으로 붙는 칸이라, 예시도 `100,000,000`·`30,000,000`처럼 실제 입력 모습 그대로 보여주도록 바꿨어요.

## [0.2.17.0] - 2026-06-14

### Fixed

- **로그아웃하면 화면이 로그인↔홈을 무한 반복하며 멈추던 문제를 고쳤어요**: 일부 환경에서 예전에 저장돼 있던 로그인 쿠키가 남아 있으면, 화면이 로그인과 홈 사이를 끝없이 오가다 멈춰버리는(무한 새로고침) 일이 있었어요. 이제 로그아웃할 때 남아있던 로그인 정보를 도메인이 다른 것까지 빠짐없이 깨끗이 지워서, 이런 무한 반복에 갇히지 않고 곧장 로그인 화면으로 돌아가요. 혹시 지금 이 문제로 멈춰 있던 분도 로그아웃을 한 번 거치면 자연스럽게 정상으로 풀려요.

## [0.2.16.0] - 2026-06-14

### Fixed

- **이미 가입된 이메일로 가입을 끝까지 진행해도 더 이상 막히지 않아요**: 전에는 이미 쓰고 있는 이메일로 휴대폰 인증까지 마치고 가입을 마무리하면 화면이 그냥 실패하면서 왜 안 되는지 알 수 없었어요. 이제는 가입 마지막 단계에서 **"이미 가입된 이메일이에요"** 안내가 또렷이 떠서 로그인으로 넘어갈 수 있어요. 구매사·PG 가입, 초대 수락, 주요 PG사 합류까지 모든 가입 경로에서 똑같이 동작해요. (중단했던 미인증 가입을 같은 이메일로 다시 시작하는 건 예전처럼 그대로 이어서 할 수 있어요.)

### Changed

- **견적 요청서의 '현재 조건' 입력이 형식에 맞게 정리돼요**: 지금까지 현재 카드 수수료·월 정산한도·보증보험·정산주기·배송 및 서비스 기간을 자유롭게 적다 보니 사람마다 표기가 제각각이었어요. 이제 입력칸이 알아서 형식을 잡아줘요. **카드 수수료**는 숫자만 넣으면 옆에 `%`가 붙고(0~100 사이, 소수점 둘째 자리까지), **월 정산한도·보증보험**은 금액을 넣으면 천 단위 콤마와 `원`이 붙고, **정산주기·배송 및 서비스 기간**은 `D+` 다음에 숫자만 넣으면 돼요. 글자나 범위를 벗어난 값은 처음부터 들어가지 않아 잘못 적을 일이 줄어요.
- **입력한 현재 조건이 보기 좋은 형식으로 표시돼요**: 적어둔 금액은 `1억원`·`3,000만원`처럼, 수수료는 `3.4%`처럼 정리해서 PG 브리프·요청 조건·발송 전 요약·견적 비교 화면에 똑같이 보여줘요. 예전에 자유롭게 적어둔 견적도 그대로 잘 보여요.

## [0.2.14.0] - 2026-06-14

### Added

- **PG 파트너 전용 랜딩페이지가 생겼어요**: 파트너 사이트(`partner.supporter-b.com`)에 들어오면 PG 영업담당자를 위한 새 소개 화면이 떠요. "성장하는 신규 가맹점을 가장 빠르게 만나는 파트너 채널"과 "확실하게 도입 의사가 있는 가맹점을…" 두 메인 카피가 부드럽게 번갈아 바뀌고, 어디서든 **'파트너 상담 신청'** 버튼을 누르면 채널톡 상담이 바로 열려요.
- **영업 문제 → 성장 고객사 → 검증·공정 → 참여 방식 → 핵심 이점 → FAQ → 최종 제안까지 한 흐름으로**: PG 영업의 진짜 어려움(리드 수가 아니라 확실한 니즈)을 짚고, "관심은 있지만 움직이지 않는 고객사" 같은 영업 현실을 카드로 보여줘요. 새로 만날 수 있는 성장 고객사 유형 4가지는 **좌우로 넘기는 캐러셀**(자동 전환·이전/다음·점 표시)로 소개하고, 검증된 리드와 공정한 기회라는 메시지, 5단계 참여 방식, PG사가 얻는 이점 6가지, 자주 묻는 질문 7개를 차례로 담았어요.
- **궁금하면 바로 상담**: 히어로·참여 방식·최종 제안 자리의 '파트너 상담 신청'과 '제휴 소개서 받기'는 모두 채널톡 상담으로 이어져, 따로 가입하지 않아도 바로 문의할 수 있어요.

## [0.2.13.0] - 2026-06-14

### Changed

- **견적을 누르면 '딜룸' 한 화면에서 내용 확인·채팅·작업을 한 번에 해요**: 지금까지 견적을 열면 목록에서는 옆에 살짝 뜨는 패널, 전체 보기에서는 별도 페이지로 흩어져 있었고 채팅은 따로 토글해야 했어요. 이제 목록에서 견적을 누르면 **흐려진 배경 위에 큰 딜룸 창**이 떠서, 가운데에서 견적 내용을 보면서 오른쪽에서 바로 채팅까지 할 수 있어요. 왼쪽 작업 줄에서 이 견적과 관련된 기능에 바로 손이 닿고요. 주소(URL)는 그 견적 주소로 바뀌어 **링크를 공유하거나 새로고침하면 같은 내용이 단독 페이지로** 열리고, 뒤로 가기를 누르면 창이 닫히며 목록으로 돌아와요. 표 뷰의 행이든 보드(칸반) 뷰의 카드든 똑같이 이 딜룸으로 열려요.
- **구매사 딜룸**: 가운데 탭에서 여러 PG의 견적을 나란히 비교하고, 오른쪽 채팅은 지금 보고 있는 PG를 따라가요. 왼쪽 작업 줄에서 선정·재요청·PG 관리·마감·취소를 바로 할 수 있어요(선정은 비교 화면에서 고른 PG를 대상으로 동작해요). '요청 조건'·'첨부'도 같은 창의 탭으로 옮겨 다녀요.
- **PG 딜룸**: 가운데에서 요청 내용을 보면서 바로 견적을 작성·수정하고, 오른쪽에서 구매사와 채팅해요. 작성을 마치면 별도의 완료 페이지로 튕기지 않고 같은 창에서 이어져요. 왼쪽 작업 줄에서 견적 작성·요청 보기·첨부·철회에 접근해요.
- **상대방 채팅과 팀 채팅을 견적 옆에서 그대로**: 딜룸 오른쪽 채팅에는 상대방 채팅과 우리 팀끼리만 보는 팀 채팅이 함께 있어, 견적 내용을 보면서 대화를 이어갈 수 있어요. 한 워크스페이스에 담당자가 여러 명이어도 누가 보낸 메시지인지 이름·아바타로 구분돼요. (견적을 열어보는 것만으로는 상대에게 어떤 신호도 가지 않아요 — 봉인 입찰 원칙 그대로.)
- **전체화면으로 넓게 보기**: 딜룸 위쪽의 전체화면 버튼으로 창을 화면 가득 넓힐 수 있고, 위/아래 화살표(‹ ›)로 목록의 이전·다음 견적으로 바로 넘어갈 수 있어요(작성 중이던 채팅·견적 내용은 그대로 유지돼요).

### Removed

- **흩어져 있던 옛 견적 보기 UI를 정리했어요**: 목록 옆에 살짝 뜨던 미리보기 패널과 별도 채팅 토글 등 딜룸으로 일원화되며 더 이상 쓰지 않는 화면 코드를 제거했어요. 견적 작성 페이지 주소만 `/rfp/new` 에서 `/rfp-create` 로 옮겼고, 기존 `/rfp/new` 링크는 자동으로 새 주소로 이어져요.

## [0.2.12.0] - 2026-06-14

### Added

- **견적 요청을 보낼 때 '현재 카드 수수료'를 PG사에 공개할지 고를 수 있어요**: 지금까지는 견적 요청에 적은 현재 카드 수수료가 초대한 PG사 모두에게 그대로 보였어요. 이제 작성 화면의 현재 카드 수수료 아래에서 'PG사에 공개하기'를 끄면, PG사 견적 화면에서 이 수수료가 보이지 않아요. 더 공격적인 제안을 받고 싶거나 기존 계약 조건을 드러내고 싶지 않을 때 쓸 수 있어요. 끄더라도 내 견적 비교 화면에서는 현재 수수료가 기준값으로 그대로 보여서, 받은 제안이 얼마나 좋아졌는지 비교하는 데는 영향이 없어요. 기본값은 '공개'라서, 따로 끄지 않으면 예전과 똑같이 동작해요. 비공개로 두면 수수료 값 자체가 PG사 쪽으로 전달되지 않아, 화면뿐 아니라 데이터로도 보이지 않아요.

## [0.2.11.0] - 2026-06-14

### Added

- **견적 메모를 '팀 채팅'에 파일까지 붙여 남길 수 있어요**: 지금까지 한 견적에 대한 팀 내부 메모는 견적별 '내 메모'와 채팅 레일의 '팀 채팅'으로 흩어져 있었고, 팀 채팅은 글만 남길 수 있었어요. 이제 팀 채팅에서 PDF·이미지(PNG·JPEG)를 바로 첨부해 보낼 수 있어, 견적 관련 자료·캡처·결정 근거를 대화 한곳에 모아둘 수 있어요. 첨부는 우리 워크스페이스 팀에게만 보여요(상대 PG나 다른 PG에는 절대 보이지 않아요 — 봉인 입찰 원칙 그대로). 글 없이 파일만 보내는 것도 돼요.
- **온보딩 샘플 견적에서도 '팀 채팅'을 체험할 수 있어요**: 예전엔 샘플 견적 화면에서 채팅 레일이 통째로 숨겨져 있었어요. 이제 샘플에서도 채팅 레일이 열려 '팀 채팅'으로 메모·첨부를 실제로 남겨볼 수 있어요. 단, 샘플의 '상대방 채팅' 탭은 상대가 데모 PG라 실제로 전송되지 않도록 막아두고 안내 문구를 보여줘요(팀 채팅은 정상 동작).

### Removed

- **견적별 '내 메모' 패널을 없애고 '팀 채팅'으로 일원화했어요**: 각 PG 견적 탭 안에 따로 있던 '내 메모'는 사실 이미 팀 전원에게 공유되던 기록이라, 같은 성격의 '팀 채팅'과 역할이 겹쳤어요. 이제 견적별 '내 메모' 패널은 없애고, 팀 메모는 채팅 레일의 '팀 채팅' 한곳에서 첨부와 함께 남기도록 통합했어요. 기존에 쌓인 메모 데이터는 그대로 보존돼요(화면 표시만 정리).

## [0.2.10.10] - 2026-06-14

### Changed

- **회원가입을 시작할 때 '구매사인지 PG인지' 직접 고르는 단계가 사라졌어요**: 예전에는 가입을 시작하면 "어떤 계정으로 시작할까요?" 화면에서 역할(구매사·PG 영업담당)을 손수 골라야 했어요. 이제는 접속한 주소가 역할을 알아서 정해, 구매사 사이트(`supporter-b.com`)에서는 곧장 구매사 가입으로, 파트너 사이트(`partner.supporter-b.com`)에서는 곧장 PG 가입으로 들어가요. 역할이 따로 나뉘지 않은 단일 주소(로컬·개발 환경)나 알 수 없는 주소로 들어오면 구매사 가입으로 안내해요. 초대 링크나 구매사 홈의 '가입하기' 버튼처럼 역할이 정해진 기존 진입 경로는 예전 그대로 동작하고, 가입 뒤 원래 보려던 화면으로 돌아가는 복귀 경로도 단계마다 잘 이어져요.

### Removed

- **가입 첫 화면의 역할 선택 카드 화면을 없앴어요**: 위 변경에 따라 더 이상 쓰지 않는 "어떤 계정으로 시작할까요?" 역할 선택 화면을 제거했어요.

## [0.2.10.9] - 2026-06-14

### Changed

- **채팅에서 '어느 담당자'와 메시지를 주고받았는지 보여요**: 지금까지 대화 메시지에는 상대 '회사 이름'만 떠서, 한 회사에 담당자가 여러 명이면 누가 보낸 메시지인지 알 수 없었어요. 이제 메시지 묶음마다 보낸 담당자의 이름과 아바타가 함께 떠요. 받은 메시지뿐 아니라 우리 팀이 보낸 메시지에도 누가 보냈는지 표시돼서, 한 대화를 팀원 여러 명이 함께 쓸 때도 누가 응답했는지 한눈에 알 수 있어요. 담당자 이름에 마우스를 올리면 이메일도 확인할 수 있고요. 같은 담당자가 연달아 보낸 메시지는 이름을 한 번만 묶어서 깔끔하게 보여줘요. (대화 상대 식별 방식은 그대로라 영업 정보가 새지 않아요.)

## [0.2.10.8] - 2026-06-14

### Changed

- **견적 제안서의 숫자 입력칸이 숫자만 받고, 입력하면 얼마인지 바로 보여줘요**: 카드·간편결제 구간별 우대수수료 표는 글자·기호까지 그대로 입력되고, 입력한 요율이 실제로 얼마인지 알 길이 없었어요. 이제 이 칸들은 숫자(소수 둘째 자리)만 받고, 칸에 마우스를 올리거나 포커스하면 "1만원 결제 시 N원" 환산을 툴팁으로 보여줘요. 단일요율 수수료(%)와 정산 주기 일수 입력칸도 같은 방식으로 정리해 `e`·`+`·`-` 같은 숫자가 아닌 입력이 더는 새어 들어오지 않아요(정산 주기는 정수만, 최대 99). 정산한도·보증보험 금액 칸은 이전처럼 천 단위 구분과 "5,000만원" 식 한글 환산을 그대로 보여줘요.

## [0.2.10.7] - 2026-06-13

### Changed

- **견적 비교의 '개선 요약'에서 항목들이 표처럼 가지런히 정렬돼요**: 선택한 견적이 지금 조건보다 얼마나 좋아지는지 보여주는 요약(카드 수수료·정산주기·월 정산한도·보증보험)에서 현재 값·화살표·제안 값·개선폭이 줄마다 위치가 제각각이라 한눈에 비교하기 어려웠어요. 이제 네 지표가 같은 열에 맞춰 정렬돼, 무엇이 얼마나 달라지는지 위아래로 쭉 훑어보기 좋아졌어요. 일부 항목만 현재 조건을 입력했거나 현재 조건이 아예 없을 때도 열이 흐트러지지 않아요.

## [0.2.10.6] - 2026-06-13

### Changed

- **개발/CI 환경을 운영과 동일한 Node 22로 통일했어요** (내부 변경, 사용자 영향 없음): 운영 서버(Lightsail)는 이미 Node 22로 동작하지만 로컬과 CI는 Node 20 기준이라 "테스트는 통과했는데 운영에서만 다르게 동작"할 여지가 있었어요. 이제 `.nvmrc`(22)·`package.json` `engines`(>=22)·CI 워크플로(3개 잡)·`@types/node`(^22.13.0)를 모두 Node 22로 맞춰 로컬·CI·운영이 같은 런타임에서 검증돼요. 전체 유닛 테스트 2735개가 Node 22에서 통과하는 것도 확인했어요.

## [0.2.10.5] - 2026-06-13

### Changed

- **구매사 홈에서 '견적 요청하기' 버튼이 항상 보여요**: 온보딩 샘플 견적이 있거나 이미 보낸 견적이 있으면 홈 화면에서 새 견적을 만드는 버튼이 사라져, 사이드바로 들어가야만 새 견적을 시작할 수 있던 문제를 고쳤어요. 이제 구매사 홈 상단에 '견적 요청하기' 버튼이 항상 떠 있어 어떤 상태에서든 바로 새 견적을 시작할 수 있어요(견적 목록 화면과 같은 버튼이에요). 샘플 견적이 항상 깔리면서 더 이상 뜨지 않던 옛 온보딩 안내 목록은 함께 정리했어요.

## [0.2.10.4] - 2026-06-13

### Fixed

- **입점 심사 대기 화면의 '홈으로 가기'가 이제 실제로 이동해요**: 가입 후 심사 대기(`/pending-approval`) 화면에서 '홈으로 가기'를 눌러도 아무 일도 없이 같은 심사 대기 화면으로 되돌아오던 문제를 고쳤어요. 아직 워크스페이스 승인 전이라 앱 홈(`/home`)은 접근 가드에 막혀 다시 심사 대기 화면으로 튕기던 것이 원인으로, 이제 이 버튼은 공개 첫 화면으로 이동해요(이미 같은 방식으로 동작하던 이메일 인증 대기 화면과 맞췄어요). 승인이 끝나면 자동으로 홈으로 이동하던 동작은 그대로예요.

## [0.2.10.3] - 2026-06-13

### Fixed

- **PG 파트너 호스트에서 로그인·가입 완료가 다시 정상 동작해요**: `partner.supporter-b.com`에서 로그인하거나 가입을 완료할 때, 로그인한 호스트와 워크스페이스의 home 호스트가 다른 경우(예: buyer 워크스페이스가 활성인 계정이 PG 호스트에서 로그인) 다른 도메인으로 이동하다 브라우저 CORS 오류로 화면이 멈추던 문제를 고쳤어요. 클라이언트 화면 전환(`router.push`)이 다른 origin으로의 서버 리다이렉트를 따라가다 막히던 것이 원인으로, 이제 다른 호스트로 가야 할 때는 전체 페이지 이동으로 처리해 정상적으로 도착해요(같은 호스트는 기존처럼 부드럽게 전환). 워크스페이스 전환·초대 수락이 이미 쓰던 방식에 로그인·가입 완료를 맞춘 거예요.

## [0.2.10.2] - 2026-06-13

### Fixed

- **운영 빌드가 다시 정상적으로 빌드돼요**: 직전 `/_axiom/*` 인증 우회 수정(0.2.10.1)에서 인증 미들웨어 경로 매처를 별도 모듈 상수(`PROXY_MATCHER`)로 분리하면서, Next.js가 `config.matcher`를 정적 문자열 리터럴로만 허용하는 제약에 걸려 `next build`가 `matcher[0] need to be static strings`로 실패하던 문제를 고쳤어요(매처 동작 자체는 그대로). 매처를 리터럴로 인라인하고, 정규 정의(`lib/auth/proxy-matcher.ts`)와 인라인 리터럴이 절대 어긋나지 않도록 드리프트 가드 테스트를 추가했어요. (유닛 테스트는 통과했지만 빌드가 깨져 있던 상태라, 다음 운영 배포가 빌드 단계에서 실패했을 문제예요.)

## [0.2.10.1] - 2026-06-13

### Fixed

- **성능 측정(웹 바이탈) 데이터가 다시 정상 수집돼요**: Axiom 웹 바이탈·로그 비콘(`/_axiom/*`)이 인증 미들웨어에 걸려 로그인 화면으로 리다이렉트되면서 400 오류가 나고 측정 데이터가 유실되던 문제를 고쳤어요. 이제 비콘이 인증 가드를 우회해 Axiom으로 바로 전송됩니다. (개발자 도구 네트워크 탭의 `/login?next=/_axiom/...` 400 노이즈도 사라져요.)

## [0.2.10.0] - 2026-06-13

### Fixed

- **휴대전화 인증을 마치면 회원가입이 정상적으로 끝나요**: 담당자 정보 단계에서 휴대전화 인증 후 '가입 완료'를 누르면, 첫 가입 화면으로 되돌아가지 않고 그대로 다음 단계로 넘어갑니다 (기존엔 인증을 마쳐도 처음 화면으로 튕기는 경우가 있었어요). 구매사·PG 가입 모두에 적용돼요.

## [0.2.9.0] - 2026-06-13

### Added

- **마스터/운영자 계정 (Google OAuth 전용)**: 운영 담당자가 모든 active 워크스페이스에 접속. 숨겨진 `/login/ops` 주소에서 Google 계정으로만 로그인하며, `MASTER_ACCOUNT_EMAILS` 환경변수 allowlist(쉼표로 여러 운영자)에 등록된 Google 계정만 허용(default-deny). 최초 로그인 시 계정이 자동 생성되어 시드 스크립트가 필요 없음. 로그인 후 사이드바 스위처에서 이름 검색으로 워크스페이스 전환.
- **운영자 행위 감사 표시**: 마스터가 워크스페이스에서 수행한 변경은 해당 워크스페이스 활동 기록에 '운영자' 배지로 표시됨.

### Changed

- **보안**: 마스터 계정은 비밀번호 로그인이 차단되고(allowlist 이메일은 비밀번호 경로 거부), 워크스페이스 생성·이메일 변경·계정 삭제·비밀번호 재설정이 모두 차단됨. `isMaster` 판정은 매 요청마다 서버 전용 env에서 재유도되어 토큰 조작으로 승격할 수 없음.

## [0.2.8.0] - 2026-06-13

### Fixed

- **칸반 보드에서 '진행중 → 선정 완료' 드래그가 이제 동작해요**: 카드를 선정 완료 컬럼으로 끌면 해당 견적 요청 상세로 바로 이동해 견적을 비교·선정할 수 있습니다 (기존엔 아무 반응이 없었어요).
- **끝난 견적에 '재요청' 표시가 남지 않아요**: 선정됨·미선정으로 끝난 건에는 더 이상 응답할 수 없으므로, 남아 있던 재요청 표시를 보드 카드와 목록에서 숨깁니다.
- **표의 '마감' 필터가 취소된 견적 요청도 함께 보여줘요**: 보드의 마감 컬럼과 같은 기준이라, 보드에서 넘어와도 찾던 카드가 빠지지 않습니다.

### Added

- **견적 카드에서 더 많은 걸 바로 알 수 있어요**: 받은 견적 요청 카드에 보낸 구매사 이름과 '재요청' 알림이 표시되고, 구매사 카드에는 '취소됨' 표시가 붙습니다. 이미 끝난 카드의 D-day 표시는 정리했어요.
- **드래그하면 옮길 수 있는 컬럼이 한눈에 보여요**: 카드를 드는 순간 옮길 수 없는 컬럼은 흐려지고, 드는 카드는 떠 있는 미리보기로 따라옵니다. 옮기는 즉시 화면에 반영돼요.
- **마감·선정 완료 같은 종결 컬럼은 최근 10장만 보여줘요**: 오래된 카드가 보드를 채우지 않도록 하고, '표에서 전체 보기'로 전체 목록에 바로 갈 수 있습니다 (현재 필터 유지).

### Changed

- **보드 뷰에서는 상태 필터 칩이 사라져요**: 컬럼이 곧 상태라 중복이었어요. 보드로 전환하면 남아 있던 상태 필터도 함께 해제됩니다 (주소로 직접 들어와도 동일).
- **모바일에서 카드를 길게 눌러 드래그해요**: 카드 위에서도 세로 스크롤이 막히지 않습니다. 보드 드래그는 마우스·터치 전용이 됐고, 키보드로는 상세 화면에서 같은 작업을 할 수 있어요.

## [0.2.7.0] - 2026-06-12

### Added

- **워크스페이스 활동 기록을 볼 수 있어요 (관리자 전용)**: 설정 > 활동 기록에서 우리 워크스페이스에서 일어난 주요 활동 — 견적 요청 생성·발송·선정·취소·마감·재요청, 견적 제출·철회, 멤버 초대·수락·역할 변경·내보내기 — 을 누가 언제 했는지 시간순으로 확인할 수 있습니다. 각 기록의 견적 번호를 누르면 해당 상세 화면으로 바로 이동하고, "더 보기"로 과거 기록을 이어서 볼 수 있어요. 관리자만 볼 수 있고 일반 멤버에게는 보이지 않습니다.
- **로그인 세션이 더 안전해졌어요**: 비밀번호를 재설정하거나 이메일을 바꾸면, 다른 기기·브라우저에 남아 있던 기존 로그인이 모두 즉시 풀립니다 (혹시 토큰이 새어 나갔더라도 비밀번호만 바꾸면 차단돼요). 페이지·버튼 동작은 물론 파일 다운로드와 채팅 연결까지 같은 기준으로 막힙니다. 세션 유지 기간도 최대 7일로 줄었어요 (계속 사용하면 자동 연장).
- **보안 응답 헤더가 적용됐어요**: 모든 화면에 HSTS·콘텐츠 타입 고정·프레임 제한·리퍼러 정책·기기 권한 차단 헤더가 붙어 클릭재킹·MIME 스니핑류 공격 표면이 줄었습니다.

### Changed

- **DB 스키마 변경 (additive)**: `users.session_version` 컬럼과 `audit_logs` 테이블이 추가됐습니다. 운영 DB에 `pnpm db:push` 적용 필요 — 두 변경 모두 추가형이라 데이터 손실 위험은 없지만, push 는 전체 스키마를 비교하므로 적용 전 statement 를 확인하세요. 새 환경변수는 없습니다.
- **기존 로그인은 배포 후에도 유지돼요**: 이미 로그인된 사용자가 일괄 로그아웃되지는 않습니다. 이후 비밀번호 재설정·이메일 변경을 한 사용자부터 옛 세션이 무효화됩니다.

## [0.2.6.0] - 2026-06-12

### Added

- **PG로 가입하면 바로 견적을 한 번 보내볼 수 있어요 (온보딩 샘플)**: 결제대행사 담당자가 새로 가입하면 받은 요청함에 둘러보기용 샘플 견적 요청 1건이 들어와 있어요. 실제 견적 작성 흐름 그대로 4단계로 조건을 채워 보내면, 잠시 뒤 "견적이 선정됐어요" 축하 화면이 떠서 요청 → 제안 → 선정까지의 전 과정을 손으로 한 번 체험할 수 있습니다. 샘플에는 `샘플` 표시가 붙고, 상단 안내에서 언제든 삭제할 수 있어요. 실제 구매사에게는 알림이 가지 않는 안전한 연습용이에요.

### Changed

- **배포 주의**: 기존 PG 워크스페이스에 샘플을 심으려면 배포 후 `pnpm backfill:sample-pg`를 1회 실행하세요 (멱등 — 여러 번 돌려도 안전). 신규 PG는 가입 시 자동으로 시드됩니다. 새 DB 스키마 변경은 없습니다 (기존 `is_sample`/`is_demo`/`sample_seeded_at` 컬럼 재사용).

## [0.2.5.0] - 2026-06-12

### Added

- **마감 전에 PG에게 견적을 다시 요청할 수 있어요 (견적 재요청)**: 구매사가 견적 비교 화면에서 마음에 드는 PG를 골라 "조건을 더 개선해서 다시 보내달라"고 요청할 수 있습니다. 개선 요청 메시지와 새 마감일을 함께 보내면, 받은 PG는 받은 요청 상세에서 안내 배너를 보고 이전 견적이 채워진 폼으로 조건을 고쳐 다시 제출해요. 1차 견적은 그대로 보존돼 라운드별로 쌓이고, 구매사는 누가 재요청에 응답했는지(재요청함·응답대기 → 재제출됨)와 라운드 번호를 한눈에 볼 수 있어요. 재요청은 고른 PG에게만 가고 경쟁사 정보는 새지 않으며, 응답하지 않으면 1차 견적이 그대로 유효해요.

## [0.2.4.0] - 2026-06-10

### Added

- **견적 상세 화면에서 바로 채팅할 수 있어요**: 구매사 견적 요청 상세(`/rfp/:id`)와 PG 받은 요청 상세(`/inbox/:rfpId`)에 우측 채팅 패널이 생겼습니다. 헤더의 "메시지" 버튼으로 열고, 화면을 떠나지 않고 상대방과 대화할 수 있어요. 구매사 쪽은 견적 비교에서 보고 있는 PG를 자동으로 따라갑니다. 패널에서 보낸 메시지에는 해당 견적 요청 태그가 자동으로 붙어요. (좁은 화면에서는 메시지함으로 이동하는 버튼으로 대체)
- **팀 채팅으로 내부 메모를 남길 수 있어요**: 채팅 패널의 "팀 채팅" 탭에서 견적 요청별로 우리 팀에게만 보이는 메모를 주고받을 수 있습니다. 구매사 팀과 PG 팀의 메모는 서로 완전히 분리되어 상대방에게 절대 보이지 않아요. 라이브로 수신되며 멘션·알림 없는 가벼운 메모장입니다.
- **대화가 없던 상대에게는 첫 메시지를 보내는 순간에만 대화가 만들어져요**: 패널을 열어 두고 견적을 구경하는 것만으로는 상대방 메시지함에 아무것도 생기지 않습니다 (검토 중이라는 신호가 새지 않아요).

### Changed

- **메시지 화면이 더 정돈됐어요**: 날짜 구분선이 라인 없는 중앙 라벨로 바뀌고, 보낸/받은 메시지 모두 말풍선 옆에 시각이 표시되며, 내 말풍선은 파란 계열로 구분됩니다.
- **한글 입력 중 Enter로 메시지가 잘려 전송되던 문제를 막았어요**: 조합 중인 Enter는 전송으로 처리하지 않습니다.
- **보낸 메시지의 시각이 서버 기준으로 표시돼요**: 기기 시계가 어긋나 있어도 새로고침 전후 시각이 일치합니다.
- **DB 스키마 변경**: `rfp_team_messages` 테이블 추가 (팀 채팅 저장소, append-only). 운영 DB에 `pnpm db:push` 필요.
- **배포 주의**: Centrifugo 설정에 `team` 네임스페이스가 추가되어 운영 컨테이너 재시작이 필요합니다 (`docker compose -f docker-compose.prod.yml restart centrifugo`). 누락 시 팀 채팅 라이브 수신만 동작하지 않습니다.

### Fixed

- **팀 채팅 탭을 갔다 와도 메시지가 사라지지 않아요**: 스레드를 다시 열 때마다 최신 내용을 새로 불러옵니다.
- **채팅 로딩이 실패해도 화면이 멈추지 않아요**: 패널 안에 "다시 시도" 버튼이 있는 안내가 표시되고, 상세 페이지 전체가 에러로 교체되던 경로를 차단했습니다.
- **읽던 위치를 유지해요**: 팀 채팅에서 위로 스크롤해 과거 메모를 읽는 중에 새 메시지가 와도 화면이 아래로 끌려가지 않습니다.

## [0.2.3.1] - 2026-06-10

### Added

- **주요 PG사를 카드로 선택해서 즉시 합류할 수 있어요**: PG사 가입 시 워크스페이스 단계에서 토스페이먼츠·KG이니시스·나이스페이먼츠·NHN KCP·헥토파이낸셜·다날·KICC(이지페이) 카드를 선택하면 해당 워크스페이스에 바로 합류됩니다. 관리자 심사 없이 즉시 `/inbox`로 이동해요. 기존 직접 입력 방식도 그대로 사용할 수 있어요.
- **마스터 계정은 어느 화면에도 표시되지 않아요**: 사전 시딩된 PG사 마스터 계정(`is_system_account=true`)은 멤버 목록·알림 팬아웃·이메일 발송 대상에서 자동으로 제외됩니다.
- **PG사 사전 시딩 스크립트**: `scripts/seed-pg-companies.ts`를 `pnpm tsx scripts/seed-pg-companies.ts`로 실행하면 7개 주요 PG사 워크스페이스와 마스터 계정이 idempotent하게 생성됩니다. 환경변수 `SEED_PG_<KEY>_EMAIL`/`SEED_PG_<KEY>_PASSWORD` 설정 필요.

### Changed

- **DB 스키마 변경**: `workspaces.canonical_pg_key` (text UNIQUE nullable) 및 `users.is_system_account` (boolean default false) 컬럼 추가. 운영 DB에 `drizzle-kit push` 필요.

## [0.2.3.0] - 2026-06-10

### Changed

- **견적 작성 화면에 헤더가 표시됩니다**: 새 견적 요청 화면(/rfp/new)에서도 다른 페이지처럼 상단 헤더(‹ › 이동·'견적 요청 / 새 견적 요청' 브레드크럼·⌘K 검색·유저메뉴)가 보입니다. 이전에는 헤더 없이 본문만 표시됐어요.

## [0.2.2.3] - 2026-06-10

### Added

- **어디서든 초성 검색이 됩니다**: Cmd+K 전체 검색에서도 초성(예: ㅇㄹ → 알림, ㅅㅅㄹ → 수수료 포함 견적)으로 이동 메뉴와 견적·견적서·오픈 기회를 바로 찾을 수 있어요.
- **PG사 검색에 초성 입력이 됩니다**: 견적 요청에서 PG사를 초대할 때 이름 전체를 입력하지 않아도 초성(예: ㄴㅇㅅ → 나이스페이먼츠)으로 바로 찾을 수 있어요.
- **입점 심사 알림 메일의 '심사하러 가기' 링크가 어드민 콘솔로 연결됩니다**: 신규 워크스페이스가 가입하면 운영자에게 전송되는 이메일의 링크가 이제 `admin.supporter-b.com`을 가리킵니다. `.env`에 `ADMIN_ORIGIN=https://admin.supporter-b.com`을 설정해 주세요.

### Changed

- **연간 거래액 입력이 숫자 전용으로 바뀌었어요**: 견적 요청 작성 2단계의 '전년도 연간 PG 총 거래액' 필드가 숫자 입력 전용으로 전환되어 천 단위 구분 기호가 자동으로 표시됩니다. 입력하면 "5,000만원" 같은 한글 표기로도 바로 확인할 수 있어요.
- **수수료·금액 한글 표기가 더 자연스러워집니다**: 숫자 금액을 한글로 보여줄 때 내부 로직을 제거하고 toss/es-hangul 라이브러리로 일원화했어요.
- **이메일·화면 문구의 조사(이/가, 으로/로)가 정확하게 붙습니다**: 역할 변경 안내나 이메일 제목처럼 회사 이름 뒤에 붙는 조사가 이전에는 괄호 표기(`이(가)`)로 처리되었는데, 이제 받침 여부를 자동으로 판단해 `토스페이먼츠가`, `한국정보통신이`처럼 올바른 형태로 나옵니다.

### Fixed

- **운영 환경에서 로그아웃 후 실제 도메인 로그인 화면으로 이동합니다**: `next start -p 3000`으로 실행 시 Next.js가 내부 hostname을 `localhost`로 채워 로그아웃 리다이렉트 URL이 `https://localhost:3000/login`이 되던 문제를 수정했습니다. 이제 Caddy가 전달하는 `Host` 헤더를 우선 사용해 실제 도메인(`supporter-b.com` 또는 `partner.supporter-b.com`)으로 올바르게 이동합니다.
- **로그인 후 외부 URL로 이동하는 오픈 리다이렉트 취약점 수정**: `/login?next=` 파라미터가 검증 없이 그대로 사용되어 외부 사이트로 리다이렉트될 수 있던 문제를 수정했습니다. 이제 내부 경로만 허용합니다.
- **심사 대기·이메일 인증 화면의 로그아웃 버튼이 일관된 방식으로 동작합니다**: 해당 화면들의 로그아웃도 `GET /logout` 단일 요청 방식으로 통일되었습니다.

## [0.2.2.2] - 2026-06-10

### Added

- **견적을 선정하면 축하 결과 화면으로 마무리를 알려줘요**: 구매사가 견적을 선정하면 조용히 화면만 바뀌던 대신, 선정한 PG와 이번 선정으로 좋아진 조건(수수료·정산주기·정산한도·보증보험)을 한눈에 보여주는 전체 화면으로 전환됩니다. 견적 요청이 끝났다는 걸 직관적으로 느낄 수 있고, 그 자리에서 바로 선정한 PG와 메시지를 시작하거나 견적 목록으로 돌아갈 수 있어요.

## [0.2.2.1] - 2026-06-09

### Fixed

- **로그아웃 후 로그인 화면으로 올바르게 이동합니다**: 로그아웃 버튼 클릭 시 세션 쿠키가 남아 있어 로그인 화면에서 홈으로 되튕기던 문제를 수정했습니다. 이전 방식(POST + 즉시 이동)은 서버 응답의 쿠키 삭제가 적용되기 전에 다음 요청이 전송되는 경쟁 조건이 있었습니다. 이제 GET /logout 단일 요청으로 쿠키 삭제와 리다이렉트가 한 번에 처리됩니다.

## [0.2.2.0] - 2026-06-09

### Changed

- **랜딩 CTA가 /rfp/new(견적 요청 작성)로 바로 이동합니다**: '무료로 시작하기' 버튼을 클릭하면 로그인 페이지를 거치지 않고 견적 작성 화면으로 직접 진입합니다.
- **/rfp/new 는 로그인(buyer 워크스페이스) 전용으로 변경됩니다**: 비로그인 유저가 진입하면 `/login?next=/rfp/new`로 리다이렉트되며, 로그인 후 자동으로 견적 작성 화면으로 돌아옵니다.
- **로그인 화면에 견적 맥락 안내 문구 추가**: `/rfp/new` 경유로 로그인 페이지에 도달한 경우 "견적을 시작하려면 가입하거나 로그인해요. 가입하면 바로 견적을 받을 수 있어요." 문구를 노출합니다.
- **신규 가입 후 견적 작성 화면으로 자동 복귀**: CTA → 가입 흐름으로 회원 가입을 마치면 기본 대시보드(/rfp) 대신 /rfp/new로 바로 이동합니다.

## [0.2.1.2] - 2026-06-09

### Fixed

- **제출 완료 화면에서 구간 요율 수단 수수료가 NaN%로 표시되던 문제 수정**: 카드·간편결제처럼 구간별 요율이 저장된 견적을 제출한 뒤 '견적을 보냈어요' 화면에서 각 구간별 요율(예: 카드 (영세) 0.50% · 카드 (일반) 1.80%)을 올바르게 표시합니다.
- **견적 템플릿 편집 시 구간 요율 데이터가 손실되던 문제 수정**: 구간별 요율이 저장된 템플릿을 설정에서 편집·저장하면 '일반' 구간 단일값으로 덮어써지던 버그를 수정했습니다. 이제 편집기에서 건드리지 않은 구간 요율은 원형 그대로 보존됩니다.

## [0.2.1.1] - 2026-06-09

### Added

- **견적 유형 필드에 ⓘ 설명 아이콘 추가**: 견적 요청 작성 2단계에서 '견적 유형' 항목 옆 ⓘ를 클릭하거나 마우스를 올리면 신규 계약·갱신 계약의 차이를 한 줄로 안내합니다.

### Fixed

- **로그아웃 버튼 클릭 즉시 화면이 전환됩니다**: 클릭 후 서버 응답을 기다리는 동안 화면이 멈추는 문제를 수정했습니다. 서버에 세션 종료를 요청하면서 동시에 로그인 화면으로 이동하며, 네트워크가 느린 환경에서도 세션이 올바르게 삭제됩니다.

## [0.2.1.0] - 2026-06-09

### Added

- **카드·간편결제 수수료를 가맹점 규모 구간별로 제안**: 견적을 작성할 때 국내 카드와 간편결제 수수료를 영세·중소1·중소2·중소3·일반 5구간(여신금융협회 우대수수료 기준)으로 묶어 한눈에 입력합니다. 수단마다 구간별 요율을 다르게 제안할 수 있고, 해외카드·계좌·기타 수단은 우대수수료 대상이 아니라 기존처럼 단일 요율로 받습니다.
- **구매사 비교 화면에 구간 선택**: 받은 견적을 비교할 때 구간(기본 ‘일반’)을 골라 그 구간 기준으로 PG별 요율을 나란히 비교하고 정렬합니다. 선택한 PG의 전체 구간 요율표도 읽기 전용 표로 함께 보여줍니다.
- **견적 템플릿이 구간 요율을 그대로 저장**: 자주 쓰는 구간별 요율표를 템플릿으로 저장해 다음 견적에 바로 불러옵니다.

### Changed

- 기존에 단일 요율로 저장된 견적·템플릿은 그대로 보이며, 모든 구간에서 같은 값으로 안전하게 표시됩니다(하위 호환). 데이터베이스 구조 변경은 없습니다.

## [0.2.0.1] - 2026-06-09

### Added

- **PG 랜딩 헤더 내비게이션**: PG 전용 랜딩(`partner.supporter-b.com`)에도 구매사 랜딩과 동일한 고정 헤더(로고 + 로그인 진입)를 넣어, PG 사용자가 랜딩에서 바로 로그인할 수 있습니다.

## [0.2.0.0] - 2026-06-09

### Added

- **PG사 전용 도메인 `partner.supporter-b.com`**: PG(결제대행사) 사용자는 이제 `partner.supporter-b.com`에서, 구매사 사용자는 `supporter-b.com`에서 각자 자기 화면을 봅니다. 한 번 로그인하면 두 도메인에서 로그인이 공유되어, 양쪽 워크스페이스를 가진 사용자가 워크스페이스를 전환하면 알맞은 도메인으로 자동 이동하면서도 로그인 상태가 그대로 유지됩니다.

### Changed

- **자기 도메인으로 자동 정리**: PG 계정으로 구매사 도메인에 들어오면(또는 그 반대) 자동으로 올바른 도메인으로 이동합니다.
- **이메일 링크가 알맞은 도메인을 가리킴**: 견적 요청 초대 메일과 채팅 알림 메일의 링크가 받는 사람(PG·구매사)에 맞는 도메인으로 연결됩니다.
- 워크스페이스 전환으로 도메인이 바뀔 때는 전체 페이지 이동으로 처리해 사이드바·헤더가 항상 올바르게 표시됩니다.

## [0.1.9.1] - 2026-06-08

### Added

- **금액 입력 시 천단위 콤마 자동 표시**: 정산한도·보증보험 등 원화 금액 입력 필드에서 숫자를 입력하면 `1,000,000` 형태로 자동 포맷됩니다. 소수점 입력은 차단되고(`decimalScale=0`), 실제 저장·제출 값은 콤마 없는 순수 숫자입니다.

### Fixed

- **견적 작성 위저드 — 무시 후 폼 수정 시 자동저장이 재개됩니다**: 이전 버전에서 '무시' 클릭 후 내용을 수정해도 초안이 저장되지 않는 버그를 수정했습니다.
- **견적 작성 위저드 — 템플릿 적용 시 초안 복원 배너가 닫힙니다**: 이전 버전에서 템플릿을 선택해도 '이전 작성 내용이 있습니다' 배너가 남아 있던 버그를 수정했습니다. 이제 템플릿 적용 시 기존 초안도 함께 삭제됩니다.

## [0.1.9.0] - 2026-06-08

### Changed

- **서비스 레이어 전면 도입(Phase 2)**: 채팅(`ChatService`), 워크스페이스(`WorkspaceService`), 인증(`AuthService`), 알림(`NotificationService`) 4개 서비스 클래스를 추가하여 Actions → Services → Repositories 3계층 구조가 전체 코드베이스에 적용됩니다. 서버 액션은 세션 검증·입력 파싱만 담당하고, 트랜잭션·이메일 아웃박스·알림 팬아웃은 서비스가 캡슐화합니다.
- **ChatService**: 메시지 전송(`sendMessage`) 및 대화 읽음 처리(`markConversationRead`) — 아웃박스 enqueue, 인앱 알림 다이제스트 소유. Centrifugo 발행은 post-commit best-effort로 액션에 잔류.
- **WorkspaceService**: 워크스페이스 생성(`createWorkspace`), 멤버 초대/수락/재발송/취소(`inviteMember`·`acceptInvite`·`resendInvite`·`cancelInvite`), 역할 변경(`changeMemberRole`), 멤버 제거(`removeMember`) — 초대 이메일 아웃박스·인앱 알림 팬아웃 소유.
- **AuthService**: 회원가입(`completeSignup`·`signupViaInvite`), 계정 삭제(`deleteAccount`), 비밀번호 재설정(`requestPasswordReset`·`resetPassword`), 이메일 변경(`requestEmailChange`·`confirmEmailChange`) — 인증 토큰 발급·consume·아웃박스 소유.
- **NotificationService**: 알림 읽음 처리(`markRead`·`markAllRead`), 이메일 재발송 enqueue(`retryEmail`) — outbox 상태 reset 소유.

## [0.1.8.0] - 2026-06-08

### Changed

- **서비스 레이어 도입(Phase 1)**: 선정(`awardRfpAction`), 취소(`cancelRfpAction`), 마감(`closeRfpAction`), 제안 철회(`withdrawBidAction`) 등 핵심 비즈니스 로직이 `RfpService`·`BidService` 클래스로 분리됩니다. 서버 액션은 세션 검증·입력 파싱 후 서비스에 위임하는 얇은 레이어로 변경되며, 트랜잭션 관리·알림 발송·이메일 큐는 서비스가 캡슐화합니다.
- **선정 로직 개선**: 존재하지 않는 bid ID를 선정 시도하면 FK 위반 에러 대신 명확한 `WINNING_BID_NOT_FOUND` 오류를 반환합니다.
- **`awardRfpAction` 입력 검증 강화**: `rfpId` 필드가 UUID 형식으로 검증됩니다. 잘못된 형식 전달 시 즉시 `INVALID_INPUT` 오류를 반환합니다.

### Fixed

- **취소·마감 액션 세션 없음 처리**: `cancelRfpAction`·`closeRfpAction` 호출 시 세션이 없으면 `FORBIDDEN_BUYER`를 반환합니다.

## [0.1.7.3] - 2026-06-08

### Changed

- **RFP 홈페이지 주소 입력 개선**: 구매사가 사업 운영 홈페이지를 입력할 때 `https://` 스킴 없이 `example.com` 형태로 입력해도 유효한 도메인으로 인정됩니다. 입력 필드에서 포커스가 벗어나면 자동으로 `https://`가 앞에 붙어 저장됩니다. 유효하지 않은 TLD(`example.invalidtld`)나 localhost, 단순 문자열은 여전히 거부됩니다.

## [0.1.7.2] - 2026-06-07

### Added

- **견적 계약 유형(신규/갱신) 선택**: 구매사가 새 견적 요청 작성 위저드(Step 2)에서 '신규 계약' 또는 '갱신 계약'을 선택할 수 있습니다(선택 사항). 선택한 유형은 PG사 오픈 게시판(`/opportunities`), 받은 견적함 목록(`/inbox`), 견적 상세 패널에 Chip으로 표시돼 PG사가 계약 성격을 한눈에 파악할 수 있습니다.
### Changed

- **소셜 카드 문구 업데이트**: OG 이미지와 사이트 제목·설명이 'Supporter B — PG사 비교 견적 플랫폼 / 다수의 PG사의 견적을 비교하여 최적의 견적을 받아보세요'로 갱신됩니다. 카카오·슬랙·SNS 공유 시 새 문구로 노출됩니다.

## [0.1.7.1] - 2026-06-07

### Changed

- **홈 새로고침 버튼 애니메이션**: 새로고침 버튼 클릭 시 아이콘이 0.35초 동안 한 바퀴 빠르게 회전해 즉각적인 시각 피드백을 제공합니다. 새로고침이 오래 걸릴 경우 이후 지속 회전으로 자연스럽게 이어집니다. 클릭 중 버튼은 비활성화돼 중복 요청을 방지합니다.

## [0.1.7.0] - 2026-06-07

### Fixed

- **메시지 컴포저 레이아웃 정렬**: 메시지 입력창, 파일 첨부 버튼, 전송 버튼이 올바르게 배치되지 않던 문제를 수정. 입력창과 전송 버튼의 높이를 동일하게 맞추고 스크롤 영역이 화면 전체를 채우도록 flex 레이아웃을 개선.
- **파일 첨부 아이콘 통일**: 채팅 컴포저의 파일 첨부 버튼 아이콘을 프로젝트 표준 아이콘 라이브러리(lucide-react)로 교체. 나머지 버튼들과 획 굵기·스타일이 일치해 시각적으로 통일감 있는 컴포저 UI를 볼 수 있습니다.
- **메시지 전송 네트워크 오류 복구**: 전송 중 네트워크 오류가 발생하면 전송 버튼이 영구적으로 비활성화되던 문제를 수정. 오류 발생 시 작성 중인 메시지와 첨부파일을 복원하고 재시도가 가능하도록 개선.

## [0.1.6] - 2026-06-07

### Added

- **견적 요청 홈페이지 URL 형식 검증**: 새 견적 요청(`/rfp/new`) 작성 위저드 Step 2의 '사업 운영 홈페이지'(`websiteUrl`) 입력에 도메인 형식 유효성 검증을 추가. 선택 필드라 빈 값은 그대로 허용하되, 값을 입력하면 `http(s)://` 스킴 + 점(.)+TLD 도메인 형태여야 한다. 형식이 어긋나면 입력 즉시 인라인 안내(`role="alert"`)를 보여주고 발송을 막는다(위저드 Step 2 미완료 처리 + 서버 `createRfpAction` zod 검증). `https://trusted.com@evil.com`처럼 표시 host와 실제 목적지가 다른 userinfo 형태도 거부 — 홈페이지가 오픈 게시판에 링크로 노출되므로 피싱을 차단한다.

## [0.1.5] - 2026-06-07

### Fixed

- **견적 요청 위저드 첨부 파일 빈 스크롤**: 새 견적 요청(`/rfp/new`) 작성 위저드에서 폼·버튼 아래로 거대한 빈 흰 화면이 생기고 그 영역까지 스크롤되던 버그 수정. 원인은 숨김 파일 input이 쓰던 Tailwind `sr-only` 클래스(`position:absolute`)가 긴 폼 안에서 스크롤 컨테이너의 클립을 벗어나 문서 높이를 늘린 것 — `display:none`이 아니라 absolute 박스라 레이아웃에 1px이 남아 문서가 늘어났다. 프로그래밍(`ref.click()`·`<label>`)으로만 열리고 별도 가시 트리거가 있는 숨김 파일 input 5곳을 `hidden`(`display:none`)으로 교체. 첨부 드롭존(`RfpAttachmentDropzone`)·입찰 위저드 견적서 업로드·워크스페이스 로고·메시지/채팅 첨부에 동일 패턴 일괄 적용.

## [0.1.4] - 2026-06-07

### Changed

- **홈 새로고침 버튼 타임스탬프**: 페이지 진입 즉시 "방금 전" 등 마지막 갱신 시각이 표시됩니다. 기존에는 버튼을 한 번 클릭해야만 시각이 나타났습니다.
- **새로고침 아이콘**: lucide-react 표준 `RefreshCw` 아이콘으로 교체. 클릭 시 아이콘이 회전하는 애니메이션은 그대로 유지됩니다.

## [0.1.3] - 2026-06-07

### Fixed

- **견적 요청 위저드 사이드바 스크롤**: 새 견적 요청(`/rfp/new`) 작성 위저드에서 좌측 단계 네비게이션(사업자 확인·견적 내용·PG 선택·보내기 확인) 위에 마우스를 올려 스크롤하면 폼이 아니라 화면 전체가 스크롤되던 버그 수정. 스크롤 컨테이너를 위저드 루트로 통일하고 좌측 사이드바를 sticky로 고정해, 좌·우 어느 영역에서 스크롤해도 견적 내용 영역과 동일하게 폼만 스크롤되도록 변경.

## [0.1.2] - 2026-06-07

### Fixed

- **채팅 첨부파일 ACL**: `canAccessAttachment`에서 `chatMessageId` 분기가 누락되어 상대방이 파일을 다운로드 시 403이 발생하던 버그 수정. 대화 양쪽 워크스페이스(buyer·PG) 모두 허용.
- **채팅 실시간 첨부 표시**: `publishChatEvent` 페이로드에 `attachments` 필드가 없어 상대방이 메시지를 실시간으로 받았을 때 첨부 타일이 렌더되지 않던 버그 수정 — 페이로드에 파일 메타데이터 포함.
- **ThreadView `onMessage` 핸들러**: 수신된 실시간 메시지의 `attachments`를 항상 빈 배열로 덮어쓰던 버그 수정 — 페이로드의 `attachments` 값을 실제로 사용하도록 변경.

## [0.1.1] - 2026-06-07

### Removed

- **공유 링크 기능 완전 제거**: 견적 요청 상세 페이지에서 '공유 링크' 섹션(URL 입력 + 복사 버튼)이 제거됨.
  PG 초대는 워크스페이스 직접 초대 또는 오픈 게시판 콜드 피치 경로만 사용.
- `/share/rfp/[token]` 공개 라우트 삭제 — 토큰 기반 비인증 접근 경로 완전 제거.
- `claimShareTokenAction` 서버 액션 삭제.
- RFP 생성 시 `share_token` 자동 발급 중단. DB 컬럼은 하위 호환 보존.
