/**
 * `target="_blank"` 링크의 새 탭 고지 — 단일 출처.
 *
 * 새 탭이 열리는 건 사용자가 요청하지 않은 맥락 변경이라 링크가 미리 알려야 한다
 * (WCAG 2.4.4 / G201). 이 프로젝트의 규칙은 링크 라벨의 성격으로 갈린다:
 *
 * - **라벨이 목적지 이름**(파일명·URL·문서명)이면 → 이 상수를 `sr-only` 로 링크 텍스트에
 *   넣어 접근성 이름에만 싣는다. 시각적으로는 아이콘·밑줄이 이미 affordance 를 준다.
 * - **링크의 목적 자체가 "새 창으로 열기"** 면 → 라벨에 그 문구를 그대로 쓴다.
 *   `BidPdfPane` · `AttachmentPreviewList` 의 보이는 `새 창 열기 →` 가 그 경우다.
 *
 * 문구를 바꾸면 모든 호출부에 동시에 반영된다 — 개별 컴포넌트에 리터럴을 복제하지 말 것.
 */

/** 새 탭에서 열리기만 하는 링크(외부 URL·첨부 미리보기 등). */
export const NEW_TAB_NOTICE = '새 탭에서 열려요';

/** 새 탭에서 파일이 내려오는 링크(전자서명 완료본 등). */
export const NEW_TAB_DOWNLOAD_NOTICE = '새 탭에서 내려받아요';
