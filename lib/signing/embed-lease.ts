/**
 * 발송 임베드 리스의 시간 상수 — 서버(리스 길이)와 화면(하트비트 주기)이 **같은 곳**에서
 * 읽는다.
 *
 * 둘의 관계가 곧 계약이다: 하트비트가 리스보다 충분히 짧아야 백그라운드 탭에서 브라우저가
 * 타이머를 조여 몇 번을 놓쳐도 리스가 살아 있다. 이 관계가 깨지면 작업 중이던 PG 의
 * 임베드가 조용히 닫히고 올려둔 계약서가 사라진다 — 유닛 테스트로는 드러나지 않는 종류라
 * `__tests__/embed-lease.test.ts` 가 비율을 못박는다.
 */

/** 서버가 잡는 발송 리스 길이. 이 시간 동안 다른 담당자는 임베드를 열 수 없다. */
export const EMBED_SEND_LEASE_MS = 5 * 60_000;

/** 패널이 열려 있는 동안 화면이 리스를 연장하는 주기. */
export const EMBED_HEARTBEAT_MS = 60_000;
