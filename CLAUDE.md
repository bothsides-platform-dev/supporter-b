# CLAUDE.md

This file provides guidance to coding agents when working with code in this repository — Claude Code (claude.ai/code) reads it directly, Codex reads it through the `AGENTS.md` symlink.

## Documentation Map

This file is the agent entry point — **`AGENTS.md` is a symlink to this file** (relative target, mode `120000`), not a second copy, so Codex and Claude Code read the same bytes. **지침은 여기서만 고친다**: `AGENTS.md` 를 직접 편집·생성하지 마라 — 제자리 쓰기는 이 파일을 덮어쓰고, 원자적 저장(임시파일→rename)은 심링크를 일반 파일로 바꿔 사본을 되살린다. `lib/__tests__/agent-entrypoint.test.ts` 가 그 두 경우를 잡는다. The live content docs:
- `CLAUDE.md` (this file) — stack, routing, design hard-rules, TDD rules, skill routing.
- `README.md` — local setup / run instructions.
- `DESIGN.md` — Linear design language: tokens, typography, color, component visual rules.
- `SCREEN_DESIGN.md` — screen IA, route map, per-screen UX spec.
- `UX_WRITING.md` — 토스 보이스톤 기반 UX 라이팅 원칙 (해요체·능동형·긍정형·캐주얼 경어·버튼 문구). UI 문구 작성 시 필수 참조.
- `docs/THREAT_MODEL.md` — 위협 모델·수용 리스크 대장 (AR-N; 실시간/presence 포함). 각 항목의 규범은 링크된 가드 테스트가 SSOT — 신뢰 경계를 바꾸는 변경은 같은 PR 에서 해당 절을 갱신한다.
- `docs/SNOWSIGN_API.md` — 스노우싸인(SnowSign) Public API 레퍼런스 원문 사본(엔드포인트·요청/응답 스키마·에러코드·rate limit). 외부 서비스 스펙이지 이 레포의 스펙이 아니다 — 실제 연동 코드는 `lib/server/signing/`·`ContractSigningService`(위 "선정 후 전자서명" 절).
- `TODOS.md` — 미해결 부채 대장 (영역별 P1~P4). 해결분은 지우지 않고 `~~취소선~~ — 해결 (vX.Y.Z.W)` 로 남겨 결정 이력을 보존한다.
- `CHANGELOG.md` — 릴리스별 사용자 관점 변경 기록. 버전은 `VERSION`·`package.json` 과 항상 같은 값이어야 한다.

> ⚠️ **Codex 로 이 파일을 읽는 경우 — 잘렸는지 먼저 확인하라.** Codex 는 프로젝트 문서를 `project_doc_max_bytes`(기본 32KiB)에서 **조용히 잘라서** 주입한다(잘렸다는 표시가 없다). 이 파일은 80KB 를 넘어 기본값이면 앞 38% 정도만 들어오고 Stack·라우팅·**디자인 하드룰·TDD 하드룰**·워크트리 규칙이 통째로 사라진다. 판별법(문자열 검색이 아니라 **끝나는 위치**로 판단하라): 온전한 이 파일은 `## Worktree Workflow` 절, 그리고 마지막 줄 `<!-- CLAUDE.md: END -->` 로 **끝난다**. 지금 읽고 있는 내용이 그 표식으로 끝나지 않으면 잘린 것이다 — 그때는 **`cat "$(git rev-parse --show-toplevel)/CLAUDE.md"` 로 전문을 다시 읽고 나서** 작업을 시작하라(하위 디렉터리에서 시작했으면 상대경로 `cat` 은 파일을 못 찾는다). 그 출력마저 도구 출력 상한에 걸려 **가운데가** 잘리면 꼬리의 센티널은 남으므로, 한 번에 다 못 읽었으면 `sed -n '1,60p'` → `sed -n '61,120p'` 처럼 앞에서부터 나눠 읽어라. 항구적 해결은 `~/.codex/config.toml` 에 `project_doc_max_bytes = 262144`(저장소 안 `.codex/config.toml` 은 무시된다 — 실측).

**라이브 배포**: AWS Lightsail 단일 VM 자체호스팅 (Caddy + PM2 `next start` + Docker Postgres). 현행 런북은 `docs/DEPLOY_LIGHTSAIL.md`, 관련 자산은 `ecosystem.config.cjs`(PM2) · `docker-compose.prod.yml`(운영 Postgres) · `deploy/Caddyfile` · `scripts/deploy/lightsail-*.sh` · `.env.production.example`.

**Historical / NOT current truth** (verify against code before trusting): `docs/superpowers/**` (point-in-time plan & spec artifacts). The legacy `PG_RFP_SPEC.md` / `SPEC.md` docs were **removed** — do not reference them; canonical product rules now live in code + tests + SCREEN_DESIGN.md 의 "확정 결정" 블록 (Context 절).

## Domain Context (memorize)

- **Two-sided platform**: `buyer` workspace (구매사) sends RFPs; `pg` workspace (결제대행사 영업담당) responds with bids
- **Bidding is sealed 1:N; discovery is open (opt-out)**: participation (who can bid) is buyer-controlled via the workspace-ID allowlist (`rfp_allowed_pg`), and **bids stay sealed — PGs never see each other or a competitor count (`Bid.competitorCount` does not exist by design).** Two axes, kept separate:
  - **Discovery = open by default, buyer opt-out.** Every `sent` RFP with `deadline > now` and `board_visible=true` (the default) appears on the PG-facing open board (`/opportunities` + PG home 탐색 section). The board listing exposes a **non-competitive whitelist — `견적번호`·`구매사명`(workspace name)·`제목`·`홈페이지`·`마감일`·`요청 결제수단`(+커스텀 라벨)·`주요 상품`·`계약 유형`** — and never fees/current-terms/volume/bizNo/memo/attachments. The whitelist is the `OpportunityListing` type (`lib/types/pg-request.ts`), enforced at the query layer by an explicit SELECT projection in `lib/server/repositories/drizzle/rfp-pg-request.ts` and pinned by an exact-key guard test in its `__tests__`. **Adding a field to that type widens the public boundary — treat it as a sealed-bid decision, not a UI tweak.** **This block is the only place the field list is spelled out in prose** — README.md and SCREEN_DESIGN.md deliberately point here instead of restating it (they drifted apart when all three carried copies). **Board visibility is set once at RFP creation (wizard step 4, `RfpStep4Review` checkbox — gated on `OPEN_BOARD_ENABLED`) and is read-only thereafter** — the UI shows a status chip (`RfpBoardVisibilityStatus`) with no toggle. `setRfpBoardVisibilityAction`/`rfpRepo.setBoardVisible` are kept server-side for admin/recovery use but no UI surface calls them. **Kill switch**: `OPEN_BOARD_ENABLED = false` in `lib/features/open-board.ts` temporarily hides the entire open-board surface (board page, nav, home 탐색 section, wizard checkbox). Data and server logic are intact — flip to `true` to restore. (v0.2.52.0)
  - **Participation = buyer-gated.** A non-invited PG sends a one-time cold-pitch request (`rfp_pg_requests`, UNIQUE per (rfp, pg), rejection permanent). Buyer **accept** adds them to the allowlist + a real invitation (full info then visible in their inbox); **reject** is final.
  - **구매사 PG 피커에 무엇이 뜨는가 (v0.4.53.0)**: 견적요청 위저드 3단계와 발송 후 PG 추가 초대는 둘 다 `searchWorkspaces` → `DrizzleWorkspaceRepository.search()` 한 곳을 지난다. 그 조회는 **`status='active'` 만** 돌려주고(이전에는 조건이 `type` 하나뿐이라 셀프서비스 생성 기본값인 **심사 대기 워크스페이스가 모든 구매사에게 노출**됐다), 여기에 더해 **이름에 `test`/`테스트` 가 든 워크스페이스를 기본 제외**한다 — 내부 테스트용 PG 를 실사용자에게 숨기기 위함이고, 규칙 단일 출처는 `lib/features/test-pg.ts` 의 `TEST_PG_NAME_TOKENS` 다(TS 술어 `isTestPgName` 과 SQL `notIlike` 가 같은 배열에서 파생되며 PGlite 토큰 루프 테스트가 드리프트를 잡는다). **필터는 앱이 아니라 SQL 에 있다** — `limit(q?20:500)` 이 필터 이후에 걸려야 결과 개수가 줄지 않기 때문. 숨김 해제는 **콘솔 쿠키** `document.cookie='support-b-show-test-pg=1; path=/'` + 새로고침이며(끄기는 `max-age=0`), 서버(RSC 는 `cookies()`, 라우트는 `request.cookies`)가 직접 읽으므로 기본 상태에서는 숨긴 이름이 RSC 페이로드에도 JSON 응답에도 실리지 않는다. **보안 경계가 아니라 가시성 조정이다** — 쿠키 이름을 아는 사람은 누구나 켜며(`/login/ops` 의 숨은 URL 과 같은 성격), 실제 경계는 라우트의 buyer+`type=pg` 게이트다. 두 축은 **직교**한다: 쿠키를 켜도 `pending` 은 나오지 않는다. 부분 문자열 매칭이라 상호에 토큰이 우연히 든 정상 PG 도 조용히 숨겨지는 오탐은 **수용된 트레이드오프**다. 범위는 발견·선택 표면뿐 — 딜룸·채팅·알림·이메일의 PG 이름 표기는 `search()` 를 지나지 않아 그대로다. 쓰기 경로(`filterPgIds`·`createRfp`)는 아직 status 를 보지 않는다(UI 로는 도달 불가, TODOS.md P3).
  - **Per-field opt-out for invited PGs — `현재 카드 수수료`.** Even an invited PG who sees the full brief can be denied one field: the buyer's **current card fee** (default shown, opt-out per RFP). When off, the value is stripped server-side in `loadPgRfpDetail` (the PG never reads it from the RSC payload/network — `RfpBriefPanel`'s render gate is only the visual fallback). The buyer's own comparison baseline always keeps the fee. Toggle lives in the RFP create wizard (step 2, under 현재 카드 수수료). **Implementation note (v0.2.26.x, migration complete):** current-terms is stored as a versioned JSONB document (`current_terms` column, `lib/types/rfp-terms.ts`). PG field visibility is controlled by a `hidden_from_pg` text-array (path allowlist) — `loadPgRfpDetail` strips any path that has a handler in `PG_STRIP` (fail-closed). The legacy `current_fee_visible_to_pg` boolean column and individual `current_*` columns were dropped in v0.2.26.2 — `current_terms` is the sole authoritative store. `currentFeeVisibleToPg` is derived at the app layer from `hiddenFromPg`. User-facing behavior is unchanged. **New-contract exemption (v0.2.68.0):** when 견적 유형 = 신규 계약 (`contractType==='new'`), the PG-contract-history fields that can't exist for a first-time PG contract — 전년도 연간 PG 총 거래액(`annualPgVolume`), 현재 카드 수수료(`currentFeeRate`) + its PG-visibility, 현재 월 정산한도, 현재 보증보험, 현재 정산주기 — are hidden in both wizard step 2 (input) and step 4 (review) and stripped server-side in `createRfpAction` (trust boundary — a tampered draft/direct call still can't leak them into `current_terms`). The previously-required `annualPgVolume` is no longer required for `'new'` — the SSOT gate is `isAnnualPgVolumeSatisfied` in `lib/rfp/required-fields.ts`. 배송·서비스 기간 and 현재 운영 솔루션 are KEPT (PG-independent). Renewal/null behavior unchanged.
- **Per-RFP unique URL + token** in invitation email; token authoritative only for first entry, then workspace membership takes over
- **용어 주의 — 코드는 `RFP`/`bid`, 사용자 화면은 '견적' 언어**: 코드 식별자·라우트(`/rfp`)·DB(`rfps`/`bids`)는 영어 그대로지만, **사용자에게 보이는 모든 한국어 문구는 '견적 요청'(RFP)·'견적'(bid)·'선정'(award)** 으로 통일한다. UI 문구 작성·수정 시 `UX_WRITING.md` §8 도메인 용어집을 따른다. 랜딩/마케팅 면만 '경쟁 입찰' 프레이밍 유지.
- **선정 후 전자서명 (SnowSign 건별 임베드)**: 선정(award)에서 흐름이 끝나지 않고 서포트비 안에서 전자서명까지 잇는다. **재사용 계약서 템플릿이 있다 (v0.4.37.0 폐지 → PR#470 재도입, 자체 PDF 에디터 — v0.4.56.0 재활성화)** — **Kill switch: `CONTRACT_TEMPLATES_ENABLED` in `lib/features/contract-templates.ts`, 현재 `true`.** `false` 로 뒤집으면 계약서 템플릿 표면 전체(사이드바·⌘K 팔레트·`G`→`C` 단축키, `/contract-templates` 페이지 → 준비중 안내, 견적 작성 4단계 템플릿 피커, 딜룸 계약 탭의 `연결된 템플릿으로 보내기` 지름길)가 숨는다. 게이트는 4곳뿐이다 — `lib/nav/nav-config.ts` 의 pg `top` 배열, 그 페이지와 `loading.tsx`(꺼진 동안 목록 스켈레톤 대신 헤더만), `PgDealRoomBody`(위저드 prop 과 딜룸 지름길이 여기서 함께 끊긴다). 하류 컴포넌트는 입력이 비면 스스로 사라진다. `lib/features/__tests__/contract-templates-flag.test.ts` 드리프트 가드는 **등록된 게이트가 사라지는 것만** 잡는다 — 새 표면을 추가하면 SURFACES 갱신은 사람이 해야 한다. **오픈게시판과 같은 UI-only 차단** — 데이터·서버 액션·PDF 프록시 라우트(`/api/signing/templates/{id}/document`)는 플래그와 무관하게 그대로다. 주의 둘: ① `PgDealRoomBody` 는 꺼진 상태에서 위저드에 `undefined` 를 넘겨야 한다 — `[]` 는 truthy 라 '저장된 템플릿이 없어요' 안내 카드가 남는다. ② 다시 끌 때는 off-branch 회귀 테스트 복원 절차가 있다(플래그 파일 주석 참조 — v0.4.56.0 이 삭제한 `*.contract-templates.test.*` 3파일과 `vi.mock(...true)` 2곳). 기본 경로는 여전히 건별 임베드다: PG 가 선정된 딜룸의 계약 탭에서 스노우싸인 임베드(iframe)를 열어 **건마다** 자사 계약서 PDF 를 올리고 서명칸을 배치해 그 자리에서 발송하며, 이 경로에서 앱은 PDF 도 좌표도 저장하지 않는다. 여기에 더해 PG 는 `/contract-templates` 에서 **자체 PDF 서명칸 배치 에디터**(pdf.js + react-rnd)로 템플릿을 미리 등록할 수 있다 — 브라우저가 스노우싸인에 직접 PDF 를 올리고(`POST /v1/uploads`, purpose=template_document) 서버 액션이 `POST /v1/templates` 를 호출하며(`signers` 는 항상 `['구매사','PG사']` 고정 — 역할 매핑 단계 없음, 라벨은 `SIGNING_ROLE_LABELS` 단일 출처, **각 역할에 `security_method:'easy_cert'` 를 심는다** — v0.4.46.0 본인인증 기본강제), 우리 DB 에는 링크 행(`pg_signing_templates`: workspaceId·snowsignTemplateId·name)만 남는다. **등록한 템플릿은 확인·수정도 된다 (v0.4.43.0)** — 목록의 `수정` 이 `GET /v1/templates/{id}`(서명칸 좌표 되읽기 — 쓰기 `role` ↔ 읽기 `role_name` 비대칭, 미지 role/type·변수 실린 템플릿은 `TEMPLATE_UNSUPPORTED` fail-closed: 조용히 버리고 저장하면 그 필드가 소실된다)와 `GET /v1/templates/{id}/download`(JSON 봉투 → 1시간 presigned URL)를 소비해 에디터를 기존 상태로 연다. PDF 는 스트리밍 프록시(`/api/signing/templates/{id}/document` — 302 가 아닌 이유: 소비자가 에디터의 fetch 라 S3 CORS 에 종속되면 안 되고, 같은 바이트가 재업로드에도 쓰인다. PG 승인 게이트 + 실바이트 50MB 캡 + 15s 데드라인)가 중계한다. **수정 저장 = 재생성 후 링크 행 교체**: SnowSign 에 템플릿 수정 API 가 없어(PATCH/PUT/DELETE 전무) 새 업로드 → 새 `POST /v1/templates` → 행의 `snowsignTemplateId` 를 in-place UPDATE 한다(행 유지 = `bids.signing_template_id` 보존 — **수정하면 그 템플릿을 골라 둔 기존 견적의 발송에도 새 판이 쓰인다**, 그것이 수정의 목적). 업로드는 저장 시점에 처음 나가고(deferred — 배치 작업이 세션 TTL 10분에 안 잘림) 같은 바이트 재시도는 세션을 재사용한다(조직 공유 3슬롯 보호). 옛 provider 템플릿은 무해한 고아(삭제 API 도 없음). **서식에는 종류가 둘이다 — `kind='pdf'`(위 경로)와 `kind='composed'`(조항형, v0.4.57.0)** 이며 한 테이블(`pg_signing_templates`)이 `kind` 로 갈리는 **판별 유니온**을 담는다(별도 표로 나누지 않은 이유: `bids.signing_template_id` 링크가 둘로 갈라지고 견적 피커가 두 목록을 합쳐야 한다 — 컬럼 하나가 싸다). 불변식은 주석이 아니라 **DB CHECK** 가 지킨다(`pdf` ⇒ provider id 있고 document 없음 / `composed` ⇒ 그 반대). 조항형은 **문서가 우리 DB 에 있다**(`document` jsonb, 버전드 `ContractDocV1` — 조항 배열 + 전문 + 말미문언, 제N조 번호는 저장하지 않고 렌더 시 index 로 매긴다). PG 는 `/contract-templates` 에서 조항을 직접 쓰고(리치텍스트 없음 — 조항 배열 + textarea 가 곧 구조), 본문의 `{{토큰}}` 은 발송 시 낙찰 견적 값으로 치환된다(`lib/contract-doc/variables.ts` 가 토큰 단일 출처 — 미등록 토큰은 저장 시 fail-closed 거부). **조항 제목의 토큰은 등록된 것이라도 거부한다** — 제목은 치환 대상이 아니어서(목차가 딜마다 달라지면 조항을 특정할 수 없다) 통과시키면 `{{계약일}}` 이 그대로 인쇄된 계약서가 서명된다. 수수료는 텍스트가 아니라 `feeTable` 블록이며 `Bid.paymentFees` 에서 등급별 구간을 전부 인쇄한다(한 등급만 박으면 계약 기간 중 등급이 바뀌는 순간 사실과 어긋난다). 발송은 `sendComposedContract` — 서버가 **PDF 를 직접 렌더해**(pdf-lib + `@pdf-lib/fontkit`) 업로드하고 `POST /v1/contracts` 로 건별 계약을 만든다(공급자 템플릿을 만들지 않는다 — SnowSign 템플릿 변수는 고정 레이아웃 위 고정 박스라 흐르는 조항 본문을 재배치하지 못한다). **서명칸 좌표는 사람이 찍지 않는다 — 레이아웃 엔진이 뱉는다**(`lib/contract-doc/layout.ts` 는 순수 함수라 폰트 없이 테스트되고, 공급자 좌표계(좌상단 원점)로 계산해 `render-pdf.ts` 한 곳에서만 뒤집는다). **폰트 함정 셋**(전부 실측): ① OTF 금지 — Pretendard `static/*.otf` 는 CFF 라 fontkit 이 `CFFGlyph._getPath` 에서 죽는다, TTF 는 `static/alternative/` 에 있다. ② `cmapOnlyFontkit` 프록시 필수 — pdf-lib 의 ToUnicode 는 `glyphForCodePoint` 로, 그리기는 `layout()` 로 만들어지는데 Pretendard 의 **필수** GSUB 가 숫자 옆 `-` 를 이형자로 바꿔 사업자번호가 `123㏄45㏄67890` 으로 추출된다(피처 배열로 못 끈다). ③ **`subset:false` 필수** — 서브셋은 한글 외곽선을 통째로 날리는데 **pdfjs 텍스트 추출은 그래도 통과한다**(ToUnicode + 콘텐츠 스트림만 읽는다). 즉 추출 통과는 렌더 성공의 증거가 아니며, 진짜 가드는 PDF 에 박힌 폰트를 되꺼내 한글 글리프 bbox 를 보는 테스트다. **글리프 커버리지는 두 지점에서 본다** — 저장 시(조항 텍스트)와 발송 시(치환된 문서 + **수수료 표 라벨** + 사업자번호). 발송 시가 따로 필요한 이유: 구매사 상호·커스텀 결제수단 라벨은 발송 시점에야 들어오고 Pretendard 는 한자를 담지 않아, 빠뜨리면 **서명된 계약서에 조용한 빈칸**이 남는다(라벨은 구매사 자유 입력이라 보내는 PG 가 고칠 수도 없다). **인증 정책은 템플릿 경로와 같다 — 차단이다**(사용자 결정 2026-08-17 이 08-08 의 "compose 는 강등"을 뒤집었다. 근거는 `lib/signing/security-method.ts` 주석이 단일 출처). **초안은 재사용하지 않는다** — 문서가 우리 DB 에 있어 언제든 다시 만들 수 있고, 재사용은 편집 후 옛 본문 발송이라는 v0.4.52.0 이 막은 사고를 되살린다(프로브-후-폐기: 발송됨이면 바인딩하고 종료 / 초안이면 `clearDraftRefIf` 로 지우고 새로 / **프로브 실패면 ref 를 보존한 채 중단**). ⚠️ **조항형 계약에는 서명 마감이 없다** — `deadline_days` 가 `POST /v1/contracts` 에서 201 로 수락된 뒤 조용히 무시되어(S6 실측) `expired` 에 도달할 수 없다. 템플릿 경로에는 마감이 있으므로 **두 경로의 수명이 다르다**. 마감을 흉내내지 않고(거짓 약속 금지) **관측으로 덮는다** — 진행 카드가 마감 줄과 같은 자리에서 `보낸 지 N일째`(`ElapsedDays`, 상호배타)를 띄우고, 폴러의 `notifyStaleSent` 가 30일(`STALE_SENT_AFTER_DAYS` — 템플릿 마감 상수에서 파생) 넘게 열린 계약을 운영자 슬랙으로 알린다(재알림 7일, **자동 취소 없음**). 스로틀 마커는 전용 컬럼 `stale_notified_at` 이다 — `last_polled_at` 은 폴러가 2분마다 전진시켜 못 쓰고 `last_reminded_at` 은 사용자 리마인더 쿨다운과 충돌한다. 미리보기는 `POST /api/signing/templates/preview` 가 **같은 렌더러**로 만든 실제 PDF 다(저장 안 한 편집 상태를 그려야 해서 GET 이 아니라 POST) — `/api` 는 프록시 매처 밖이라 인라인 게이트(`isPgMembershipBlocked` 포함)가 유일한 게이트이고, 문서 크기 상한(UTF-8 바이트)과 사용자별 렌더 예산(`preview-rate-limit.ts`)이 단일 fork 의 CPU 를 지킨다. **배포는 DDL 선행이다** — `TEMPLATE_COLUMNS` 명시 projection 이 새 컬럼을 조건 없이 SELECT 하므로 행이 0건이어도 파스 타임에 깨진다(`docs/DEPLOY_LIGHTSAIL.md` 의 조항형 블록 참조 — push 계획서 맨 앞의 `DROP NOT NULL` 은 "DROP 이면 중단" 규칙이 말하는 그 DROP 이 아니다). pdf.js `getDocument({data})` 는 버퍼를 워커로 transfer(detach)하므로 재업로드용 원본은 따로 쥐고 파서에는 복사본을 준다. 견적 제출 시 선택(`bids.signing_template_id`, 봉인 경계상 `Bid` 도메인 타입에는 절대 얹지 않고 좁은 `findSigningTemplateId` 로만 읽음)해 두면 낙찰 후 딜룸에서 `연결된 템플릿으로 보내기` 지름길(`sendFromTemplate` — create-contract + send 2회 호출, 임베드 없음, 같은 발송 리스 재사용)이 열린다. **서명 본인인증은 기본강제이며 이 지름길에서만 성립한다 (v0.4.46.0)** — 인증수단은 **템플릿 역할 단위**로만 저장돼(계약별 지정 불가) 강제는 "템플릿에 `easy_cert`" + "발송 시 `phone` 필수"의 짝으로만 만들어진다. `easy_cert` 역할에 phone 이 없으면 공급자가 `VALIDATION_ERROR` 400 을 내므로 강등이 불가능하고, 그래서 정책은 **차단**이다: `sendFromTemplate` 이 공급자 왕복 **전에** ① 양측 phone 을 `resolveSecurityMethod`(`lib/signing/security-method.ts` 단일 출처 — **010 만**, 구 번호대는 공급자가 거부)로 검증해 `PG_PHONE_REQUIRED`/`BUYER_PHONE_REQUIRED` 로 갈라 막고(누가 무엇을 할 수 있는지가 달라서 코드를 나눴다), ② `getTemplate` 의 `signers[].security_method` 로 템플릿 **실제** 정책이 두 역할 모두 `easy_cert` 인지 정확일치 확인해 아니면 `TEMPLATE_AUTH_NOT_ENFORCED` 로 막는다 — 이 검사가 **마이그레이션 스크립트를 대신한다**(본인인증 도입 전 템플릿은 기본 email 정책이고, 막힌 PG 가 재저장하면 재생성 경로가 `easy_cert` 를 심어 스스로 풀린다). ②가 없으면 참여자 행에는 `easy_cert` 가 적히는데 계약은 이메일 링크로 서명 가능한 **거짓말**이 되고, reconcile 이 바로잡는 건 계약이 나간 뒤라 강제가 아니다. 정책 조회가 실패하면 통과시키지 않는다(fail-closed). **③ 이미 만들어진 초안을 재사용할 때는 그 초안 *자신의* 참여자 정책을 확인한다 (v0.4.50.0)** — ①②는 *새로* 만드는 계약만 지킨다. `sendFromTemplate` 은 `providerRef` 가 있으면 create 를 건너뛰므로, 본인인증 도입 전에 create 와 send 사이에서 죽은 발송이 남긴 초안(= 공급자 기본 email 정책)이 ①②를 다 통과한 뒤 그대로 나갈 수 있었다 — **그리고 처방된 복구(`TEMPLATE_AUTH_NOT_ENFORCED` → 템플릿 재저장)가 정확히 그 경로의 방아쇠였다**(재저장이 ②를 풀어 주면 다음 시도가 옛 초안을 재사용한다). 그래서 재사용은 **양성 증명**을 요구한다: H3 프로브가 이미 조회하는 `stale` 상세로 `isDraftAuthEnforced`(참여자 둘 이상 + 전원 `identity_verification`, fail-closed)를 확인하고, 아니면 종결 ref 와 같은 방식으로 `providerRef` 를 지우고 새로 만든다(발송 전이라 비용은 공급자 측 고아 초안 하나). **프로브가 실패하면 보내지 않는다** — 리스는 반납하되 `providerRef` 는 **보존한다**(일시 실패였는데 실제로는 dispatched 였다면 지우는 순간 취소 핸들을 잃는다). **④ 재사용은 "내 초안인가"도 묻는다 — 출처·판본 게이트**: `provider_ref` 는 세 경로(임베드 바인딩·템플릿 지름길·compose)가 공유하는 **슬롯 하나**라 인증 판정만으로는 **다른 계약서가 발송되는 축**을 못 막는다(양측에 010 번호가 있으면 어느 출처의 초안이든 전원 `identity_verification` 이라 ③을 통과한다). 축은 둘이다 — compose 초안을 템플릿 버튼이 재사용하는 것(Stage 2 가 열린다)과, **compose 없이도 성립하는 것**: 템플릿 수정이 `pg_signing_templates` 행의 provider id 를 in-place 로 갈아치우므로 send 실패 → 수정 → 재시도가 **옛 판 PDF·서명칸** 초안을 보낸다(화면은 "연결된 템플릿을 보냈다"고 말한다). 그래서 재사용은 `origin === 'template' AND 초안에 기록된 판본 == 지금 연결된 템플릿` 을 요구한다. 출처는 `signing_contracts.provider_draft_origin`, 판본은 **되살린 이력 컬럼** `snowsign_template_id` 이며(그래서 아래 "신규 발송에는 안 채워진다"는 더 이상 사실이 아니다 — 템플릿 경로가 초안 생성 시 채운다), 둘 다 `bindDraftRef` 가 `provider_ref` 와 **한 UPDATE** 로만 쓰고, 지우기는 `clearDraftRefIf`(기대 ref + awaiting 상태 CAS — 출처·판본까지 같은 UPDATE 로 NULL, 실패는 CONTRACT_BUSY) 하나다 — `SigningContractPatch` 에서 `providerRef`·`snowsignTemplateId` 팔을 **삭제**해 반쪽 쓰기·반쪽 지우기가 컴파일 타임에 표현 불가능하다(id 만 보던 블라인드 clear 는 프로브 왕복 중 임베드 attach 가 바인딩한 발송된 계약의 ref 를 지워 영구 조정불가 행을 만들 수 있었다). 재바인딩(`markSentIfAwaiting`)도 필수 `draft` 판별 필드로 출처·판본을 같은 UPDATE 로 정리하며(템플릿 발송 = 유지 / 임베드·복구·자가치유 = NULL), 재사용 게이트는 검증한 ref 와 실제로 보내는 ref(리스 직후 스냅샷)의 동일성까지 요구한다(다르면 CONTRACT_BUSY — 검증 안 된 값을 보내지도, 검증된 쪽으로 갈아타지도 않는다). `provider_ref IS NULL` CAS 라 낡은 스냅샷이 남의 핸들을 덮어쓸 수 없고, **판정은 리스 획득 뒤 재조회한 상태로 한다**(전에는 리스 *이전* 스냅샷이라 두 템플릿 시도가 서로의 ref 를 덮어쓸 수 있었다 — 같은 모양이 `createSendEmbedSession` 에 남아 있다, TODOS P3). 출처 미상(이 기능 이전 행)은 **fail-closed**. `isDraftAuthEnforced` 는 의미가 그대로다 — 출처가 다른 초안이 애초에 그 술어에 도달하지 않게 해서 닫았다. ⚠️ 어휘가 갈린다: 계약 **참여자**는 `identity_verification`, 템플릿 **서명자**는 `easy_cert` — 리터럴 단일 출처는 `PROVIDER_ENFORCED_SECURITY_METHOD`(`lib/signing/security-method.ts`)이고, 혼동하면 판정이 통째로 뒤집혀 재사용이 조용히 죽는다. 자력 복구를 위해 **설정 > 프로필에 휴대폰 인증 화면**(`UserPhoneForm` + `updateMyPhoneAction`, 저장 게이트는 같은 `resolveSecurityMethod`)을 함께 냈다 — 그 전에는 가입 외에 번호를 넣을 경로가 없어 차단이 재가입밖에 없는 데드엔드였다. **임베드 경로는 여전히 이메일 인증이다**(PG 가 iframe 안에서 수신자를 직접 타이핑하고 `POST /v1/embed-sessions` 에 보안정책 파라미터가 없다) — 강제의 실제 범위가 여기서 갈린다. 실측 근거는 `docs/SNOWSIGN_SANDBOX.md` "본인인증 강제 경로 실측"(S1~S6 + 프로브), 잔여는 TODOS.md Signing 절. v0.4.37.0 이 폐지했던 이유(스노우싸인에 **템플릿을 만드는 임베드 flow 가 없다** — `POST /v1/embed-sessions` 의 flows 는 전부 계약 생성 계열)는 여전히 사실이고, 그래서 재도입은 임베드가 아니라 **우리가 에디터를 소유**하는 방식이다(`signature_fields` 좌표는 pdf.js `getViewport({scale:1})` 픽셀 — 원점은 **좌상단으로 실측 확정**(2026-08-04, 스모크 T6) — y-플립 불필요). **자동 발송은 없다** — 구매사가 선정하는 순간 `awardRfpAction`이 `ContractSigningService.onAward`를 호출하지만 이는 **항상** `awaiting_pg_template` 행을 만들 뿐이고, 실제 발송은 PG 가 임베드에서 완주할 때 한 번 일어난다. **선정~발송 구간은 더 이상 인앱 전용이 아니다 (v0.5.3.0)** — `signing.awaiting_template` 알림이 인앱과 **이메일** 양쪽으로 나간다(수신자 = 낙찰 PG 워크스페이스 승인 멤버 전원, 템플릿 `signingAwaitingTemplate`). 앞뒤 구간에는 외부 채널이 있는데(선정은 `rfp.awarded` 메일, 발송 후는 스노우싸인의 서명 요청 메일) **가운데만 앱 안에 갇혀** PG 담당자가 들어오지 않으면 계약이 조용히 멈춰 있었다. 7일 방치 재넛지(`nudgeStaleAwaiting`)도 같은 두 채널로 나가며, 그 메일의 **outbox `dedupeKey` 에는 수신자와 회차가 둘 다 들어간다**(회차 성분 = 스로틀 마커로 쓰는 `lastPolledAt` 과 같은 값) — 어느 하나라도 빠지면 dedupe UNIQUE 에 걸려 **인앱은 쌓이는데 메일만 조용히 사라지는** 실패가 된다. SSR 렌더는 트랜잭션 **밖**에서 한다(루프가 최대 50건이라 안에서 돌리면 그만큼 풀 커넥션을 쥔다). **발송은 두 단계다**: ① `createSendEmbedSession(rfpId, actor)` — 낙찰 PG·awaiting 에서만, `claimForSend` 리스를 잡고 `flows:['pdf_send']` + `externalId:'sc:<signingContractId>'` 로 세션을 발급하며 리스 토큰(`claimedAt`)을 함께 돌려준다. **리스는 짧고(`EMBED_SEND_LEASE_MS` 5분) 패널이 열려 있는 동안 하트비트로 연장된다**(`renewSendEmbedClaim`, 화면은 `EMBED_HEARTBEAT_MS` 60초 주기). 사람의 작업 시간을 리스 길이로 덮으려던 초기 구현(30분 고정)은 두 가지가 틀렸다 — 닫기가 리스를 안 풀어 **방금 닫은 본인이 30분 잠겼고**(실사용에서 즉시 드러난 dead end), 탭을 그냥 닫으면 30분짜리 유령이 남았다. 연장 방식이면 닫기·탭 닫기·크래시·네트워크 끊김이 전부 "핑이 멎음" 하나로 수렴해 유령이 최대 5분이고, 진짜 작업 중인 세션은 무한히 살아 있다. 연장·반납 모두 `claimedAt` **정확일치**일 때만 동작하고 연장은 새 토큰을 돌려준다 — 리스가 만료돼 다른 담당자가 재취득한 뒤 옛 세션의 뒤늦은 요청이 도착해도 남의 리스를 늘리거나 풀지 못한다. 연장이 거절되면 화면은 하트비트를 멈추고 패널을 닫는다(뺏긴 리스로 발송하면 계약이 두 건 살아난다) — 단 네트워크 오류로 인한 *실패*는 다음 주기가 만회하므로 패널을 닫지 않는다. **리스는 강제로 이어받을 수 있다 (v0.4.38.0)** — 하트비트가 유령 리스를 없앤 대신, 임베드를 열어둔 채 자리를 비운 탭은 60초마다 리스를 갱신해 **영영 풀리지 않는다**(크래시·탭 닫기만 5분 만료로 낫는다). 두 진입점(임베드 `계약서 올리기`, 템플릿 지름길 `연결된 템플릿으로 보내기`)이 막히면 `SEND_HELD_BY_TEAMMATE` 를 받아 **토스트 대신 확인 다이얼로그**를 띄우고, 사용자가 확인하면 `takeoverSigningSendEmbedAction`(임베드) 또는 `sendSigningContractFromTemplateAction({takeOver:true})`(템플릿)이 `forceClaimForSend` 로 가져온다 — 스캔 `보낸 계약서 찾기`의 이어받기는 폐지됐다(Wave 3, 찾기 도중 이어받기가 동료의 작성물을 지우는 것을 막기 위함 — 잠시 후 재시도 안내만 한다). 강제 취득은 `leaseBefore` 가 없다는 것 하나만 `claimForSend` 와 다르다 — **상태 조건(`awaiting_pg_template`)은 그대로다**(강제는 *경합*에 대한 것이지 *상태*에 대한 게 아니다). 옛 소유자를 알기 위해 읽고 → 읽은 값에 CAS 하므로, 보고되는 밀려난 사람은 **쓰기 시점에 실제로 쥐고 있던** 사람이라 알림이 엉뚱한 사람을 지목할 수 없다. **밀려난 동료를 실제로 막는 것은 알림이다.** 스노우싸인 Public API 에 **임베드 세션을 취소하는 엔드포인트가 없어**(생성 하나뿐) 뺏어도 그 사람 iframe 은 살아 있고, 둘 다 보내면 나중 것은 `ALREADY_SENT` 로 막히지만 **이미 서명 요청 메일이 두 통 갔고** 그 계약은 `provider_ref` 가 없어 `cancel` 로 손댈 수도 없다. 발송 버튼은 우리 페이지 안 iframe 에만 있으므로, 트랜잭션이 밀려난 사람에게만 보내는 인앱 알림(`signing.send_taken_over`)이 기존 사용자별 SSE 버스로 그 브라우저에 닿아 패널을 즉시 내리는 것이 실제 차단이다 — 딜 판정은 `isSendTakenOverFor`(알림 `linkUrl` 의 마지막 경로 세그먼트, 접두어 매칭 금지)가 단일 출처이고, 라이브 알림만 흐르는 좁은 구멍(`subscribeToLiveNotifications`)을 쓴다(스토어를 읽으면 history hydrate 로 들어온 옛 알림이 화면을 건드린다). **신호가 닿으려면 스트림이 열려 있어야 하므로 구독 자체가 스트림을 연다**(`subscribeToLiveNotifications` 가 `useNotifications` 와 같은 ref-count 를 쓴다) — Set 에 넣기만 하면 사이드바가 마운트돼 있는지에 신호가 종속되고, 모바일에서 사이드바는 Sheet 안이라 서랍이 닫혀 있으면 신호가 통째로 죽는다. 구독은 패널이 아니라 **탭 수명**에 건다 — 세션 발급 왕복 중에 도착한 알림은 청취자가 없으면 사라지고(재생 없음) 이미 남의 것이 된 리스 위에 패널이 열린다. **이어받기는 세션을 손에 넣은 뒤에 커밋한다** — 되돌릴 수 없는 절반(동료 화면이 닫히고 작성물이 사라진다)을 발급보다 먼저 하면, 발급이 실패했을 때 동료 작업만 날아가고 리스는 아무도 안 쥔다(기본 경로는 잃을 작업이 없어 리스 먼저 잡는다). **자기 리스는 이어받지 않는다**(`getSendLeaseHolder` 의 `isSelf`) — 알림이 자기에게는 가지 않으므로 옛 탭이 닫히지 않아 같은 사람의 iframe 이 둘 살아난다. **하트비트는 폴백으로 남는다** — 실시간이 없는 환경에서도 ≤60초 안에 닫히고, 그때 화면이 가르는 기준은 **소유자**다(`SEND_TAKEN_OVER` = 남이 쥐고 있음 / `CONTRACT_BUSY` = 비었거나 자기 낡은 토큰 = 그냥 경합). 남는 창은 정직하게 열려 있다: 이어받기와 푸시 도착 사이 찰나에 동료가 발송을 누르면 계약은 나간다. 0 으로 만들 수 없어 확인 다이얼로그가 그 가능성을 문구로 경고한다(`딜룸에서 관리할 수 없어요`). 리스 반납은 닫기 버튼뿐 아니라 **언마운트**(딜룸 탭 전환·모달 닫기)에서도 일어난다 — 닫기만 반납하면 같은 자기-잠김이 경로만 바뀌어 남는다. ② `attachProviderContract(rfpId, providerContractId, actor)` — **여기가 진짜 게이트다.** 계약 id 는 브라우저 postMessage 에서 오므로 신뢰하지 않고, ACL 재검증 → `getContract` 재조회 → **실제 발송 상태 확인**(`isDispatchedProviderStatus` — `draft` 는 거부, 실측상 발송 직후는 `pending`) → `provider_ref` 바인딩 유일성 → `markSentIfAwaiting` CAS 순으로 통과해야 `sent` 가 된다. 상태 게이트가 필요한 이유: 완료 이벤트를 위조하거나 초안 단계의 id 가 흘러들면 **아무에게도 안 나간 계약으로 딜룸이 `sent` 가 되고 양측에 알림까지 나간다**(구매사는 오지 않을 서명 메일을 기다린다). `external_id` 검증과 달리 이 게이트는 실제로 동작한다 — status 는 항상 회신된다. (`external_id` 소유 검증 코드도 있지만 **현재는 실행되지 않는다** — 실측상 `getContract` 가 그 필드를 회신하지 않는다. 잔여 위험과 근거는 `docs/SNOWSIGN_SANDBOX.md` Q3 + TODOS.md Signing 절.) **완료 신호가 유실된 계약(고아)은 PG 가 직접 잇는다** — 딜룸 대기 상태의 `보낸 계약서 찾기`(`SigningRecoveryDialog`)가 `listRecoveryCandidates` 로 후보를 받아 보여주고, PG 가 고른 것을 같은 `attachProviderContract` 에 위임한다(두 번째 바인딩 경로를 만들지 않는다). **자동 채택이 아니다** — 상관키가 휴리스틱이라 기계가 틀리면 남의 계약이 이 딜룸에 붙는다. 상관키는 `participantsMatchDeal` 한 곳이 소유하며 **구매사 담당자 + 낙찰 PG 워크스페이스 승인 멤버 둘 다**를 요구한다 — 구매사 하나로는 '이 딜'이 아니라 '이 구매사'를 가리켜, 한 담당자가 견적을 여럿 낸 평범한 상황에 대기 중인 딜이 다른 딜의 계약을 집어온다(PG 쪽을 `bid.submittedBy` 로 좁히면 반대로 동료가 보낸 경우를 놓친다). 예산은 클릭당 최대 16회(목록 ≤4 + 상세 12) + 12초 데드라인이며, `AbortSignal` 을 fetch 와 **재시도 직전** 양쪽에 건다(후자가 없으면 429 폭풍이 시계를 재무장시킨다). 목록 정렬 순서는 가정하지 않고(문서에 명시 없음) 여러 장이면 마지막 장도 받아 직접 정렬하며, `in_progress` 도 훑는다(구매사가 먼저 서명한 고아). 스캔 중에는 발송 리스를 잡아 작성 중인 담당자와 상호배타를 만든다. `expectedContractId` 로 다이얼로그가 열려 있는 동안 열린 새 라운드에 잘못 붙는 것을 막고, 감사 메타의 `source` 가 복구/임베드를 구분한다 — 단 **`source` 는 감사 라벨일 뿐 보안 판정에 쓰지 않는다**(클라이언트가 보내는 선택 필드라 빼면 꺼진다). 바인딩 게이트의 규칙은 한 문장이다: **복구 스캔이 한 번이라도 노출한 공급자 계약은, 어느 딜에 붙이든 그 딜의 상관키를 통과해야 한다.** 근거는 서버가 기록한 노출 사실(`recovery_refs` 배열, 배열 겹침 조회)이다. 이 규칙이 필요한 이유는 목록이 처음으로 **미바인딩** 계약의 id 를 PG 브라우저에 알려주기 때문이다 — 그 전에는 postMessage 가 도착한 계약만 알 수 있었고 그건 이미 바인딩돼 잠겨 있었다. 노출된 적 없는 계약(임베드에서 방금 만든 것)에는 걸지 않는다: 거기까지 걸면 구매사 이메일 오타로 나간 계약이 바인딩조차 안 돼 취소 핸들(`provider_ref`)을 영영 못 얻는다. 멱등이라 복구 경로와 postMessage 가 겹쳐 두 번 도착해도 안전하다. **참여자는 우리 DB 가 아니라 스노우싸인이 실제로 계약에 넣은 사람들을 미러링한다** — 임베드가 참여자 프리필을 지원하지 않아 PG 가 수신자를 직접 타이핑하기 때문이다. 그래서 임베드 패널이 구매사 서명 담당자(이름·이메일)를 눈앞에 띄우고, 그럼에도 수신자에 그 이메일이 없으면 `participantMismatch` 로 경고한다(이미 발송된 계약이라 막지는 않고 취소를 유도). **로컬 영속이 실패해도 보상 취소하지 않는다** — 우리가 만든 계약이 아니고 이미 서명 요청 메일이 나갔다(템플릿 시절 `performSend` 와 결정적으로 다른 점). 양측은 이메일 링크의 SnowSign 페이지에서 서명하고, 앱은 **SnowSign 웹훅(저지연 트리거) + 폴링(백스톱)**으로 상태를 반영한다: `POST /api/signing/webhook`이 HMAC-SHA256 서명(`X-Webhook-Signature`)을 검증한 뒤 payload의 `contract_id`만 뽑아 `reconcileByProviderRef`→`reconcileStatus`(getContract 재조회)로 위임하고 — 웹훅은 payload를 신뢰하지 않는 순수 트리거이며 상태 매핑은 폴링과 동일한 단일 경로, 멱등 `ensureFinalized`로 중복 무해 — 웹훅 유실(SnowSign auto-retry 없음) 대비로 딜룸 lazy + `poll-signing-status` cron 폴링이 백스톱을 이룬다. 웹훅 시크릿은 `SNOWSIGN_WEBHOOK_SECRET`(미설정 시 401 fail-closed로 웹훅 무시, 폴링만 동작). **웹훅 하드닝(v0.4.42.0)**: `contract_id` 는 임베드/액션 경로와 같은 `CONTRACT_ID_RE` 화이트리스트를 통과해야 재조회를 만들고, 재조회는 인메모리 리미터(계약별 10/분 + 전역 30/분, `webhook-rate-limit.ts`)로 캡한다 — 리플레이 nonce 가 없어 인증 요청 1개가 provider 호출 1회로 증폭되는 축을 막는다. 스킵은 `signing.webhook_id_rejected`/`webhook_throttled` warn 으로 관측된다. **웹훅 경로는 실측으로 확인됐다 (2026-08-04)** — Public API 문서에는 웹훅 절이 없지만 **콘솔에는 있다**(조직 관리 → 웹훅). `prod-support-b` 가 활성으로 등록돼 있고(URL 은 우리 라우트, 구독 이벤트 7종에 **발송 계열 포함**), 전송 로그가 운영 서버의 **200** 응답을 보여준다. 라우트는 시크릿 미설정 시 fail-closed 401 이므로 200 은 운영 env 에 `SNOWSIGN_WEBHOOK_SECRET` 이 채워져 HMAC 검증이 통과한다는 직접 증거다 — 이전 문서가 적었던 "폴링이 100% 를 혼자 했다"는 틀렸다. 다만 200 은 수신·ack 까지만 증명하며 그 뒤 `after()` 재조회는 폴링과 같은 경로다. 근거는 `docs/SNOWSIGN_SANDBOX.md` T10 절. **운영자 슬랙 알림**: 서명 상태가 실제로 전이하는 CAS-guarded 분기(대기 생성·발송·연결·완료·거절/만료·취소)에서 `notifySigningOperator`(`lib/server/notifications/operator-signing.ts`)가 커밋 후 best-effort 로 슬랙 Incoming Webhook(`SLACK_WEBHOOK_URL`, `lib/integrations/slack.ts`)에 fire-and-forget 발화한다 — outbox 미경유(admin-email 독트린), no-op reconcile(2분 폴러) 무발화, 메시지에 금액·수수료 금지, `after()` 미사용(onAward 가 이미 after() 안 — 중첩 회피). **같은 웹훅으로 신규 가입·PG사 합류 심사 요청도 나간다** (`admin-signup.ts`, v0.5.4.0) — 기존 `ADMIN_NOTIFY_EMAIL` 메일과 **병행**이고 두 채널은 `after()` 안에서 **각자의 try/catch** 를 갖는다(단일 가드였을 때는 이메일 템플릿 렌더가 던지면 슬랙까지 함께 죽었다 — 슬랙을 먼저 보낸다). 사용자 입력(RFP 제목·워크스페이스명·가입자명)은 반드시 `escapeSlackText`(`lib/integrations/slack.ts`)를 통과한다: 슬랙 Incoming Webhook 페이로드에는 `allowed_mentions` 에 해당하는 **멘션 차단 필드가 없어서** `<`/`>` 이스케이프가 `<!channel>` 핑을 막는 유일한 수단이다. 반대로 디스코드판의 백슬래시·대괄호 이스케이프는 **옮기면 안 된다** — 슬랙은 `[문구](url)` 를 링크로 렌더하지 않고 백슬래시 이스케이프 자체가 없어 `\[x\]` 가 채널에 그대로 인쇄된다. 딜룸의 전용 `계약` 탭(`SigningTab` — `signing` 이 있으면 탭 맨 앞·기본 활성, 레일 도트 + 다른 탭의 `SigningSummaryStrip` 한 줄이 상태를 알린다)이 8개 상태를 상태 헤더 · 세로 서명 타임라인 · (상태별) 완료 문서 · 액션 바 고정 순서로 보여주고 리마인더·취소·재발송·완료본 온디맨드 다운로드(1h URL 302 프록시, 로컬 보관 없음)를 노출한다. **계약 라이프사이클 기본기(v0.4.42.0)**: ① 리마인더는 24h 쿨다운 — `signing_contracts.last_reminded_at` 원자 클레임(CAS, 판정과 기록이 한 UPDATE)이라 병렬 클릭이 함께 통과할 수 없고, 발송 실패 시에만 정확일치 CAS 로 되돌린다(`REMIND_COOLDOWN`, 창 값은 `lib/signing/remind-cooldown.ts` 단일 출처). ② 재발송은 확인 다이얼로그를 거치며 문구는 액션이 소유한다(`SigningAction.confirm` — resend '다시 발송'/send_failed '다시 시작'이 각자 문구). ③ 템플릿 생성에 `deadline_days: 30`(`lib/signing/deadline.ts`)을 전송하고 reconcile 이 provider `expires_at` 을 미러링해 진행 카드에 서명 마감을 표시한다(임베드 건별 경로는 만료 파라미터가 없어 미러링만). ④ 참여자 `email_delivery` 를 미러링해 반송(bounced)을 error 칩 + 지속 경고로 보여주고, 수신자 불일치(`participantMismatch`)도 토스트 1회가 아니라 지속 경고다(판정은 buyer 역할 부재 — 바인딩 시 이메일 대조의 영속 기록). ⑤ 계약 수준 전이(declined/expired/제공자취소/resent/reminded)가 전부 감사 로그에 남는다(CAS 승자만 1회, 커밋된 사실의 기록이라 best-effort). **발송 임베드는 카드 안이 아니라 딜룸 위를 덮는 거의 전체화면 모달(`SigningSendModal`)로 열린다 (v0.4.39.0)** — 딜룸 모달(최대 900px) 안 72dvh iframe 은 PDF 를 올리고 서명칸을 배치하기에 좁았고 타임라인·액션바·채팅이 같이 보여 산만했다. 모달은 표현과 이탈만 소유하고 리스·하트비트·이어받기·서버 액션은 `SigningTab` 에 그대로 남는다. **이탈 3경로(백드롭·Escape·닫기)는 전부 확인 다이얼로그로 수렴한다** — 작업물이 스노우싸인 안에만 있어 iframe 언마운트가 곧 소실이기 때문이고, 서드파티 오리진이라 진행 상태를 읽을 수 없어 확인은 무조건 뜬다(확인창이 떠도 iframe 은 언마운트되지 않는다 — 리마운트하면 확인을 받는 의미가 사라진다). 모달은 언마운트에서 `onClose` 를 부르지 않는다(반납은 `SigningTab` 의 언마운트 effect 소유 — 이중 반납이면 죽은 토큰으로 요청이 두 번 나간다). 브라우저 뒤로가기는 이 확인을 건너뛴다(딜룸 전체의 이탈 정책 문제 — TODOS.md Signing 절 P3). 임베드 iframe 은 `sandbox`(top-navigation 없음) + `referrerPolicy="no-referrer"` 로 가두고, postMessage 는 `iframeUrl` 에서 파생한 오리진과 정확히 일치할 때만(파싱 실패 시 **모두 거부** — fail-closed) 받으며 완료는 **바인딩에 성공했을 때만** 1회로 잠근다(실패에도 잠그면 계약은 실제로 발송됐는데 그 id 를 영영 못 받아 고아가 확정된다 — 자동 복구 경로가 없다). 이벤트 형태 판정은 `lib/signing/embed-events.ts` 순수 함수가 단일 출처(네임스페이스 `snowsign.embed.` 강제 + 계약 id 경로-세그먼트 화이트리스트). 상태 파생은 `components/deal-room/signing/signing-view-model.ts` 순수 함수가 단일 출처이며, 카드 전체(제목·칩·액션)가 역할별로 갈리는 것은 `awaiting_pg_template` 한 상태뿐이다 — PG 는 '계약서 올리기'(+ 낙찰 견적에 연결된 템플릿이 있으면 '연결된 템플릿으로 보내기'가 primary 로 앞서고 업로드는 outlined 로 물러난다 — PR#470) + '보낸 계약서 찾기', 구매사는 대기 안내. 칩 라벨(`계약서 보내기 전`)이 한 가지뿐인 것은 `buildSigningSummary` 가 같은 함수로 카드뷰를 다시 만들기 때문(갈리면 요약 스트립과 카드가 어긋난다). 단, 선정 마일스톤 라벨(`awardedNode` — 구매사 '견적을 선정했어요' / PG '이 견적이 선정됐어요')은 `awaiting_pg_template` 외에 `send_failed`·발송 전 취소된 `canceled`에서도 역할별로 문구가 갈린다. (`send_failed` 는 **레거시 렌더 전용** — 리스 CAS 도입 후 이 상태를 쓰는 코드가 없다.) **재발송(`resend`)은 항상 새 대기 라운드를 연다** — 계약서 PDF·서명칸이 스노우싸인 안에만 있고 우리는 사본이 없어 재사용할 수 없다. 직전 라운드의 살아있는 계약은 원자 CAS 로 취소하고, 아무것도 발송되지 않았으므로 `degraded: true` 가 늘 실린다(호출자가 '다시 보냈어요'라고 말하지 않도록). ACL: 계약 조회·조작 = 낙찰 PG ws + buyer ws(로더는 buyer 항상 / PG `awardedToMe`만). 완료 칩은 '서명 완료', buyer 대기 라벨은 '**PG사가 계약서 준비 중**'. **발송된 조항형 계약은 문서 스냅샷을 남긴다 (v0.5.3.0)** — `signing_contracts.sent_document`(jsonb, `SentContractSnapshot` = 렌더에 들어간 `LayoutInput` + `_v`). 조항형은 문서가 우리 DB 에 있지만 그 서식 행은 수정 가능하므로, 스냅샷이 없으면 서식을 고치는 순간 이미 나간 계약이 무엇이었는지 알 길이 없다(공급자 다운로드는 `completed` 에서만 열린다). 쓰기 경로는 `markSentIfAwaiting` 이 `provider_ref`·출처와 **한 UPDATE 로** 하는 것 하나뿐이고, `draft` 판별 유니온의 compose 팔이 스냅샷을 **필수**로 요구해 '발송했는데 무엇을 보냈는지 모르는' 행이 컴파일 타임에 표현 불가능하다(스냅샷은 렌더에 넘긴 **그 객체**를 그대로 쓴다 — 따로 조립하면 둘이 어긋난다). 템플릿·임베드 경로는 NULL 이다(PDF 가 공급자에만 있어 지어낼 수 없다). ⚠️ **`SIGNING_CONTRACT_COLUMNS` projection 에 넣지 않는다** — 치환된 문서 전문이 통째로 담긴 jsonb 가 딜룸 로드마다는 물론 2분 폴러의 전 행에 딸려 온다(서식 저장 시 걸리는 `MAX_DOCUMENT_BYTES` 128KB 상한은 **치환 전 원본**에만 적용되므로 이 컬럼에 보장된 천장은 없다). 읽기는 좁은 리더(`findSentDocument`)뿐이며 **아직 화면에 그리는 곳은 없다**(보존이 목적). **완료본은 R2 로 자동 보관된다 (v0.5.3.0)** — `ensureFinalized` 의 CAS 승자가 `createPendingForContract` 로 buyer·PG **양쪽 워크스페이스에 한 행씩** 만들고(best-effort — 던지지 않으며 실패는 cron 백필이 만회), `poll-signing-status` cron 이 완료본·감사추적인증서를 공급자에서 받아 R2 에 저장한다(하이드레이션). 그 뒤로는 견적 요청을 지워도(signing 행 FK SET NULL) 사본과 메타 스냅샷은 남는다 — 그것을 보는 화면이 `/contracts` 계약 보관함이다(SCREEN_DESIGN.md C1, ACL·presign 정책은 위 Storage 행 ③). **봉인 경계**: 구매사에게는 `providerRef`(이 값으로 PG 의 계약 문서를 조회할 수 있다)와 이력 컬럼 `snowsignTemplateId` 가 `stripProviderRefs` 로 벗겨져 나간다 — 경계 소유자는 **서비스** 한 곳이다(로더가 따로 벗기면 새 호출자가 생길 때 조용히 빠진다). 얕은 `SnowSignClient` seam(교체 용이성은 YAGNI로 의도적 완화) + `ContractSigningService` 파사드. 레거시 award 기록 테이블 `contracts`와는 별개(`signing_contracts`). **동시성·경계 하드닝**: 웹훅은 `after()`로 응답 후 재조회(5초 예산 비블로킹) + `provider_ref` 인덱스; 종결 전이(declined/expired)·resend 취소는 원자 CAS(`transitionIfActive`); 임베드 진입에서 리스를 잡아 담당자 둘이 각자 발송해 계약이 두 건 살아나는 것을 막고, 세션 발급이 실패하면 리스를 즉시 푼다; `getForActor` ACL-먼저(award 존재 오라클 제거); 참여자 이메일 대소문자 무시 매칭; poll cron 이 방치 awaiting 을 7일 스로틀로 재넛지; cron 인증 상수시간·헤더 전용. **실측 완료(2026-08-01)**: 실 키로 딜룸에서 계약 1건을 끝까지 발송해 검증했다 — 임베드 완주, 완료 이벤트 `snowsign.embed.contract_sent`(payload 에 `contract_id`), 임베드 오리진 `https://snowsign.jtsnowball.com`(API 호스트와 별개), 그리고 **`external_id` 는 회신되지 않는다**. 우리 sandbox 최소 집합이 임베드를 깨뜨리지 않는 것도 확인했다. 결과·측정 방법은 `docs/SNOWSIGN_SANDBOX.md` 가 SSOT. 남은 것은 e2e 자동화와 위 소유 검증 갭이다.

## Current Stack

| Layer | Choice | Version |
|---|---|---|
| Framework | Next.js App Router, Turbopack default, async `params`/`searchParams` | `next@16.2.11` |
| Runtime | React | `react@19.2.4` |
| Language | TypeScript strict | `typescript@6.0.3` |
| Auth | Auth.js v5 (no middleware — guard via `(app)/layout.tsx` redirect) | `next-auth@5.0.0-beta.32` |
| DB | Drizzle ORM + Postgres | `drizzle-orm@0.45.2`, `postgres@3.4.7` |
| Storage | ① 첨부파일: Cloudflare R2(S3 호환 API, `lib/server/storage/r2.ts`, 키 `attachments/<id>`) — 라우트는 `getStorage()` 만 본다. **업/다운로드 모두 presigned**: 업로드 = 2-phase 직행 PUT(`POST /api/files/presign` → 브라우저→R2 PUT → `POST /api/files/{id}/complete` 서버 스니핑 검증, `attachments.status` pending→ready, 클라 공용 헬퍼 `lib/attachments/upload-client.ts`), 다운로드 = `GET /api/files/{id}` 가 ACL 검증 후 302 → presigned GET(TTL 15분). 버려진 pending 은 `/api/cron/sweep-uploads`(1h 초과) 가 청소, 버킷 CORS(PUT) 필수. R2 env(`R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET`) 미설정 시 모든 환경에서 throw(폴백 없음) — 로컬 dev도 실 R2 버킷 사용, 단위 테스트는 `__setStorageForTest` mock 주입, e2e 첨부 스펙은 R2 env 없으면 skip. ② 사용자 프로필 사진: Postgres bytea — `user_avatar_blobs` 테이블, `UserAvatarRepo`(`getUserAvatarRepo()`) — `getStorage()` 외부, 별도 repo 패턴. ③ 계약 보관 문서(v0.5.3.0): 같은 R2 버킷의 별도 키 공간 — 전자서명 완료본·감사추적인증서의 **우리 사본**(`contract-archives/signing/<signingContractId>/{document,audit}.pdf`, cron 이 공급자에서 받아 저장)과 플랫폼 밖 계약서 **수동 업로드**(`contract-archives/upload/<archiveId>`). 업로드는 첨부와 같은 2-phase presign(`POST /api/contract-archives/presign` → 브라우저→R2 PUT(TTL 10분) → `POST /api/contract-archives/{id}/complete` 가 `head` 크기 + 첫 4KB 매직바이트로 재검증, 불일치면 객체·행을 함께 폐기), 다운로드는 `GET /api/contract-archives/{id}/download?doc=document|audit` 가 ACL 재검증 후 302 → presigned GET(TTL 15분). PDF 전용·건당 30MB·워크스페이스당 업로드 200건 캡. 버려진 pending 은 `/api/cron/sweep-uploads` 가 첨부와 같은 1h 컷오프로 청소하되 **`source='upload'` 만** 지운다 — signing pending 은 하이드레이션 대기 중인 정상 상태라 함께 쓸면 완료본 사본이 영영 안 생긴다 | `@aws-sdk/client-s3@3.x` |
| Styling | Tailwind v4 + CSS Variables (`@theme` block) | `tailwindcss@4.2.4` |
| Headless UI | `@base-ui/react` (shadcn base-nova style) + Radix 일부 (`@radix-ui/react-popover`, `@radix-ui/react-slider`) | `@base-ui/react@1.4.1` |
| Component tooling | shadcn (base-nova) — 컴포넌트 scaffolding 전용 | `shadcn@4.6.0` |
| State | Zustand (UI toggles, signup draft, page→shell header-actions slot) | `zustand@5.0.13` |
| Forms | zod v4 검증 + Server Actions (react-hook-form 미사용 — 폼은 useState + zod) | `zod@4.4.3` |
| Client data fetching | SWR — `force-static` 랜딩 헤더(`LandingHeaderNav`/`PgLandingHeaderNav`)가 마운트 후 `/api/auth/session`을 클라이언트에서 재조회해 로그인 상태를 반영(`components/landing/use-session-authed.ts`); 같은 키를 쓰는 두 헤더 인스턴스 요청을 SWR 캐시가 자동 중복제거 | `swr@2.4.2` |
| Numeric input | `react-number-format` — 원화 금액 입력 천단위 구분·소수점 차단 (`CurrencyInput`) + `D+N` 정수 전용 정산주기 입력 (`DayOffsetInput`) | `react-number-format@5.4.5` |
| Korean i18n | `es-hangul` (toss) — 조사 자동 선택(`josa()`), 초성 검색(`disassemble`), 숫자→한글 혼합 표기(`numberToHangulMixed`). 한글 텍스트 처리 단일 출처 | `es-hangul@2.3.8` |
| Icons | lucide-react | `lucide-react@1.14.0` |
| Fonts | `next/font/local` — Pretendard Variable + JetBrains Mono Variable, self-hosted in `public/fonts/` | — |
| Motion | `motion` (구 Framer Motion). 임포트는 `motion/react`. | `motion@12.38.0` |
| DnD | @dnd-kit — 칸반 보드 드래그·정렬 (`fractional-indexing` 병용) | `@dnd-kit/core@6.3.1` |
| Email | Resend + `@react-email/render` | `resend@6.4.0` |
| Logging | Pino + Axiom (`next-axiom`) | `pino@10.3.1`, `next-axiom@1.10.0` |
| Observability | Sentry | `@sentry/nextjs@10.51.0` |
| Support | Channel.io | `@channel.io/channel-web-sdk-loader@2.0.2` |
| Realtime | Centrifugo (자체호스팅 WS, Caddy `wss://`) + `centrifuge-js` — 채팅 라이브(즉시 수신·타이핑·프레즌스·읽음). 메시지는 자사 Postgres에만 영속, 비공개 ACL은 subscribe-proxy로 앱에 보존 | `centrifuge@5.6.0` |
| Cmdk | `cmdk` | `cmdk@1.1.1` |
| Testing | Vitest + PGlite (단위), Playwright (e2e) | `vitest@4.1.5`, `@electric-sql/pglite@0.3.13` |
| Package mgr | pnpm | — |

상세 버전·스크립트는 `package.json` 참조. 부트스트랩은 완료 (M0).

## Routing Architecture (critical)

```
app/
├─ (public)/    # Unauthenticated: /login, /signup/{buyer,pg}/*, /password/*, /invite/{rfp,workspace}/[token], /auth/*, /pending-approval, /suspended
├─ (app)/       # Authenticated, AppShell wrapped (full-height Sidebar + Header)
│  ├─ home/
│  ├─ rfp/                    # buyer workspace pages (B1~B7): /rfp, /rfp/[id] (비교·선정 인라인 — 별도 award 라우트 없음), /rfp/@modal/(.)[id] (인터셉트 딜룸 모달)
│  ├─ rfp-create/             # /rfp-create — RFP 작성 플로우 (AppShell 공유, 인터셉트 라우트)
│  ├─ inbox/                  # pg workspace pages (P2~P4): /inbox, /inbox/[rfpId] (제출 후 인플레이스 흡수 — 별도 /submitted 라우트 없음), /inbox/@modal/(.)[rfpId] (인터셉트 딜룸 모달)
│  ├─ opportunities/          # pg — 오픈 RFP 게시판 (비초대 PG 발견·콜드 피치)
│  ├─ tutorial/               # buyer+pg — 온보딩 튜토리얼. 홈 환영 모달(WelcomeModal)/재유도 배너(TutorialNudge)의 진입점. buyer는 BuyerTutorialFlow(components/onboarding/tutorial/)가 같은 라우트 내 phase 전환(작성→도착연출→비교·선정→완료, 전부 fixture)으로 실제 여정 제공. pg는 PgTutorialFlow가 같은 방식으로 초대 수신 연출→요청 조건 확인(RfpBriefPanel)→견적 작성·제출(BidWizard)→완료(봉인 입찰 안내) 여정 제공. **오픈 샌드박스 계약(v0.3.4.0)**: 폼은 전부 프리필(무입력 클릭 완주는 여전히 성립 — e2e/tutorial-click-through.spec.ts가 커버)이지만 차단이 없다 — 키보드락·클릭 실드·밖-클릭 넛지 제거, 사용자는 값을 바꾸고 화면을 자유롭게 탐색할 수 있다. action 코치마크는 타깃 클릭을 기다리며(진행 로직 불변), 타깃 버튼이 disabled면 말풍선에 막힘 힌트를 띄운다. **오프코스 리졸버(v0.3.5.0)**: 사용자가 안내 코스를 벗어나 화면을 직접 바꿔도(위저드 이전/스텝 인디케이터 점프·info 안내 무시하고 실제 버튼 클릭·검증에 막힌 클릭) CoachmarkTour가 250ms 폴링(2틱 히스테리시스, ~0.5s)으로 현재 화면에 실재하는 action 앵커를 찾아 그 스텝으로 즉시 점프·복귀한다 — 전제는 "한 화면에 이 투어의 action 앵커 최대 1개"(위저드 next-N은 스텝별 상호배타, `components/onboarding/tutorial/__tests__/tours.test.ts` 드리프트 가드가 위저드 스텝 상수와 정합을 못박음). action 2개 이상인 투어에서만 인터벌이 돌고(1개 이하는 점프 경로 도달 불가라 미생성), 앵커 0개(전환 중)·2개 이상(모호)·expected 없음(마지막 action 이후)이면 관망한다. 클릭이 접수됐지만 실제 진행이 막혀 다음 타깃이 나타나지 않는 경우도 리졸버가 ~0.5s 안에 직전 action 스텝으로 되돌리며(마지막 action 포함 — PG 제출 확인창의 확인 버튼이 pgWriteTour의 마지막 action 스텝(`tutorial-bid-confirm`, ConfirmDialog `confirmDataCoachmark`)이라, 확인창 취소로 확인 앵커가 사라지면 리졸버가 제출 스텝으로 복귀시킨다. 확인창이 열려 앵커 2개가 공존하는 동안은 '모호=관망'이 흡수), 기존 notFound 타임아웃 복귀는 폴백으로 유지된다. 점프가 사용자 화면을 당기지 않도록 이미 뷰포트에 보이는 앵커는 스크롤하지 않고(완전히 밖일 때만 center 스크롤 — useAnchorRect 공통), 스텝 전환(점프 포함)은 말풍선 옆 sr-only `role=status` 라이브 리전으로 공지된다. /tutorial 밖 내부 링크 클릭은 TutorialLeaveGuard가 가로채 [계속 체험하기|나중에 하기(dismissed)|건너뛰기(completed)] 확인 후 이동(프로그래매틱 이동·뒤로가기는 미가드 — 무스탬프라 환영 모달 재노출로 흡수). 실 백엔드 터치는 스텁: 첨부 드롭존 sampleMode(가상 첨부), 템플릿 저장 안내 토스트. 건너뛰기 계약(v0.3.2.0)·Esc 무반응·라이트 스포트라이트 링 펄스(v0.3.3.0)는 유지. **건너뛰기 계약(v0.3.2.0)**: 코치마크의 건너뛰기 버튼 클릭 = 정상 완주와 동일하게 completed 스탬프+done 화면 점프(양 플로우 handleComplete 공유, 재진입 가드 포함). Esc는 코치마크에 무반응 — onSkip이 비가역 완료와 묶이면서 오발 Esc(⌘K 닫기·앵커 탐색 구간)가 영구 완료가 되는 사고를 막기 위해 CoachmarkTour의 전역 Esc 리스너를 제거함(스킵=버튼 전용). **라이트 스포트라이트(v0.3.3.0)**: 코치마크가 화면을 어둡게 덮던 dim 스크림을 제거 — 배경은 밝게 유지된다. 타깃 강조 링에 opacity 소프트 펄스(`.coachmark-pulse`)를 더해 시선을 유도한다(`prefers-reduced-motion` 존중, 자세히는 DESIGN.md §6)
│  ├─ messages/               # buyer+pg 공통 — 라이브 채팅 (Centrifugo WS)
│  ├─ notifications/          # 인앱 알림 목록 페이지
│  ├─ contracts/              # buyer+pg 공통 — 계약 보관함 (완료본·감사추적인증서 R2 자동 사본 + 플랫폼 밖 계약서 수동 업로드). 완료 계약을 모아 보는 유일한 횡단 화면
│  ├─ quote-templates/        # pg — 견적 템플릿 (정산조건·가입비·수수료율 저장, top-level PG 라우트 — settings 하위 아님)
│  ├─ contract-templates/     # pg — 계약서 템플릿 (PDF 업로드 + 서명칸 배치를 미리 저장, 딜룸에서 지름길 발송; kill switch `CONTRACT_TEMPLATES_ENABLED`, 현재 켜짐)
│  ├─ workspace/new/          # 워크스페이스 생성
│  └─ settings/{profile,members,notifications,audit-log}/
├─ logout/route.ts            # GET (redirect to /login) + POST (204, for client-side signOut)
└─ (no middleware.ts)         # auth guard는 app/(app)/layout.tsx의 서버 redirect로 처리 (resolveShellAccess)
```

Workspace type (`buyer` vs `pg`) determines which sub-tree of `(app)/*` is shown — same shell, different navigation.

**Shell-guard gates (`resolveShellAccess` in `lib/auth/shell-access.ts`)**: the `(app)/layout.tsx` guard runs ordered redirects — unauth → `/login`, incomplete-but-authed (no membership) → `/logout`, **email-unverified → `/pending-approval`**, workspace `pending` → `/pending-approval`, workspace `suspended` → `/suspended`. The email-verification gate is **first-class and independent of workspace status** — an unverified member is redirected even when their active workspace is already `active` (closes the canonical-PG-join hole where joining an approved workspace skipped verification). `emailVerified` is read **live from the DB** (`getDbEmailVerified`, not the JWT) so a just-completed verification takes effect without re-login; after verifying, `EmailVerifyScreen` hard-navigates (`window.location.assign('/home')`) so the guard re-branches by workspace status. **PG 멤버십 승인은 서버 데이터 경계로도 강제된다 (v0.4.20.0)**: `isPgMembershipBlocked`(`lib/auth/pg-membership-gate.ts`)가 승인 상태를 DB 라이브 리드로 판정해 `requirePgSession`(PG 전용 표면)과 `requireActiveWorkspace`(채팅·보드·계약 라이프사이클 등 양측 공용 액션)에 이중 배선 — `pending_approval`/`rejected`/행 부재 전부 fail-closed 차단, 마스터(synthetic admin, 멤버십 row 없음)는 `isMasterEmail` 재확인으로 면제. 셸 가드의 승인 게이트도 `!== 'approved'` fail-closed 로 정렬됨. 잔여는 인라인-게이트 API 라우트(files presign/complete·centrifugo)뿐 — TODOS.md P4 참조.

**Host routing (prod only)**: the single Next.js app serves two hostnames — `support-b.com` (buyer) and `partner.support-b.com` (PG). Route tree is unchanged; `(app)/layout.tsx` reads the request host and redirects a mismatched session to its correct host (`lib/site-routing.ts`). `/signup` also reads the host and redirects to `/signup/buyer` or `/signup/pg` without a role-chooser screen (`signupTargetForHost` in `lib/site-routing.ts`). Session cookie is scoped to `.support-b.com` for cross-subdomain SSO (`AUTH_COOKIE_DOMAIN`). Workspace switch navigates across hosts. PG-facing emails link to `partner.support-b.com`. Local dev uses a single host (routing disabled). Env vars: `NEXT_PUBLIC_BUYER_ORIGIN`, `NEXT_PUBLIC_PARTNER_ORIGIN` — **both-or-neither, enforced fail-closed**: `appOrigins()` throws when exactly one is set (v0.4.3.0). A partial config used to let the missing one fall back to a value that could equal the other, making a broken deploy indistinguishable from single-host dev — and that state silently disabled the partner-host noindex (`shouldNoindexHost`, `robots.ts`) and the `/login/ops` OAuth PKCE host pin (`opsLoginRedirectTarget`, cf. PR#361) at the same time. The origin fallback chain itself (`NEXT_PUBLIC_BASE_URL` → `AUTH_URL` → `http://localhost:3000`) has a single source, `baseUrl()` in `lib/site-routing.ts`; `siteConfig.url` and `lib/server/env.ts`'s `baseUrlFor` (email links) both derive from it, and `lib/server/env.ts` re-exports `baseUrl` only as a deprecated compat hop — new code imports from `@/lib/site-routing`.

**SEO / AI(GEO) 텍스트 엔드포인트**: `/llms.txt`(큐레이션 인덱스)·`/llms-full.txt`(전체 마크다운)는 host-aware route handler(`app/llms.txt`·`app/llms-full.txt`)로 buyer/PG 청중별 사실을 `text/plain`으로 제공한다. `app/robots.ts`·`app/sitemap.ts`도 host-aware로 전환 — buyer 호스트는 자기 origin 참조 + 주요 AI 크롤러 명시 허용하지만, **partner(PG) 호스트는 전면 비색인**(`buildRobots(origin, 'pg')`가 모든 크롤러에 `disallow: ['/']` 반환, `buildSitemap`도 빈 배열, `proxy.ts`가 응답에 `X-Robots-Tag: noindex, nofollow` 헤더까지 얹어 이미 색인된 페이지도 제거 유도 — `lib/site-routing.ts`의 `shouldNoindexHost`). 콘텐츠 SSOT는 `lib/seo/{host,product-facts,llms,robots,sitemap,jsonld}.ts`이며 FAQ는 랜딩 상수(`faq-data`·`pg-faq-data`)를 재사용해 드리프트가 없다. 공식 표기는 '서포트비'이며 그 단일 출처는 `lib/site-config.ts`의 `siteConfig.name` 하나다 — `lib/seo/product-facts.ts`의 `PRODUCT_NAME`은 두 번째 출처가 아니라 거기서 파생하는 재export다(v0.4.3.0). 브랜드 별칭('서포트 B'·'서포트B'·'Support B'·'Supporter B')은 `lib/site-config.ts`의 `BRAND_ALIASES`가 단일 출처 — llms.txt 프리앰블의 별칭 문장(`lib/seo/llms.ts`)과 JSON-LD `alternateName`(`lib/seo/jsonld.ts`)이 이를 직접 소비한다. `siteConfig.keywords`(한글 별칭 2종)와 랜딩 푸터 표기('서포트비 CORP.')는 리터럴 병기 — 별칭 변경 시 함께 갱신해야 한다. JSON-LD 스키마 빌더는 `lib/seo/jsonld.ts`에 있고, `buildOrganizationJsonLd`는 buyer 랜딩(`app/page.tsx`)·PG 랜딩(`pg-landing-data.ts`, description 오버라이드)이 공유하며 `buildSoftwareApplicationJsonLd`는 buyer 랜딩 전용, `<script type="application/ld+json">` 인라인 직렬화는 반드시 같은 파일의 `serializeJsonLd`(`<`·`>`·U+2028/U+2029 유니코드 이스케이프 — `</script>` 태그 탈출 방지)를 쓴다(raw `JSON.stringify` 금지). 랜딩 히어로 지표(`HERO_METRICS`)는 `components/landing/hero-metrics.ts`에 단일 출처로 추출돼 `LandingHero.tsx`와 `product-facts.ts`(BUYER_FACTS.metrics)가 공유하며, `lib/seo/__tests__/product-facts.test.ts` 드리프트 가드 테스트가 양쪽 캡션 일치를 보장한다. 두 txt 경로는 `proxy.ts`/`lib/auth/proxy-matcher.ts` 매처에서 robots/sitemap과 함께 제외해 비인증 크롤러가 접근한다. `app/layout.tsx` metadata의 `alternates.types['text/plain'] = '/llms.txt'` 선언으로 ChatGPT·Perplexity 등 AI 크롤러가 `<head>`에서 자동발견 가능하다. **매처 제외 목록에 항목을 추가할 때 지켜야 할 규칙 두 가지**(v0.4.9.0): ① 세그먼트의 점(`.`)은 `\\.`으로 이스케이프해 파일명이 리터럴 점으로만 매칭된다(`llmsXtxt` 같은 경로가 의도치 않게 제외되는 것을 방지). ② 모든 항목은 세그먼트 경계(`SEGMENT_END` = `/` 또는 경로 끝)에 고정된다 — 맨 접두어로 두면 미래에 그 단어로 시작하는 실제 보호 라우트(`/landing-editor`·`/api-docs`)가 인증 프록시를 통째로 건너뛴다. **유일한 예외는 Next.js 메타데이터 이미지 규약**(`opengraph-image`·`twitter-image`·`apple-icon`)으로, 확장자가 붙어 도착하므로(`app/opengraph-image.png` → `/opengraph-image.png`, 동반 alt 는 `/opengraph-image.alt.txt`) `ASSET_SUFFIX`(`.`/`-` 뒤에만 접미사 허용)를 쓰되 `PATH_END`(`$`)로 고정해 하위 경로(`/apple-icon.png/admin`)까지 새지 않게 한다. 이 규칙은 `lib/auth/__tests__/proxy-matcher.test.ts`가 못박으며, 매처를 바꾸면 `proxy.ts`의 인라인 리터럴도 함께 갱신하고(드리프트 가드가 둘을 byte-equal 로 고정) **반드시 `next build`로 확인해야 한다** — Next 는 `config.matcher`를 모듈 실행 없이 정적 분석하므로 유닛 green 이 build green 을 보장하지 않는다.

**Admin 콘솔은 별도 레포로 분리됨**: `github.com/bothsides-platform-dev/admin-supporter-b`. 이 레포에 `app/admin/` 없음. admin 관련 코드를 찾거나 수정할 때는 해당 레포를 참조. 이 레포에는 DB 마이그레이션 소유권(`lib/db/schema/admin.ts`)과 신규 가입 알림 이메일(`lib/integrations/admin-email.ts`)만 잔존.

## Server Architecture (lib/server/)

세 계층으로 구성된다. 계층 간 의존 방향은 Actions → Services → Repositories.

```
lib/server/
├─ actions/          # 얇은 진입점: 세션 검증 + 입력 파싱 후 서비스에 위임
├─ services/         # 비즈니스 로직 캡슐화 (전체 코드베이스 적용 완료)
│  ├─ rfp.ts         # RfpService: award / cancel / close
│  ├─ bid.ts         # BidService: submit / withdraw
│  ├─ chat.ts        # ChatService: sendMessage / markConversationRead
│  ├─ workspace.ts   # WorkspaceService: create / invite / member management
│  ├─ auth.ts        # AuthService: signup / password reset / email change
│  ├─ notification.ts# NotificationService: markRead / markAllRead / retryEmail
│  └─ contract-signing.ts # ContractSigningService: 선정 후 전자서명(SnowSign 건별 임베드) — onAward(항상 대기)/createSendEmbedSession+attachProviderContract(유일한 발송 경로, 2단계)/reconcile/cancel/remind/resend/getForActor/getDownloadUrl. 얕은 SnowSignClient(lib/server/signing/) 파사드
└─ repositories/     # DB 접근 추상화 (Drizzle 구현 — 단위 테스트는 PGlite 로 실 DB 검증)
```

**서비스 레이어 규칙:**
- 서비스는 트랜잭션·알림 팬아웃·이메일 아웃박스를 소유한다. 액션은 이를 직접 다루지 않는다.
- **알림 팬아웃 단일 API**: `lib/server/notifications/notify.ts`의 `notify(tx, input)` — in-app row insert와 email outbox enqueue를 채널(`inapp`/`email`) 지정 한 번으로 처리하고, 생성한 `Notification[]`을 반환한다(호출자가 `pendingEmits`에 모아 commit 후 emit). `rfp.ts`/`bid.ts`/`chat.ts`/`team-chat.ts`/`contract-signing.ts` 의 알림 발송이 모두 이 API로 통일되어 있다 — 신규 알림 발송 코드는 `dispatchNotification`+`outboxRepo.enqueue` 두 호출을 직접 조합하지 말고 `notify()`를 쓴다. **범위 밖(의도적)**: `auth.*`/`workspace.*` 서비스와 `lib/server/actions/workspace/_workspaceInviteNotify.ts`는 기존 `dispatchNotification`/outbox 직접 호출 패턴을 그대로 유지한다.
  - **삽입은 채널당 한 문장이다 (v0.4.47.0)**: 수신자 수와 무관하게 `dispatchNotifications`(→`notificationRepo.saveMany`)와 `outboxRepo.enqueueMany` 를 각각 1회 호출한다. 예전에는 수신자마다 단건 INSERT 를 돌아 트랜잭션이 팀 규모에 비례해 길어졌다. **수신자별로 값이 갈려야 하면 루프를 돌지 말고 함수를 넘긴다** — `dedupeKey: (r) => …`, `linkUrl: string | ((r) => …)`. 이 두 필드가 함수를 받기 전에는 호출부가 `recipients:[한 명]` 으로 루프를 도는 것 말고는 표현할 방법이 없었고, 그게 12곳에 퍼져 있었다.
  - **⚠️ `dedupeKey` 는 수신자마다 달라야 한다**: 전원이 같은 키를 내면 outbox 의 dedupe UNIQUE 에 걸려 **첫 1건 말고는 메일이 조용히 사라진다**(중복 억제가 목적일 때만 의도적으로 상수 키를 준다). 파라미터 이름은 타입이 아니라서 옛 시그니처(`(email) => …`)를 그대로 둔 호출부는 **컴파일을 통과한 채** 키가 `[object Object]` 로 굳는다 — `notify()` 가 런타임에 throw 해서 막는다.
- `Actor = { userId, workspaceId }` — 세션에서 추출해 액션이 서비스에 전달한다.
- `ServiceResult<T> = { ok: true } & T | { ok: false; error: string }` — 서비스 레이어 반환 타입. 예외 throw 없이 결과를 반환한다.
- `ActionResult<T> = { ok: true } & T | { ok: false; error: string }` — 액션 레이어 반환 타입 SSOT (`lib/server/actions/_result.ts`). 각 도메인 파일의 동일 타입 선언을 대체한다.
- 액션 공통 세션 헬퍼: `requireBuyerActor` / `requirePgActor` / `requireActiveWorkspace` (`lib/server/actions/_session.ts`) — 세션 검증·Actor 추출 로직의 단일 출처. 신규 액션 작성 시 `_shared.ts` 대신 이 헬퍼를 사용한다.
- 서비스 싱글턴은 Next.js `globalThis` 캐싱 패턴 사용 (`getRfpService()` / `getBidService()` 등).

**리포지토리 경계 (ESLint 강제):** 모든 DB 접근은 `lib/server/repositories/**` 가 소유한다. 그 밖의 `lib/`·`app/` 코드는 `@/lib/db/schema`·`@/lib/db/client` 를 **값(value)으로 정적 import 할 수 없다** — 레포를 주입(`repositories/factory` 의 `get*Repo()`)해서 쓴다. `import type { DB }`(타입 전용)와 서비스의 동적 `import('@/lib/db/client')`(트랜잭션 핸들)는 허용. 위반 시 lint 에러(`@typescript-eslint/no-restricted-imports`, 규칙명 `repo-boundary/db-access`) + 독립 드리프트 가드 테스트(`lib/server/__tests__/repo-boundary.test.ts`)가 잡는다. **의도적 예외**(`lib/server/db-boundary-allowlist.mjs` 에 명문화, 단일 출처): storage 바이트-블롭 티어(`storage/{postgres,index}.ts`), 크로스-애그리거트 캐스케이드(`_purgeUnverifiedSignup.ts`), `actionDb()` 테스트-오버라이드 레지스트리(`actions/auth/_shared.ts`). 예외를 늘리려면 allowlist 에 추가하고 리뷰한다.

## Linear Design Language — Hard Rules

These are non-negotiable visual decisions enforced across all screens. The design language is **Linear** — dense, fast, structure carried by low-contrast borders not shadows. Light-first; dark mode is Linear's signature near-black (`#08090A`). **Note:** token *names* keep the `--md-sys-*` prefix from the prior MD3 system — only the values are Linear. `md-sys` in the name does not mean MD3. DESIGN.md is the canonical source.

- **No** Inter/Roboto/Arial direct import. Pretendard Variable (KR + Latin, Inter-derived) + JetBrains Mono only.
- **No** pill buttons. Interactive elements are 6px (`shape-small`). `shape-full` (9999px) only for Avatars, status dots, pills indicators.
- **No** hover shadow promotion — hover is a background-lightness shift only.
- **No** heavy/skeuomorphic shadows — most surfaces use a 1px border or elevation-1; big shadows only on floating elements (popover, dropdown, toast, dialog, command palette).
- **No** high-contrast dividers — default to `outline-variant` (the deliberately low-contrast border). The faint border IS the Linear look, not a bug.
- **No** `outline` token as text/icon color — it's a **border** token (1.41:1 light / 1.45:1 dark against `surface`, fails AA). Use `on-surface-variant` for secondary text/icons. Sole exception: purely decorative `aria-hidden` separators, enumerated in `OUTLINE_TEXT_ALLOWLIST` (`lib/design/design-hardrule-allowlist.mjs`) and enforced by `lib/design/__tests__/outline-text-drift.test.ts`. `on-surface-variant` itself still misses AA on container-tier backgrounds — see DESIGN.md §2 and TODOS.md.
- **No** body text ≥ 16px — app body is 14px, dense (~32px rows, 28–36px buttons / default 32px).
- **No** accent gradients/neon/glassmorphism/blurred orbs. The accent is solid trust blue `#0061A4`. (단 하나의 좁은 예외: 랜딩 히어로 다크 씬 소프트 블룸 — DESIGN.md §9 랜딩·마케팅 예외 ⑤.)
- **No** illustrated empty states. Line SVGs (1.4–1.5 stroke) only.
- **로딩 모션 허용** — 넓은 영역은 펄스 스켈레톤, 인라인·타이핑 인디케이터는 펄스 점(staggered). `prefers-reduced-motion: reduce` 존중(저감 시 정지/단순화). 버튼 진행 등 짧은 진행 표시는 한국어(`처리 중…`/`불러오는 중이에요…`)로 통일 — 영문 `LOADING…` 은 폐지(DESIGN.md §6). 장식적 컨페티·강한 모멘텀 모션 제한은 유지(DESIGN.md §9 네 예외 — "축하 모먼트"·"테마 전환 리빌"·"브랜드 마크 진입"·"랜딩/마케팅 모션"). 자세히는 DESIGN.md §6 "로딩 모션".
- **No** № symbol (U+2116 NUMERO SIGN) anywhere — use plain numerics or zero-padded strings.
- **All** numerics (₩, qty, dates, RFP numbers like `P-2605-0042`) use `.md-numeric` class (mono + tabular-nums). Never on nav/labels/buttons.
- **Status** uses Chip component — never bracketed plain text `[ 결재중 ]`.
- **Typography** uses the typescale tokens — no `font-mono uppercase tracking` on labels/nav; sentence case with slight negative tracking.
- **Chip color** mapping: 성공/완료→tertiary, 실패/오류→error, 보류/신규→warning, 중립→surface, 주요→primary.
- **Motion** animates transform/opacity/color only (never layout); cause→effect under ~100ms (`duration-short-4`). 단, DESIGN.md §9의 네 예외(① "축하 모먼트" — 종결 성공 1회성 컨페티, ② "테마 전환 리빌" — View Transitions clip-path, ③ "브랜드 마크 진입" — 앱 셸 브랜드 마크의 1회성 SVG draw-on, ④ "랜딩·마케팅 모션" — 랜딩/마케팅 면은 스크롤 pin·진입 스케일·가이드 커서 등 몰입형 모션 및 `prefers-reduced-motion` 미존중 허용)는 별도.

If frontend code looks "generic SaaS", check DESIGN.md §9 (anti-patterns) before defending it.

## TDD — Hard Rules

모든 코드 변경은 `superpowers:test-driven-development` 스킬을 발동하고 **RED → GREEN → REFACTOR** 사이클로 진행한다. 이 스킬의 Iron Law가 본 프로젝트의 비결정 사항이다:

> **NO PRODUCTION CODE WITHOUT A FAILING TEST FIRST.**

- **Failing test 먼저, 구현 나중**. `__tests__/<name>.test.ts(x)`에 테스트를 작성하고 `pnpm test <path>`로 빨갛게 떨어지는 것을 직접 확인한 뒤 구현 코드를 작성한다.
- **테스트 후행 금지**. 구현부터 작성한 코드는 "참고용"으로도 남기지 말고 삭제 후 테스트부터 다시 시작. ("이미 X시간 썼는데 아까워서…"는 sunk-cost.)
- **즉시 통과한 테스트는 가짜 테스트**. RED를 직접 본 적 없으면 그 테스트는 무엇도 보장하지 않는다.
- **수동 브라우저 클릭은 테스트 대체재가 아니다**. 시각/UX 확인용이지 회귀 방지는 아니다 — 자동 테스트와 병행한다.
- **버그 픽스는 먼저 회귀 테스트로 재현**. 테스트가 빨갛게 뜨는 것을 보고 나서 픽스.
- **GREEN은 최소 코드만**. 통과시키기 위한 최소 구현 — 미래를 위한 옵션·파라미터·추상화 금지(YAGNI).

**TDD 면제 (그 외에는 모두 적용)**:
- 일회용 prototype/spike (커밋 안 함)
- 생성 코드 (codegen 산출물)
- 순수 설정 파일 (`*.config.*`, `eslint.config.mjs`, `drizzle.config.ts` 등)
- 시각/스타일만 손대는 변경 — 단 상태(state)·핸들러·조건 분기를 같이 추가하면 비예외.
- `app/**/page.tsx`·`app/**/layout.tsx` shell이 단순 컴포넌트 조립일 때 (안의 client component·server function 단위로 테스트).

면제에 해당해도 **확신이 안 서면 우선 테스트부터** — 30초 손해보다 회귀 한 번이 비싸다.

세부 RED-Flag 합리화 패턴(예: "이건 너무 사소해서…", "이미 수동으로 확인했어")과 cycle 가이드는 `superpowers:test-driven-development` 스킬 본문 참조.

## When Editing Documentation

The content docs (this file, `README.md`, `DESIGN.md`, `SCREEN_DESIGN.md`) cross-reference each other. After any change:
- If you edit DESIGN.md tokens → also bump `styles/tokens.css`. The canvas token (`--md-sys-color-background`) additionally drives browser/PWA chrome, and its JS copy lives in exactly one place: `lib/theme/canvas-colors.ts` (`CANVAS_COLOR`). `app/layout.tsx` (`viewport.themeColor` + the FOUC inline script), `app/manifest.ts`, and the runtime updater `lib/theme/chrome-color.ts` all dereference it — do not re-type the hex. `app/__tests__/chrome-colors.test.ts` reads `tokens.css` directly and fails if any link in that chain drifts, including if a hex literal reappears in layout/manifest. See DESIGN.md §2.
- If you add or change a screen/route → register it in SCREEN_DESIGN.md (§0 route map + screen table) and, for new top-level trees, the Routing Architecture block above. A new **public** page under `app/(public)` that must open while logged in (magic links) also needs registering in `ALWAYS_PASSTHROUGH_PREFIXES` (`lib/auth/route-decision.ts`) — `lib/auth/__tests__/public-routes-registered.test.ts` walks the folder and fails otherwise.
- **도메인 어휘는 각각 출처가 하나다** (v0.4.3.0 정리, 전부 드리프트 가드 테스트로 고정): 결제수단 = `PAYMENT_METHODS`(`lib/types/bid.ts`, 런타임 튜플이라 `z.enum()` 에 그대로 들어간다 — 라벨 누락은 컴파일러가(`PAYMENT_METHOD_LABELS` 가 `Record<PaymentMethod,_>`), 카테고리 누락은 드리프트 가드 테스트가 잡는다(`PAYMENT_METHOD_CATEGORIES` 는 평범한 배열이라 컴파일러가 못 잡는다 — `lib/types/__tests__/bid-tiers.test.ts`)), 가맹점 등급 = `MERCHANT_TIERS`(같은 파일 — `payment-fees-schema.ts` 의 구간 키가 여기서 파생하므로 손으로 나열하면 `.strict()` 가 신규 등급 요율을 조용히 거부한다), 현재 운영 솔루션 = `SOLUTION_VALUES`(`lib/types/rfp-terms.ts`, 라벨·옵션은 `lib/rfp/solutions.ts` 가 얹는다 — UI 는 `solutions.ts` 만, 서버·검증 계층은 `rfp-terms.ts` 만 임포트). 새 값 추가는 해당 배열 한 곳 + 라벨이며, 액션·스키마·위저드에 배열을 복제하지 않는다.
- 절을 추가·이동해도 파일 마지막 줄의 센티널 `<!-- CLAUDE.md: END -->` 는 **항상 맨 끝**에 남긴다 — 위 ⚠️ 블록의 절단 판별이 그 위치 하나에 걸려 있다(뒤에 절을 붙이면 온전한 로드가 잘린 것처럼 보인다).
- Canonical product rules now live in **code + tests + SCREEN_DESIGN.md 의 "확정 결정" 블록** (Context 절; the legacy `PG_RFP_SPEC.md` / `SPEC.md` docs were removed). If a decision contradicts the "확정 결정" rules, **stop and ask**.

## Skill routing (project-specific only)

대부분의 스킬은 description 자동 매칭에 의존한다. 아래는 프로젝트 특수 라우팅:

- `superpowers:test-driven-development` — **모든 신규 코드/버그픽스/리팩터링 직전 필수**. 면제 범위는 "TDD — Hard Rules" 참조.
- `/plan-eng-review` — M2 이후 새 기능 코딩 시작 전 (아키텍처 락인)
- `/design-review` — 화면 시각 폴리시 (Linear 디자인 시스템 정합 검증)
- `/investigate` — 버그·에러·예상치 못한 동작
- `/ship` — PR 생성·배포 단계

## Health Stack

`/health` 가 사용하는 도구 체인. e2e는 별도 (`/qa` 또는 `pnpm e2e`).

- typecheck: `pnpm tsc --noEmit`
- lint: `pnpm lint`
- test: `pnpm test`

TDD 사이클 중 단일 파일만 실행: `pnpm test <path-to-test>` — RED/GREEN 확인은 항상 단일 파일로 빠르게, 전체 그린 확인은 `pnpm test`.

**CI (`.github/workflows/ci.yml`)는 `main`·`dev` 대상 PR 과 `main` push 에서 세 잡(lint+tsc · unit · e2e)을 모두 돌린다** (2026-08-26 변경 — 그 전에는 e2e 가 `main` push 전용이라 PR 에서 required 인데도 **보고되지 않은 채** 통과했고, 깨진 스펙 6개가 두 달간 머지돼 들어왔다). e2e 로컬 실행 전제는 `docker compose --profile test up -d pg-test`(5433/`supporter_b_test`)이며, `pnpm e2e` 출력은 `| tail` 로 파이프하지 말 것 — 종료코드가 가려진다.

## Worktree Workflow

모든 기능 개발·버그픽스는 **worktree 브랜치**에서 진행한다. dev는 항상 clean 상태 유지.

- 새 작업 시작 시 `EnterWorktree` (네이티브 도구)로 `.worktrees/<branch-name>` 생성
- Worktree 디렉터리: `.worktrees/` (`.gitignore`에 등록됨)
- 브랜치 명명: `feat/<slug>`, `fix/<slug>`, `chore/<slug>`
- PR은 `/ship` 스킬로 생성
- PR 머지 후 `git worktree remove .worktrees/<name> && git branch -d <name>` 정리

**동의 없이 worktree를 자동 생성해도 된다.** 별도 확인 불필요.

<!-- CLAUDE.md: END -->
